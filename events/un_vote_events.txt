namespace = un_vote

# ============================================================================
# UN GENERAL ASSEMBLY VOTING EVENTS
# Three-phase vote on a UN resolution.
#
# Each resolution is a script container (tags, variables and lifecycle in
# common/scripted_effects/un_vote_effects.txt). un_resolution_open creates it
# from a proposer event (un_events) or a JE button, and every event here gets
# it as scope:un_resolution.
#
# Phase 1: un_vote.1 fires to every other UN member 30 days later. Votes go
#          into the resolution's un_res_yes / un_res_no lists.
# Phase 2: un_vote.2 fires to the proposer 365 days after opening. Its
#          immediate closes the vote; its option applies the outcome.
# Phase 3: un_vote.3 notifies every other member, who comply or refuse.
#
# Vote topics (tag un_topic_<topic> on the resolution):
#   condemn               - Condemn aggressor (event 2 / button)
#   human_rights          - Human Rights Declaration (event 3)
#   reform                - UN Institutional Reform (event 6)
#   heritage              - Cultural Heritage Program (event 9)
#   decolonization        - Decolonization Resolution (event 12)
#   npt                   - Non-Proliferation Treaty (event 14)
#   pandemic              - Global Pandemic Response (event 16)
#   climate               - Climate Accord (event 17)
#   refugee               - International Refugee Crisis (event 18)
#   space                 - International Space Cooperation (event 19)
#   law_of_sea            - Convention on the Law of the Sea (event 21)
#   icc                   - International Criminal Court (event 22)
#   peacekeeping_request  - Peacekeeping deployment (request button)
#   aid_request           - Humanitarian aid (request button)
#   sanctions             - International sanctions (sanctions button)
#   expulsion             - Strip a permanent member's seat (expulsion button)
#   military_mandate      - Authorize one member to recover one claimed state
#                           region from one censured state (mandate button).
#                           Binding and vetoable, with no graduated form: a
#                           veto blocks the authorization outright, exactly as
#                           it does for reform. The resolution carries two
#                           extra variables, un_res_mandate_region and
#                           un_res_mandate_beneficiary; carrying it un-vetoed
#                           creates the mandate container
#                           (common/scripted_effects/un_mandate_effects.txt).
# ============================================================================

# ---- Phase 1: All UN members vote on the proposed resolution ----
un_vote.1 = {
	type = country_event
	placement = ROOT

	event_image = { texture = "gfx/event_pictures/un_assembly_hall_vote.dds" }

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_map.dds"

	title = {
		first_valid = {
			triggered_desc = {
				desc = un_vote.1.t_condemn
				trigger = { scope:un_resolution ?= { has_tag = un_topic_condemn } }
			}
			triggered_desc = {
				desc = un_vote.1.t_human_rights
				trigger = { scope:un_resolution ?= { has_tag = un_topic_human_rights } }
			}
			triggered_desc = {
				desc = un_vote.1.t_npt
				trigger = { scope:un_resolution ?= { has_tag = un_topic_npt } }
			}
			triggered_desc = {
				desc = un_vote.1.t_climate
				trigger = { scope:un_resolution ?= { has_tag = un_topic_climate } }
			}
			triggered_desc = {
				desc = un_vote.1.t_reform
				trigger = { scope:un_resolution ?= { has_tag = un_topic_reform } }
			}
			triggered_desc = {
				desc = un_vote.1.t_heritage
				trigger = { scope:un_resolution ?= { has_tag = un_topic_heritage } }
			}
			triggered_desc = {
				desc = un_vote.1.t_decolonization
				trigger = { scope:un_resolution ?= { has_tag = un_topic_decolonization } }
			}
			triggered_desc = {
				desc = un_vote.1.t_pandemic
				trigger = { scope:un_resolution ?= { has_tag = un_topic_pandemic } }
			}
			triggered_desc = {
				desc = un_vote.1.t_refugee
				trigger = { scope:un_resolution ?= { has_tag = un_topic_refugee } }
			}
			triggered_desc = {
				desc = un_vote.1.t_space
				trigger = { scope:un_resolution ?= { has_tag = un_topic_space } }
			}
			triggered_desc = {
				desc = un_vote.1.t_peacekeeping_request
				trigger = { scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request } }
			}
			triggered_desc = {
				desc = un_vote.1.t_aid_request
				trigger = { scope:un_resolution ?= { has_tag = un_topic_aid_request } }
			}
			triggered_desc = {
				desc = un_vote.1.t_sanctions
				trigger = { scope:un_resolution ?= { has_tag = un_topic_sanctions } }
			}
			triggered_desc = {
				desc = un_vote.1.t_law_of_sea
				trigger = { scope:un_resolution ?= { has_tag = un_topic_law_of_sea } }
			}
			triggered_desc = {
				desc = un_vote.1.t_icc
				trigger = { scope:un_resolution ?= { has_tag = un_topic_icc } }
			}
			triggered_desc = {
				desc = un_vote.1.t_expulsion
				trigger = { scope:un_resolution ?= { has_tag = un_topic_expulsion } }
			}
			triggered_desc = {
				desc = un_vote.1.t_military_mandate
				trigger = { scope:un_resolution ?= { has_tag = un_topic_military_mandate } }
			}
			desc = un_vote.1.t
		}
	}
	desc = {
		first_valid = {
			# Target-perspective variants for the two topics that name a target.
			# Must come before the generic `_condemn` / `_sanctions` blocks so
			# they win first_valid when ROOT is the country being condemned/sanctioned.
			triggered_desc = {
				desc = un_vote.1.d_condemn_target
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_condemn }
					scope:un_vote_target ?= this
				}
			}
			triggered_desc = {
				desc = un_vote.1.d_sanctions_target
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					scope:un_vote_target ?= this
				}
			}
			triggered_desc = {
				desc = un_vote.1.d_military_mandate_target
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					scope:un_vote_target ?= this
				}
			}
			triggered_desc = {
				desc = un_vote.1.d_condemn
				trigger = { scope:un_resolution ?= { has_tag = un_topic_condemn } }
			}
			triggered_desc = {
				desc = un_vote.1.d_human_rights
				trigger = { scope:un_resolution ?= { has_tag = un_topic_human_rights } }
			}
			triggered_desc = {
				desc = un_vote.1.d_npt
				trigger = { scope:un_resolution ?= { has_tag = un_topic_npt } }
			}
			triggered_desc = {
				desc = un_vote.1.d_climate
				trigger = { scope:un_resolution ?= { has_tag = un_topic_climate } }
			}
			triggered_desc = {
				desc = un_vote.1.d_reform
				trigger = { scope:un_resolution ?= { has_tag = un_topic_reform } }
			}
			triggered_desc = {
				desc = un_vote.1.d_heritage
				trigger = { scope:un_resolution ?= { has_tag = un_topic_heritage } }
			}
			triggered_desc = {
				desc = un_vote.1.d_decolonization
				trigger = { scope:un_resolution ?= { has_tag = un_topic_decolonization } }
			}
			triggered_desc = {
				desc = un_vote.1.d_pandemic
				trigger = { scope:un_resolution ?= { has_tag = un_topic_pandemic } }
			}
			triggered_desc = {
				desc = un_vote.1.d_refugee
				trigger = { scope:un_resolution ?= { has_tag = un_topic_refugee } }
			}
			triggered_desc = {
				desc = un_vote.1.d_space
				trigger = { scope:un_resolution ?= { has_tag = un_topic_space } }
			}
			triggered_desc = {
				desc = un_vote.1.d_peacekeeping_request
				trigger = { scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request } }
			}
			triggered_desc = {
				desc = un_vote.1.d_aid_request
				trigger = { scope:un_resolution ?= { has_tag = un_topic_aid_request } }
			}
			triggered_desc = {
				desc = un_vote.1.d_sanctions
				trigger = { scope:un_resolution ?= { has_tag = un_topic_sanctions } }
			}
			triggered_desc = {
				desc = un_vote.1.d_law_of_sea
				trigger = { scope:un_resolution ?= { has_tag = un_topic_law_of_sea } }
			}
			triggered_desc = {
				desc = un_vote.1.d_icc
				trigger = { scope:un_resolution ?= { has_tag = un_topic_icc } }
			}
			# Target-perspective: voting member is the country being expelled
			triggered_desc = {
				desc = un_vote.1.d_expulsion_target
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_expulsion }
					scope:un_vote_target ?= this
				}
			}
			triggered_desc = {
				desc = un_vote.1.d_expulsion
				trigger = { scope:un_resolution ?= { has_tag = un_topic_expulsion } }
			}
			triggered_desc = {
				desc = un_vote.1.d_military_mandate
				trigger = { scope:un_resolution ?= { has_tag = un_topic_military_mandate } }
			}
			desc = un_vote.1.d
		}
	}
	flavor = un_vote.1.f

	duration = 3

	# scope:un_resolution comes from un_resolution_open. Queued events from
	# before containers have none and fail here.
	trigger = {
		je:je_united_nations ?= { has_modifier = un_member_modifier }
		scope:un_resolution ?= {
			has_tag = un_res_voting
			NOT = { un_resolution_has_vote = { LIST = un_res_yes } }
			NOT = { un_resolution_has_vote = { LIST = un_res_no } }
		}
	}

	immediate = {
		scope:un_resolution = {
			var:un_res_proposer ?= { save_scope_as = un_vote_proposer }
			var:un_res_target ?= { save_scope_as = un_vote_target }
		}
	}

	# Option A: Vote in favor
	option = {
		# Target-perspective override: voting "in favor" of a condemnation /
		# sanctions resolution against ROOT means accepting the verdict.
		name = {
			trigger = { scope:un_vote_target ?= this }
			text = un_vote.1.a_target
		}
		name = {
			trigger = { always = yes }
			text = un_vote.1.a
		}
		default_option = yes

		# Everything this option does — the treaty-topic show_as_tooltip preview,
		# the lobbying keep line, the recorded ballot, the target-acceptance branch
		# and the IG reactions — lives in un_vote_cast_yes
		# (common/scripted_effects/un_vote_cast_effects.txt), because the chamber
		# widget's Vote in Favour button casts the same ballot and must show the
		# same tooltip. Moved verbatim; the only addition there is the
		# already-voted guard.
		un_vote_cast_yes = yes

		ai_chance = {
			base = 40

			# ================================================================
			# SHARED PROPOSER-STANDING LEAN  <-- LOBBYING STAGE: ADD YOURS HERE
			# ================================================================
			# The Assembly listens harder to a member with a record. One block per
			# standing tier, literal adds (range -5 .. +5 against the base of 40),
			# kept together here rather than spread per topic. Literal adds on
			# purpose: no vanilla or mod event uses a named script value as an
			# ai_chance modifier's `add`, and a rejected value would fail silently.
			# The tier triggers are mutually exclusive and treat a proposer with no
			# recorded standing as Neutral, so at most one block ever applies.
			#
			# The lobbying stage should add its own block(s) right here, in the
			# same literal form, not fold itself into these. Standing and lobbying
			# have to stay separately tunable and separately attributable.
			#
			# Only the yes option is touched. Option B ("vote against") is left
			# exactly as it was: raising A already lowers B's share of the roll,
			# and a symmetric penalty there would double the effect of a
			# deliberately modest nudge.
			modifier = {
				trigger = { scope:un_vote_proposer ?= { un_standing_tier_exemplary = yes } }
				add = 5
			}
			modifier = {
				trigger = { scope:un_vote_proposer ?= { un_standing_tier_respected = yes } }
				add = 2
			}
			modifier = {
				trigger = { scope:un_vote_proposer ?= { un_standing_tier_poor = yes } }
				add = -2
			}
			modifier = {
				trigger = { scope:un_vote_proposer ?= { un_standing_tier_disgraced = yes } }
				add = -5
			}

			# ================================================================
			# RESOLUTION LOBBYING — A PLEDGED VOTE
			# ================================================================
			# We gave our word and took an obligation for it. Kept separate from
			# the standing block above on purpose: standing is a lean, this is a
			# promise, and the two must stay separately tunable and separately
			# attributable.
			#
			# +150 against a base of 40 dwarfs every other term in this block
			# (the largest of which is ±30), which together with the mirrored
			# penalties on option B and on the veto is what "a committed AI
			# honours its pledge" means in practice. It is a weight, not a
			# bypass: the AI still runs the event and could in principle roll
			# another way, and the option itself is never removed.
			#
			# Literal, like every other add here — a named script value in an
			# ai_chance modifier is not a shape any vanilla or mod event uses and
			# a rejected one fails silently (see *Why literals* above).
			modifier = {
				trigger = { un_lobby_holds_pending_commitment = yes }
				add = 150
			}

			# ================================================================
			# DIPLOMATIC RELATIONSHIP MODIFIERS
			# ================================================================

			# Allies of proposer lean support
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					has_treaty_alliance_with = { TARGET = scope:un_vote_proposer }
				}
				add = 20
			}
			# Same power bloc as proposer: bloc solidarity
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					is_in_same_power_bloc = scope:un_vote_proposer
				}
				add = 15
			}
			# Diplomatically relevant proposer: pay more attention
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					has_diplomatic_relevance = scope:un_vote_proposer
				}
				add = 5
			}
			# Rivals of proposer lean oppose
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					has_diplomatic_pact = {
						who = scope:un_vote_proposer
						type = rivalry
					}
				}
				add = -30
			}
			# Great powers more opinionated
			modifier = {
				trigger = { country_rank >= rank_value:great_power }
				factor = 1.5
			}
			# Target accepted the resolution: lowers political cost for other
			# voters to support it ("permission" effect). Gated on NOT-target so
			# the target's own ai_chance doesn't double-dip its own gesture.
			# Note: vote events fire concurrently; only AI evaluated AFTER the
			# target's tick will see the tag — partial coverage by design.
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_res_target_accepted }
					NOT = { scope:un_vote_target ?= this }
				}
				add = 15
			}

			# ================================================================
			# INFAMY-BURNING INCENTIVE: countries with high infamy support
			# humanitarian votes to rehabilitate their image
			# ================================================================
			modifier = {
				trigger = {
					infamy >= 25
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_aid_request }
						scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
						scope:un_resolution ?= { has_tag = un_topic_human_rights }
						scope:un_resolution ?= { has_tag = un_topic_refugee }
						scope:un_resolution ?= { has_tag = un_topic_pandemic }
					}
				}
				add = 25
			}
			modifier = {
				trigger = {
					infamy >= 50
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_aid_request }
						scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
						scope:un_resolution ?= { has_tag = un_topic_human_rights }
						scope:un_resolution ?= { has_tag = un_topic_refugee }
						scope:un_resolution ?= { has_tag = un_topic_pandemic }
					}
				}
				add = 15
			}

			# ================================================================
			# DIMINISHING RETURNS: each existing commitment reduces enthusiasm
			# for pledging more (stacking penalty per program funded)
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_aid_request }
					has_variable = un_aid_contribution_count
					var:un_aid_contribution_count >= 1
				}
				add = -15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_aid_request }
					has_variable = un_aid_contribution_count
					var:un_aid_contribution_count >= 2
				}
				add = -15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_aid_request }
					has_variable = un_aid_contribution_count
					var:un_aid_contribution_count >= 3
				}
				add = -15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					has_variable = un_peacekeeping_contribution_count
					var:un_peacekeeping_contribution_count >= 1
				}
				add = -15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					has_variable = un_peacekeeping_contribution_count
					var:un_peacekeeping_contribution_count >= 2
				}
				add = -15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					has_variable = un_peacekeeping_contribution_count
					var:un_peacekeeping_contribution_count >= 3
				}
				add = -15
			}

			# Power bloc leaders are more willing to fund aid/peacekeeping (leverage gain)
			modifier = {
				trigger = {
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_aid_request }
						scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					}
					is_power_bloc_leader = yes
				}
				add = 20
			}

			# ================================================================
			# TOPIC: Condemn aggressor
			# ================================================================
			# Support if target is our rival
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_condemn }
					exists = scope:un_vote_target
					has_diplomatic_pact = {
						who = scope:un_vote_target
						type = rivalry
					}
				}
				add = 30
			}
			# Oppose if target is our ally
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_condemn }
					exists = scope:un_vote_target
					has_treaty_alliance_with = { TARGET = scope:un_vote_target }
				}
				add = -40
			}
			# Same power bloc as target: protect bloc member
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_condemn }
					exists = scope:un_vote_target
					is_in_same_power_bloc = scope:un_vote_target
				}
				add = -20
			}
			# Peaceful nations support condemnation
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_condemn }
					has_law = law_type:law_humanitarian_regulations
				}
				add = 15
			}

			# ================================================================
			# TOPIC: Human Rights
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_human_rights }
					has_law = law_type:law_multicultural
				}
				add = 20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_human_rights }
					has_law = law_type:law_universal_suffrage
				}
				add = 15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_human_rights }
					has_law = law_type:law_ethnostate
				}
				add = -40
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_human_rights }
					has_law = law_type:law_outlawed_dissent
				}
				add = -30
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_human_rights }
					has_law = law_type:law_guaranteed_liberties
				}
				add = 15
			}

			# ================================================================
			# TOPIC: Non-Proliferation
			# ================================================================
			# Nations without nukes support disarmament
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_npt }
					NOT = {
						has_variable = nuclear_weapon_stockpile
					}
				}
				add = 20
			}
			# Nuclear powers resist
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_npt }
					has_variable = nuclear_weapon_stockpile
					var:nuclear_weapon_stockpile >= 1
				}
				add = -30
			}
			# Those developing nukes strongly oppose
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_npt }
					has_journal_entry = je_nuclear_program
				}
				add = -25
			}

			# ================================================================
			# TOPIC: Climate Accord
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_climate }
					has_technology_researched = green_revolution
				}
				add = 20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_climate }
					has_technology_researched = environmental_movement
				}
				add = 10
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_climate }
					has_law = law_type:law_laissez_faire
				}
				add = -20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_climate }
					has_law = law_type:law_command_economy
				}
				add = -10
			}

			# ================================================================
			# TOPIC: UN Reform
			# ================================================================
			# Countries frustrated with the current order support reform
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_reform }
					global_var:un_authority < 40
				}
				add = 15
			}
			# SC members resist losing their privilege
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_reform }
					country_rank >= rank_value:great_power
					je:je_united_nations ?= { has_modifier = un_security_council_modifier }
				}
				add = -20
			}

			# ================================================================
			# TOPIC: Heritage
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_heritage }
					has_law = law_type:law_multicultural
				}
				add = 15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_heritage }
					country_rank >= rank_value:major_power
				}
				add = 10
			}

			# ================================================================
			# TOPIC: Decolonization
			# ================================================================
			# Colonial powers strongly oppose
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_decolonization }
					any_subject_or_below = {
						is_direct_subject_of = ROOT
					}
				}
				add = -40
			}
			# Anti-imperialist nations support
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_decolonization }
					has_law = law_type:law_council_republic
				}
				add = 20
			}
			# Non-colonial nations lean in favor
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_decolonization }
					NOT = {
						any_subject_or_below = {
							count >= 1
						}
					}
				}
				add = 15
			}

			# ================================================================
			# TOPIC: Pandemic
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_pandemic }
					has_law = law_type:law_worker_protections
				}
				add = 15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_pandemic }
					has_law = law_type:law_isolationism
				}
				add = -20
			}

			# ================================================================
			# TOPIC: Refugee
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_refugee }
					has_law = law_type:law_no_migration_controls
				}
				add = 20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_refugee }
					has_law = law_type:law_closed_borders
				}
				add = -30
			}

			# ================================================================
			# TOPIC: Space
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_space }
					has_technology_researched = space_exploration
				}
				add = 20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_space }
					has_law = law_type:law_single_party_state
				}
				add = -15
			}

			# ================================================================
			# TOPIC: Peacekeeping Request
			# ================================================================
			# Humanitarian nations support
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					has_law = law_type:law_humanitarian_regulations
				}
				add = 25
			}
			# GPs want to project power
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					country_rank >= rank_value:great_power
				}
				add = 15
			}
			# Isolationists resist
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					has_law = law_type:law_isolationism
				}
				add = -20
			}

			# ================================================================
			# TOPIC: Humanitarian Aid Request
			# ================================================================
			# Wealthy nations more likely to support
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_aid_request }
					country_rank >= rank_value:great_power
				}
				add = 20
			}
			# Interventionist/humanitarian support
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_aid_request }
					has_law = law_type:law_humanitarian_regulations
				}
				add = 20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_aid_request }
					has_law = law_type:law_isolationism
				}
				add = -25
			}

			# ================================================================
			# TOPIC: Sanctions
			# ================================================================
			# Rivals of the target support sanctions
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					exists = scope:un_vote_target
					has_diplomatic_pact = { who = scope:un_vote_target type = rivalry }
				}
				add = 30
			}
			# Same power bloc as target: protect bloc member
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					exists = scope:un_vote_target
					is_in_same_power_bloc = scope:un_vote_target
				}
				add = -25
			}
			# Great powers want to project authority
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					country_rank >= rank_value:great_power
				}
				add = 15
			}
			# Humanitarian nations support sanctions against aggressors
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					OR = {
						has_law = law_type:law_humanitarian_regulations
						has_law = law_type:law_limited_war
					}
				}
				add = 15
			}
			# Isolationists resist involvement
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					has_law = law_type:law_isolationism
				}
				add = -20
			}

			# ================================================================
			# TOPIC: Law of the Sea
			# ================================================================
			# Free traders support maritime regulation
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_law_of_sea }
					has_law = law_type:law_free_trade
				}
				add = 20
			}
			# Great powers benefit from codified maritime rules
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_law_of_sea }
					country_rank >= rank_value:great_power
				}
				add = 10
			}
			# Isolationists oppose
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_law_of_sea }
					has_law = law_type:law_isolationism
				}
				add = -15
			}

			# ================================================================
			# TOPIC: International Criminal Court
			# ================================================================
			# Democracies support accountability
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					has_law = law_type:law_universal_suffrage
				}
				add = 15
			}
			# Human rights-conscious nations support
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					has_law = law_type:law_multicultural
				}
				add = 10
			}
			# Great powers wary of sovereignty loss
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					country_rank >= rank_value:great_power
				}
				add = -20
			}
			# Warring nations strongly oppose
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					is_at_war = yes
				}
				add = -30
			}
			# Authoritarian governments oppose accountability
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					has_law = law_type:law_single_party_state
				}
				add = -25
			}

			# ================================================================
			# TOPIC: Authorized military mandate
			# ================================================================
			# Licensing force is the Assembly's gravest act, so the base lean
			# is against and the inputs are the same ones the other
			# target-naming topics use: how we stand to the target, how we
			# stand to the proposer, and how we read the use of force.
			# Authorising a war against our ally or bloc partner is
			# unthinkable; authorising one against a rival is cheap.
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
				}
				add = -25
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					exists = scope:un_vote_target
					has_treaty_alliance_with = { TARGET = scope:un_vote_target }
				}
				add = -60
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					exists = scope:un_vote_target
					is_in_same_power_bloc = scope:un_vote_target
				}
				add = -30
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					exists = scope:un_vote_target
					has_diplomatic_pact = {
						who = scope:un_vote_target
						type = rivalry
					}
				}
				add = 35
			}
			# The target already stands censured by this Assembly — the case
			# for enforcement makes itself.
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					exists = scope:un_vote_target
					scope:un_vote_target = { has_modifier = un_condemned_modifier }
				}
				add = 25
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					exists = scope:un_vote_target
					scope:un_vote_target = { has_modifier = un_sanctions_target_modifier }
				}
				add = 20
			}
			# Allies and bloc partners of the proposer back their own.
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					exists = scope:un_vote_proposer
					has_treaty_alliance_with = { TARGET = scope:un_vote_proposer }
				}
				add = 25
			}
			# Doctrine: states that have restrained war by law are the least
			# willing to license it; states that have not are the most.
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					has_law = law_type:law_humanitarian_regulations
				}
				add = -20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					has_law = law_type:law_isolationism
				}
				add = -25
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					has_law = law_type:law_total_war
				}
				add = 15
			}
			# A strong Assembly can afford to act; a weak one cannot carry it.
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					has_global_variable = un_authority
					global_var:un_authority >= 70
				}
				add = 15
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					has_global_variable = un_authority
					global_var:un_authority < 50
				}
				add = -20
			}
			# Nobody wants a precedent that could be turned on them: a state
			# that is itself censurable enough to be a future target votes its
			# fears. Same test the propose button applies to the target
			# (un_mandate_target_is_notorious).
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					un_mandate_target_is_notorious = yes
				}
				add = -25
			}
		}
	}

	# Option B: Vote against
	option = {
		# Target-perspective override: voting "against" a condemnation /
		# sanctions resolution targeting ROOT means rejecting the charges.
		name = {
			trigger = { scope:un_vote_target ?= this }
			text = un_vote.1.b_target
		}
		name = {
			trigger = { always = yes }
			text = un_vote.1.b
		}

		# Lobbying: a human recipient keeps a free choice here, and this is where
		# it is told what going back on its pledge costs. Same reason as option
		# A for sitting outside the custom_tooltip wrapper.
		# Body shared with the chamber widget's Vote Against button — see
		# un_vote_cast_no in common/scripted_effects/un_vote_cast_effects.txt.
		un_vote_cast_no = yes
		ai_chance = {
			base = 10

			# ================================================================
			# RESOLUTION LOBBYING — THE MIRROR OF OPTION A's PLEDGE BONUS
			# ================================================================
			# Voting against a resolution we pledged to carry. The one place in
			# this event where a negative mirror is warranted: option A's lean
			# terms are nudges that only need to move a probability, but a
			# pledge is supposed to be kept, and leaving option B untouched
			# would let an AI with strong independent reasons to oppose take the
			# obligation and then renege anyway.
			modifier = {
				trigger = { un_lobby_holds_pending_commitment = yes }
				add = -100
			}

			# ================================================================
			# DIPLOMATIC RELATIONSHIP MODIFIERS
			# ================================================================

			# Rivals of proposer lean oppose
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					has_diplomatic_pact = {
						who = scope:un_vote_proposer
						type = rivalry
					}
				}
				add = 30
			}
			# Same power bloc as proposer: bloc solidarity discourages opposition
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					is_in_same_power_bloc = scope:un_vote_proposer
				}
				add = -20
			}
			# Allies of proposer less likely to oppose openly
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					has_treaty_alliance_with = { TARGET = scope:un_vote_proposer }
				}
				add = -15
			}
			# Great powers more willing to take public stands
			modifier = {
				trigger = { country_rank >= rank_value:great_power }
				factor = 1.3
			}

			# ================================================================
			# INFAMY-CONSCIOUS: high infamy countries avoid opposing humanitarian votes
			# (voting no on human rights looks bad)
			# ================================================================
			modifier = {
				trigger = {
					infamy >= 25
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_human_rights }
						scope:un_resolution ?= { has_tag = un_topic_refugee }
						scope:un_resolution ?= { has_tag = un_topic_pandemic }
					}
				}
				add = -20
			}

			# ================================================================
			# TOPIC: Condemn - allies of target oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_condemn }
					exists = scope:un_vote_target
					has_treaty_alliance_with = { TARGET = scope:un_vote_target }
				}
				add = 40
			}
			# Same power bloc as target: protect bloc member
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_condemn }
					exists = scope:un_vote_target
					is_in_same_power_bloc = scope:un_vote_target
				}
				add = 25
			}

			# ================================================================
			# TOPIC: Human Rights - authoritarian regimes oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_human_rights }
					has_law = law_type:law_ethnostate
				}
				add = 30
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_human_rights }
					has_law = law_type:law_outlawed_dissent
				}
				add = 25
			}

			# ================================================================
			# TOPIC: NPT - nuclear powers oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_npt }
					has_variable = nuclear_weapon_stockpile
					var:nuclear_weapon_stockpile >= 1
				}
				add = 35
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_npt }
					has_journal_entry = je_nuclear_program
				}
				add = 25
			}

			# ================================================================
			# TOPIC: Climate - industrial economies oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_climate }
					has_law = law_type:law_laissez_faire
				}
				add = 25
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_climate }
					has_law = law_type:law_command_economy
				}
				add = 15
			}

			# ================================================================
			# TOPIC: Reform - SC privileged members oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_reform }
					je:je_united_nations ?= { has_modifier = un_security_council_modifier }
				}
				add = 25
			}

			# ================================================================
			# TOPIC: Decolonization - colonial powers oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_decolonization }
					any_subject_or_below = {
						is_direct_subject_of = ROOT
					}
				}
				add = 40
			}

			# ================================================================
			# TOPIC: Pandemic - isolationists oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_pandemic }
					has_law = law_type:law_isolationism
				}
				add = 25
			}

			# ================================================================
			# TOPIC: Refugee - closed borders oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_refugee }
					has_law = law_type:law_closed_borders
				}
				add = 30
			}

			# ================================================================
			# TOPIC: Space - authoritarian regimes oppose sharing
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_space }
					has_law = law_type:law_single_party_state
				}
				add = 20
			}

			# ================================================================
			# TOPIC: Peacekeeping Request - isolationists/warmongers oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					has_law = law_type:law_isolationism
				}
				add = 25
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					has_law = law_type:law_total_war
				}
				add = 15
			}

			# ================================================================
			# TOPIC: Aid Request - laissez-faire/isolationists oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_aid_request }
					has_law = law_type:law_isolationism
				}
				add = 25
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_aid_request }
					has_law = law_type:law_laissez_faire
				}
				add = 15
			}

			# ================================================================
			# TOPIC: Sanctions - allies of target oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					exists = scope:un_vote_target
					has_treaty_alliance_with = { TARGET = scope:un_vote_target }
				}
				add = 40
			}
			# Same power bloc as target: protect bloc member
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					exists = scope:un_vote_target
					is_in_same_power_bloc = scope:un_vote_target
				}
				add = 25
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					has_law = law_type:law_isolationism
				}
				add = 20
			}

			# ================================================================
			# TOPIC: Law of the Sea - protectionists oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_law_of_sea }
					has_law = law_type:law_protectionism
				}
				add = 20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_law_of_sea }
					has_law = law_type:law_isolationism
				}
				add = 25
			}

			# ================================================================
			# TOPIC: ICC - great powers and warring nations oppose
			# ================================================================
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					country_rank >= rank_value:great_power
				}
				add = 30
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					is_at_war = yes
				}
				add = 40
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					has_law = law_type:law_single_party_state
				}
				add = 30
			}
		}
	}

	# Option C: VETO (permanent members only, on binding topics only)
	# Hard-blocks the full binding form of the resolution. The GA simple-majority
	# tally is preserved — if it still passes, the graduated/weak form applies
	# (see un_vote.2). Costs: UN authority drain, isolation modifier, and infamy
	# on punitive topics (ICC, condemn, peacekeeping).
	option = {
		name = {
			trigger = { scope:un_vote_target ?= this }
			text = un_vote.1.veto
		}
		name = {
			trigger = { always = yes }
			text = un_vote.1.veto
		}
		trigger = {
			is_un_permanent_member = yes
			scope:un_resolution ?= { un_resolution_is_binding = yes }
		}

		# Lobbying: the veto right is never gated on a pledge — this option's own
		# trigger is untouched — but a veto books a NO vote, so it settles a
		# commitment as broken. Say so before it is pressed.
		# Body shared with the chamber widget's Veto control — see
		# un_vote_cast_veto in common/scripted_effects/un_vote_cast_effects.txt.
		# This option's own trigger above stays the veto's eligibility gate on the
		# event path.
		un_vote_cast_veto = yes
		ai_chance = {
			base = 0

			# Resolution lobbying: a pledged vote weighs against reaching for the
			# veto at all. The RIGHT is untouched (this option's trigger never
			# reads a commitment) — this is only the AI declining to spend it on
			# a resolution it promised to carry.
			modifier = {
				trigger = { un_lobby_holds_pending_commitment = yes }
				add = -100
			}

			# Only consider vetoing when the resolution would otherwise pass.
			# (un_veto_would_change_outcome checks a margin >= 2, meaning even our
			# "no" alone wouldn't sink it — so a veto is needed to actually block.)
			modifier = {
				trigger = { un_veto_would_change_outcome = yes }
				add = 30
			}

			# Strongly veto rivals' proposals
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					has_diplomatic_pact = { who = scope:un_vote_proposer type = rivalry }
				}
				add = 35
			}

			# Protect allied / same-bloc targets from sanctions or condemnation
			modifier = {
				trigger = {
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_sanctions }
						scope:un_resolution ?= { has_tag = un_topic_condemn }
					}
					exists = scope:un_vote_target
					has_treaty_alliance_with = { TARGET = scope:un_vote_target }
				}
				add = 40
			}
			modifier = {
				trigger = {
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_sanctions }
						scope:un_resolution ?= { has_tag = un_topic_condemn }
					}
					exists = scope:un_vote_target
					is_in_same_power_bloc = scope:un_vote_target
				}
				add = 25
			}

			# ICC referrals targeting US or our allies are existential — strongly veto
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					OR = {
						scope:un_vote_target ?= this
						AND = {
							exists = scope:un_vote_target
							has_treaty_alliance_with = { TARGET = scope:un_vote_target }
						}
					}
				}
				add = 50
			}

			# Reform that would weaken SC privileges (i.e., proposer is non-permanent)
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_reform }
					exists = scope:un_vote_proposer
					NOT = { scope:un_vote_proposer = { is_un_permanent_member = yes } }
				}
				add = 30
			}

			# Authorizing force is the thing the veto exists for. A permanent
			# member leans toward blocking any mandate, and blocks outright when
			# the country about to be invaded is its ally or bloc partner —
			# there is no graduated form to fall back on, so the veto is the
			# only instrument available.
			modifier = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_military_mandate } }
				add = 20
			}
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					exists = scope:un_vote_target
					OR = {
						scope:un_vote_target ?= this
						has_treaty_alliance_with = { TARGET = scope:un_vote_target }
						is_in_same_power_bloc = scope:un_vote_target
					}
				}
				add = 55
			}
			# ...but not against a rival we would rather see cut down to size.
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					exists = scope:un_vote_target
					has_diplomatic_pact = {
						who = scope:un_vote_target
						type = rivalry
					}
				}
				add = -40
			}

			# Peacekeeping deployment requested by an aligned target — let it go
			# (we want our friends helped); but veto when requested by a rival's bloc
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					exists = scope:un_vote_proposer
					NOT = { is_in_same_power_bloc = scope:un_vote_proposer }
					NOT = { has_treaty_alliance_with = { TARGET = scope:un_vote_proposer } }
				}
				add = 10
			}

			# Costs / restraints — discourage vetoing when:
			# Our UN authority is already weak (cost is steep)
			modifier = {
				trigger = {
					has_global_variable = un_authority
					global_var:un_authority < 30
				}
				add = -25
			}
			# We've recently vetoed (cool down)
			modifier = {
				trigger = { has_modifier = un_veto_isolation_modifier }
				add = -20
			}
			# Our infamy is already high and this topic would add more
			modifier = {
				trigger = {
					infamy >= 25
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_icc }
						scope:un_resolution ?= { has_tag = un_topic_condemn }
						scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					}
				}
				add = -15
			}
		}
	}
}

# ---- Phase 2: Vote results - fires for the proposer 365 days after opening ----
un_vote.2 = {
	type = country_event
	placement = ROOT

	event_image = { texture = "gfx/event_pictures/un_assembly_hall_vote.dds" }

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_map.dds"

	title = un_vote.2.t

	desc = {
		first_valid = {
			# Condemn - Passed (vetoed: non-binding rebuke)
			triggered_desc = {
				trigger = {
					scope:un_resolution ?= {
						has_tag = un_topic_condemn
						has_tag = un_res_passed
						has_tag = un_res_vetoed
					}
				}
				desc = un_vote.2.d_condemn_passed_vetoed
			}
			# Condemn - Passed (full binding condemnation)
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_condemn has_tag = un_res_passed } }
				desc = un_vote.2.d_condemn_passed
			}
			# Condemn - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_condemn } }
				desc = un_vote.2.d_condemn_failed
			}
			# Human Rights - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_human_rights has_tag = un_res_passed } }
				desc = un_vote.2.d_human_rights_passed
			}
			# Human Rights - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_human_rights } }
				desc = un_vote.2.d_human_rights_failed
			}
			# NPT - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_npt has_tag = un_res_passed } }
				desc = un_vote.2.d_npt_passed
			}
			# NPT - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_npt } }
				desc = un_vote.2.d_npt_failed
			}
			# Climate - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_climate has_tag = un_res_passed } }
				desc = un_vote.2.d_climate_passed
			}
			# Climate - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_climate } }
				desc = un_vote.2.d_climate_failed
			}
			# Reform - Passed (vetoed: flat block)
			triggered_desc = {
				trigger = {
					scope:un_resolution ?= {
						has_tag = un_topic_reform
						has_tag = un_res_passed
						has_tag = un_res_vetoed
					}
				}
				desc = un_vote.2.d_reform_passed_vetoed
			}
			# Reform - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_reform has_tag = un_res_passed } }
				desc = un_vote.2.d_reform_passed
			}
			# Reform - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_reform } }
				desc = un_vote.2.d_reform_failed
			}
			# Heritage - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_heritage has_tag = un_res_passed } }
				desc = un_vote.2.d_heritage_passed
			}
			# Heritage - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_heritage } }
				desc = un_vote.2.d_heritage_failed
			}
			# Decolonization - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_decolonization has_tag = un_res_passed } }
				desc = un_vote.2.d_decolonization_passed
			}
			# Decolonization - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_decolonization } }
				desc = un_vote.2.d_decolonization_failed
			}
			# Pandemic - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_pandemic has_tag = un_res_passed } }
				desc = un_vote.2.d_pandemic_passed
			}
			# Pandemic - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_pandemic } }
				desc = un_vote.2.d_pandemic_failed
			}
			# Refugee - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_refugee has_tag = un_res_passed } }
				desc = un_vote.2.d_refugee_passed
			}
			# Refugee - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_refugee } }
				desc = un_vote.2.d_refugee_failed
			}
			# Space - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_space has_tag = un_res_passed } }
				desc = un_vote.2.d_space_passed
			}
			# Space - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_space } }
				desc = un_vote.2.d_space_failed
			}
			# Peacekeeping Request - Passed (vetoed: observer mission only)
			triggered_desc = {
				trigger = {
					scope:un_resolution ?= {
						has_tag = un_topic_peacekeeping_request
						has_tag = un_res_passed
						has_tag = un_res_vetoed
					}
				}
				desc = un_vote.2.d_peacekeeping_request_passed_vetoed
			}
			# Peacekeeping Request - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request has_tag = un_res_passed } }
				desc = un_vote.2.d_peacekeeping_request_passed
			}
			# Peacekeeping Request - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request } }
				desc = un_vote.2.d_peacekeeping_request_failed
			}
			# Aid Request - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_aid_request has_tag = un_res_passed } }
				desc = un_vote.2.d_aid_request_passed
			}
			# Aid Request - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_aid_request } }
				desc = un_vote.2.d_aid_request_failed
			}
			# Sanctions - Passed (vetoed: voluntary partial sanctions)
			triggered_desc = {
				trigger = {
					scope:un_resolution ?= {
						has_tag = un_topic_sanctions
						has_tag = un_res_passed
						has_tag = un_res_vetoed
					}
				}
				desc = un_vote.2.d_sanctions_passed_vetoed
			}
			# Sanctions - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_sanctions has_tag = un_res_passed } }
				desc = un_vote.2.d_sanctions_passed
			}
			# Sanctions - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_sanctions } }
				desc = un_vote.2.d_sanctions_failed
			}
			# Law of the Sea - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_law_of_sea has_tag = un_res_passed } }
				desc = un_vote.2.d_law_of_sea_passed
			}
			# Law of the Sea - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_law_of_sea } }
				desc = un_vote.2.d_law_of_sea_failed
			}
			# International Criminal Court - Passed (vetoed: symbolic censure only)
			triggered_desc = {
				trigger = {
					scope:un_resolution ?= {
						has_tag = un_topic_icc
						has_tag = un_res_passed
						has_tag = un_res_vetoed
					}
				}
				desc = un_vote.2.d_icc_passed_vetoed
			}
			# International Criminal Court - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_icc has_tag = un_res_passed } }
				desc = un_vote.2.d_icc_passed
			}
			# International Criminal Court - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_icc } }
				desc = un_vote.2.d_icc_failed
			}
			# Military Mandate - Passed but vetoed (blocked outright, as reform)
			triggered_desc = {
				trigger = {
					scope:un_resolution ?= {
						has_tag = un_topic_military_mandate
						has_tag = un_res_passed
						has_tag = un_res_vetoed
					}
				}
				desc = un_vote.2.d_military_mandate_passed_vetoed
			}
			# Military Mandate - Passed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_military_mandate has_tag = un_res_passed } }
				desc = un_vote.2.d_military_mandate_passed
			}
			# Military Mandate - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_military_mandate } }
				desc = un_vote.2.d_military_mandate_failed
			}
			# Expulsion - Passed with promotion (2/3 supermajority; vacated seat filled)
			triggered_desc = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_expulsion has_tag = un_res_passed }
					exists = scope:un_vote_promoted
				}
				desc = un_vote.2.d_expulsion_passed_promoted
			}
			# Expulsion - Passed but no eligible challenger (seat left vacant)
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_expulsion has_tag = un_res_passed } }
				desc = un_vote.2.d_expulsion_passed
			}
			# Expulsion - Failed
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_topic_expulsion } }
				desc = un_vote.2.d_expulsion_failed
			}
			# Generic fallback
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_res_passed } }
				desc = un_vote.2.d_passed
			}
			desc = un_vote.2.d_failed
		}
	}
	flavor = {
		first_valid = {
			triggered_desc = {
				desc = un_vote.2.f_passed
				trigger = { scope:un_resolution ?= { has_tag = un_res_passed } }
			}
			desc = un_vote.2.f_failed
		}
	}

	duration = 3

	# scope:un_resolution comes from un_resolution_open. A resolution the
	# watchdog already lapsed, or a queued event from before containers, fails here.
	trigger = {
		je:je_united_nations ?= { has_modifier = un_member_modifier }
		scope:un_resolution ?= { has_tag = un_res_voting }
	}

	immediate = {
		# Close the vote: tag passed/failed, start the topic cooldown, file it in
		# un_resolution_history and free the vote lock. The option below applies
		# the outcome; everything it reads is on the resolution, not the lock.
		scope:un_resolution = {
			un_resolution_decide = yes
			var:un_res_proposer ?= { save_scope_as = un_vote_proposer }
			var:un_res_target ?= { save_scope_as = un_vote_target }
		}
		# Vote counts as country vars for loc display (loc has no accessor
		# for a script container held in a saved scope)
		set_variable = {
			name = un_display_support
			value = scope:un_resolution.var:un_res_support
		}
		set_variable = {
			name = un_display_oppose
			value = scope:un_resolution.var:un_res_oppose
		}
		# Successful-expulsion promotion: pick the highest-ranking eligible
		# non-permanent UN member to fill the vacated seat. Saved as scope so
		# the description below and the option block below can reference it.
		# Eligibility: UN member, GP rank, not already permanent, not the
		# country we are about to expel. If no candidate exists, scope is
		# unset and the seat goes vacant (description falls back accordingly).
		if = {
			limit = {
				scope:un_resolution ?= {
					has_tag = un_topic_expulsion
					has_tag = un_res_passed
				}
			}
			ordered_country = {
				limit = {
					je:je_united_nations ?= { has_modifier = un_member_modifier }
					country_rank >= rank_value:great_power
					NOT = { is_un_permanent_member = yes }
					NOT = { this ?= scope:un_vote_target }
				}
				order_by = country_rank
				max = 1
				save_scope_as = un_vote_promoted
				scope:un_resolution = { set_variable = { name = un_res_promoted value = scope:un_vote_promoted } }
			}
		}
		scope:un_resolution = { un_resolution_archive = yes }
	}

	# Single option: acknowledge the results
	option = {
		name = un_vote.2.a
		default_option = yes

		# ---- EXPULSION VOTE (2/3 supermajority threshold; non-vetoable) ----
		# Handled separately because the pass condition is supermajority, not
		# simple majority. The standard pass/fail branches below are gated to
		# skip when the topic is expulsion.
		if = {
			limit = {
				scope:un_resolution ?= {
					has_tag = un_topic_expulsion
					has_tag = un_res_passed
				}
			}
			custom_tooltip = un_vote_expulsion_passed_tt
			change_global_variable = { name = un_authority add = 5 }
			if = {
				limit = { exists = scope:un_vote_target }
				scope:un_vote_target = {
					je:je_united_nations ?= { remove_modifier = un_permanent_member_modifier }
					je:je_united_nations ?= { remove_modifier = un_security_council_modifier }
					add_modifier = {
						name = un_withdrawal_penalty_modifier
						days = long_modifier_time
						is_decaying = yes
					}
					# International standing: having the Assembly vote your seat
					# away is a serious violation, so the benefits are suspended
					# as well as the score dented. Paid once — this option runs
					# once per resolution, and the expulsion topic carries a
					# ten-year cooldown afterwards.
					custom_tooltip = {
						text = un_standing_seat_stripped_tt
						un_standing_loss = { AMOUNT = un_standing_seat_stripped_loss REASON = 29 }
						un_standing_suspend = { DAYS = 1825 }
					}
				}
				change_relations = { country = scope:un_vote_target value = -25 }
			}
			# Promote the highest-ranking eligible non-permanent member into the
			# vacated seat (scope:un_vote_promoted was selected in the immediate
			# block). No-op when no eligible challenger exists; the seat then
			# remains vacant.
			if = {
				limit = { exists = scope:un_vote_promoted }
				scope:un_vote_promoted = {
					je:je_united_nations ?= { add_modifier = { name = un_security_council_modifier } }
					je:je_united_nations ?= { add_modifier = { name = un_permanent_member_modifier } }
				}
			}
		}
		else_if = {
			limit = { scope:un_resolution ?= { has_tag = un_topic_expulsion } }
			custom_tooltip = un_vote_expulsion_failed_tt
			change_global_variable = { name = un_authority add = -3 }
			if = {
				limit = { exists = scope:un_vote_target }
				change_relations = { country = scope:un_vote_target value = -15 }
			}
		}

		# ---- RESOLUTION PASSED ----
		if = {
			limit = {
				scope:un_resolution ?= {
					has_tag = un_res_passed
					NOT = { has_tag = un_topic_expulsion }
				}
			}

			# ---- Condemn: target gets condemnation, proposer gets prestige ----
			# If the target voted to accept the resolution, they already bear
			# un_self_corrected_modifier (set in un_vote.1 option A target branch);
			# skip un_condemned_modifier so the player isn't double-penalized.
			# If a permanent member vetoed, downgrade to a non-binding rebuke
			# (graduated outcome: GA majority still spoke but the resolution lacks
			# SC weight).
			if = {
				limit = {
					scope:un_resolution ?= { has_tag = un_topic_condemn }
					NOT = { scope:un_resolution ?= { has_tag = un_res_vetoed } }
				}
				custom_tooltip = un_vote_condemn_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				if = {
					limit = { exists = scope:un_vote_target }
					change_relations = { country = scope:un_vote_target value = -30 }
					scope:un_vote_target = {
						if = {
							limit = { NOT = { scope:un_resolution ?= { has_tag = un_res_target_accepted } } }
							add_modifier = {
								name = un_condemned_modifier
								days = long_modifier_time
								is_decaying = yes
							}
							custom_tooltip = {
								text = un_standing_censured_tt
								un_standing_loss = { AMOUNT = un_standing_condemned_loss REASON = 22 }
							}
						}
						else = {
							# Accepted the verdict before the vote: a much lighter
							# dent. Owning a censure is still compliance, and the
							# target already carries un_self_corrected_modifier.
							custom_tooltip = {
								text = un_standing_censured_accepted_tt
								un_standing_loss = { AMOUNT = un_standing_rebuked_loss REASON = 22 }
							}
						}
					}
				}
				add_modifier = {
					name = un_condemnation_leader_modifier
					days = normal_modifier_time
					is_decaying = yes
				}
			}
			else_if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_condemn } }
				# Graduated condemn: SC veto downgrades to non-binding rebuke
				custom_tooltip = un_vote_condemn_passed_vetoed_tt
				change_global_variable = { name = un_authority add = 2 }
				if = {
					limit = { exists = scope:un_vote_target }
					change_relations = { country = scope:un_vote_target value = -10 }
					scope:un_vote_target = {
						if = {
							limit = { NOT = { scope:un_resolution ?= { has_tag = un_res_target_accepted } } }
							add_modifier = {
								name = un_non_binding_rebuke_modifier
								days = normal_modifier_time
								is_decaying = yes
							}
							# The graduated form of a censure costs the graduated
							# dent. An accepted rebuke costs nothing at all —
							# it is the mildest outcome the Assembly can reach.
							custom_tooltip = {
								text = un_standing_rebuked_tt
								un_standing_loss = { AMOUNT = un_standing_rebuked_loss REASON = 23 }
							}
						}
					}
				}
			}

			# ---- Human Rights: UNHRC established ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_human_rights } }
				custom_tooltip = un_vote_human_rights_passed_tt
				change_global_variable = { name = un_authority add = 10 }
				if = {
					limit = { NOT = { has_global_variable = un_agency_unhrc } }
					set_global_variable = un_agency_unhrc
				}
			}

			# ---- NPT: IAEA established ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_npt } }
				custom_tooltip = un_vote_npt_passed_tt
				change_global_variable = { name = un_authority add = 10 }
				if = {
					limit = { NOT = { has_global_variable = un_agency_iaea } }
					set_global_variable = un_agency_iaea
				}
			}

			# ---- Climate: UNEP established ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_climate } }
				custom_tooltip = un_vote_climate_passed_tt
				change_global_variable = { name = un_authority add = 10 }
				if = {
					limit = { NOT = { has_global_variable = un_agency_unep } }
					set_global_variable = un_agency_unep
				}
			}

			# ---- Reform: Modernize UN institutions ----
			# Vetoed reform is the one binding topic with NO graduated fallback —
			# charter changes really do need P5 unanimity (no Uniting-for-Peace
			# style override is available here). The vote.3 notification still
			# fires so members know it was attempted, but no effect applies.
			if = {
				limit = {
					scope:un_resolution ?= { has_tag = un_topic_reform }
					NOT = { scope:un_resolution ?= { has_tag = un_res_vetoed } }
				}
				custom_tooltip = un_vote_reform_passed_tt
				change_global_variable = { name = un_authority add = 10 }
			}
			else_if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_reform } }
				custom_tooltip = un_vote_reform_passed_vetoed_tt
				# No effect — flat block
			}

			# ---- Heritage: UNESCO established ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_heritage } }
				custom_tooltip = un_vote_heritage_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				if = {
					limit = { NOT = { has_global_variable = un_agency_unesco } }
					set_global_variable = un_agency_unesco
				}
			}

			# ---- Decolonization: Anti-colonial declaration ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_decolonization } }
				custom_tooltip = un_vote_decolonization_passed_tt
				change_global_variable = { name = un_authority add = 10 }
				# Cash the loc promise of "Colonial powers face increased
				# international pressure" — apply a one-shot decaying
				# pressure modifier on every overseas-colonial owner.
				hidden_effect = {
					every_country = {
						limit = {
							any_scope_state = { is_overseas_colonial_state = yes }
							NOT = { has_modifier = anti_colonial_momentum_modifier }
						}
						add_modifier = {
							name = anti_colonial_momentum_modifier
							days = normal_modifier_time
							is_decaying = yes
						}
					}
				}
			}

			# ---- Pandemic: WHO established ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_pandemic } }
				custom_tooltip = un_vote_pandemic_passed_tt
				change_global_variable = { name = un_authority add = 10 }
				if = {
					limit = { NOT = { has_global_variable = un_agency_who } }
					set_global_variable = un_agency_who
				}
			}

			# ---- Refugee: UNHCR established ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_refugee } }
				custom_tooltip = un_vote_refugee_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				if = {
					limit = { NOT = { has_global_variable = un_agency_unhcr } }
					set_global_variable = un_agency_unhcr
				}
			}

			# ---- Space: UNOOSA established ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_space } }
				custom_tooltip = un_vote_space_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				if = {
					limit = { NOT = { has_global_variable = un_agency_unoosa } }
					set_global_variable = un_agency_unoosa
				}
			}

			# ---- Peacekeeping Request: Deploy peacekeepers to requesting country ----
			# Vetoed: deploy a non-coercive observer mission instead — the
			# proposer gets a small benefit but no contributors are levied.
			if = {
				limit = {
					scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					NOT = { scope:un_resolution ?= { has_tag = un_res_vetoed } }
				}
				custom_tooltip = un_vote_peacekeeping_request_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				# Apply scaled peacekeeping benefit to the requesting country
				if = {
					limit = { exists = scope:un_vote_proposer }
					scope:un_vote_proposer = {
						add_modifier = {
							name = un_peacekeeping_received_modifier
							multiplier = un_peacekeeping_benefit_scale
							days = long_modifier_time
							is_decaying = yes
						}
					}
				}
				# Apply costs to all countries that pledged contributions
				hidden_effect = {
					every_country = {
						limit = {
							je:je_united_nations ?= { has_modifier = un_peacekeeping_contributor_modifier }
						}
						# Remove old cost to re-apply with updated count-based scaling
						je:je_united_nations ?= { if = { limit = { has_modifier = un_peacekeeping_contributor_cost } remove_modifier = un_peacekeeping_contributor_cost } }
						je:je_united_nations ?= { add_modifier = {
								name = un_peacekeeping_contributor_cost
								multiplier = un_peacekeeping_total_expense_value
							} }
						add_modifier = {
							name = un_peacekeeping_cooldown
							days = long_modifier_time
							is_decaying = yes
						}
					}
				}
			}
			else_if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request } }
				custom_tooltip = un_vote_peacekeeping_request_passed_vetoed_tt
				change_global_variable = { name = un_authority add = 2 }
				# Observer mission to the proposer only — no enforcement, no levies
				if = {
					limit = { exists = scope:un_vote_proposer }
					scope:un_vote_proposer = {
						add_modifier = {
							name = un_observer_mission_modifier
							days = normal_modifier_time
							is_decaying = yes
						}
					}
				}
				# Drop pledged contributors (the binding deployment was vetoed)
				hidden_effect = {
					every_country = {
						limit = {
							je:je_united_nations ?= { has_modifier = un_peacekeeping_contributor_modifier }
							NOT = { je:je_united_nations ?= { has_modifier = un_peacekeeping_contributor_cost } }
						}
						je:je_united_nations ?= { remove_modifier = un_peacekeeping_contributor_modifier }
					}
				}
			}

			# ---- Aid Request: Deliver humanitarian aid to requesting country ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_aid_request } }
				custom_tooltip = un_vote_aid_request_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				# Apply scaled aid benefit to the requesting country
				if = {
					limit = { exists = scope:un_vote_proposer }
					scope:un_vote_proposer = {
						add_modifier = {
							name = un_aid_received_modifier
							multiplier = un_aid_benefit_scale
							days = long_modifier_time
							is_decaying = yes
						}
					}
				}
				# Apply costs to all countries that pledged aid
				hidden_effect = {
					every_country = {
						limit = { has_modifier = un_humanitarian_aid_modifier }
						# Remove old cost to re-apply with updated count-based scaling
						if = { limit = { has_modifier = un_humanitarian_aid_cost } remove_modifier = un_humanitarian_aid_cost }
						add_modifier = {
							name = un_humanitarian_aid_cost
							multiplier = un_aid_total_expense_value
							days = long_modifier_time
							is_decaying = yes
						}
					}
				}
			}

			# ---- Sanctions: Apply sanctions to the target country ----
			# If the target voted to accept the resolution, they already bear
			# un_self_corrected_modifier; skip un_sanctions_target_modifier so
			# the player isn't double-penalized.
			# Vetoed: voluntary partial sanctions — apply un_sanctions_partial_modifier
			# to the target instead of the full sanctions package.
			if = {
				limit = {
					scope:un_resolution ?= { has_tag = un_topic_sanctions }
					NOT = { scope:un_resolution ?= { has_tag = un_res_vetoed } }
				}
				custom_tooltip = un_vote_sanctions_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				# Proposer becomes sanctions enforcer
				add_modifier = { name = un_sanctions_enforcer_modifier }
				# Apply sanctions to target
				if = {
					limit = { exists = scope:un_vote_target }
					scope:un_vote_target = {
						if = {
							limit = { NOT = { scope:un_resolution ?= { has_tag = un_res_target_accepted } } }
							add_modifier = { name = un_sanctions_target_modifier }
							custom_tooltip = {
								text = un_standing_sanctioned_tt
								un_standing_loss = { AMOUNT = un_standing_sanctioned_loss REASON = 24 }
							}
						}
						else = {
							custom_tooltip = {
								text = un_standing_sanctioned_accepted_tt
								un_standing_loss = { AMOUNT = un_standing_sanctioned_partial_loss REASON = 24 }
							}
						}
					}
					change_relations = { country = scope:un_vote_target value = -30 }
				}
			}
			else_if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_sanctions } }
				custom_tooltip = un_vote_sanctions_passed_vetoed_tt
				change_global_variable = { name = un_authority add = 2 }
				if = {
					limit = { exists = scope:un_vote_target }
					scope:un_vote_target = {
						if = {
							limit = { NOT = { scope:un_resolution ?= { has_tag = un_res_target_accepted } } }
							add_modifier = { name = un_sanctions_partial_modifier }
							custom_tooltip = {
								text = un_standing_sanctioned_partial_tt
								un_standing_loss = { AMOUNT = un_standing_sanctioned_partial_loss REASON = 25 }
							}
						}
					}
					change_relations = { country = scope:un_vote_target value = -10 }
				}
			}

			# ---- Law of the Sea: Maritime cooperation framework ----
			if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_law_of_sea } }
				custom_tooltip = un_vote_law_of_sea_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				set_global_variable = un_agency_itlos
			}

			# ---- Military Mandate: authorize one narrow objective ----
			# Like reform, this binding topic has NO graduated form. There is
			# no weaker version of a licence to use force, so a veto blocks it
			# outright and no mandate is issued. un_mandate_create re-validates
			# before issuing: a year has passed since the vote opened, and the
			# target may have lost the territory, we may have left the UN, or
			# we may already hold another authorization — in any of those cases
			# the resolution carries but grants nothing.
			if = {
				limit = {
					scope:un_resolution ?= { has_tag = un_topic_military_mandate }
					NOT = { scope:un_resolution ?= { has_tag = un_res_vetoed } }
				}
				custom_tooltip = un_vote_military_mandate_passed_tt
				change_global_variable = { name = un_authority add = 3 }
				# Hidden: create_container / set_variable have no player-facing
				# tooltip worth showing, and letting them render would push the
				# container's internals in under the custom_tooltip above. Same
				# reasoning as the un_vote_apply_treaty_modifier split — this
				# file controls what the option tooltip says.
				hidden_effect = {
					un_mandate_create = yes
				}
			}
			else_if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_military_mandate } }
				custom_tooltip = un_vote_military_mandate_passed_vetoed_tt
				# No effect — flat block, as with reform.
			}

			# ---- International Criminal Court: Global criminal justice ----
			# Vetoed: no agency established, just a symbolic censure (proposer
			# gets un_court_contested_modifier as a small accountability gesture).
			if = {
				limit = {
					scope:un_resolution ?= { has_tag = un_topic_icc }
					NOT = { scope:un_resolution ?= { has_tag = un_res_vetoed } }
				}
				custom_tooltip = un_vote_icc_passed_tt
				change_global_variable = { name = un_authority add = 5 }
				set_global_variable = un_agency_icc
			}
			else_if = {
				limit = { scope:un_resolution ?= { has_tag = un_topic_icc } }
				custom_tooltip = un_vote_icc_passed_vetoed_tt
				change_global_variable = { name = un_authority add = 2 }
				add_modifier = {
					name = un_court_contested_modifier
					days = normal_modifier_time
					is_decaying = yes
				}
			}

			# Influence reward for leading a successful resolution.
			# Skipped for vetoed binding resolutions — only the graduated form
			# went through, so the proposer didn't fully succeed.
			if = {
				limit = { NOT = { scope:un_resolution ?= { has_tag = un_res_vetoed } } }
				add_modifier = {
					name = un_vote_success_reward
					days = normal_modifier_time
					is_decaying = yes
				}
			}

			# Infamy reduction: successfully leading a humanitarian resolution rehabilitates image
			if = {
				limit = {
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_human_rights }
						scope:un_resolution ?= { has_tag = un_topic_refugee }
						scope:un_resolution ?= { has_tag = un_topic_pandemic }
						scope:un_resolution ?= { has_tag = un_topic_aid_request }
						scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request }
					}
					infamy > 0
				}
				change_infamy = -3
			}

			# Power bloc leverage: successful proposer gains leverage over bloc members who voted
			if = {
				limit = { is_power_bloc_leader = yes }
				power_bloc ?= {
					every_power_bloc_member = {
						limit = {
							je:je_united_nations ?= { has_modifier = un_member_modifier }
							NOT = { THIS = ROOT }
						}
						root = {
							power_bloc ?= {
								add_leverage = { target = prev value = 3 }
							}
						}
					}
				}
			}
		}

		# ---- RESOLUTION FAILED ----
		if = {
			limit = {
				scope:un_resolution ?= {
					has_tag = un_res_failed
					NOT = { has_tag = un_topic_expulsion }
				}
			}
			custom_tooltip = un_vote_failed_tt
			change_global_variable = { name = un_authority add = -3 }
			add_modifier = {
				name = un_vote_failure_penalty
				days = normal_modifier_time
				is_decaying = yes
			}
			# Embarrassment: failed humanitarian resolution costs credibility
			if = {
				limit = {
					OR = {
						scope:un_resolution ?= { has_tag = un_topic_human_rights }
						scope:un_resolution ?= { has_tag = un_topic_refugee }
						scope:un_resolution ?= { has_tag = un_topic_pandemic }
					}
				}
				change_infamy = 2
			}
			# Power bloc leverage loss: failed proposer loses credibility with bloc
			if = {
				limit = { is_power_bloc_leader = yes }
				power_bloc ?= {
					every_power_bloc_member = {
						limit = {
							je:je_united_nations ?= { has_modifier = un_member_modifier }
							NOT = { THIS = ROOT }
						}
						root = {
							power_bloc ?= {
								add_leverage = { target = prev value = -3 }
							}
						}
					}
				}
			}
			# Revoke unfulfilled pledges from voters
			hidden_effect = {
				if = {
					limit = { scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request } }
					every_country = {
						limit = {
							je:je_united_nations ?= { has_modifier = un_peacekeeping_contributor_modifier }
							NOT = { je:je_united_nations ?= { has_modifier = un_peacekeeping_contributor_cost } }
						}
						je:je_united_nations ?= { remove_modifier = un_peacekeeping_contributor_modifier }
					}
				}
				if = {
					limit = { scope:un_resolution ?= { has_tag = un_topic_aid_request } }
					every_country = {
						limit = {
							has_modifier = un_humanitarian_aid_modifier
							NOT = { has_modifier = un_humanitarian_aid_cost }
						}
						remove_modifier = un_humanitarian_aid_modifier
					}
				}
			}
		}

		# ---- Notify all other UN members of the result ----
		hidden_effect = {
			every_country = {
				limit = {
					je:je_united_nations ?= { has_modifier = un_member_modifier }
					NOT = { THIS = ROOT }
				}
				trigger_event = { id = un_vote.3 }
			}
		}

		ai_chance = { base = 100 }
	}
}

# ---- Phase 3: Notification to all other UN members ----
un_vote.3 = {
	type = country_event
	placement = ROOT

	event_image = { texture = "gfx/event_pictures/un_assembly_hall_vote.dds" }

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_map.dds"

	title = un_vote.3.t

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { scope:un_resolution ?= { has_tag = un_res_passed } }
				desc = un_vote.3.d_passed
			}
			desc = un_vote.3.d_failed
		}
	}

	duration = 3

	# scope:un_resolution is the closed resolution, passed on from un_vote.2.
	trigger = {
		je:je_united_nations ?= { has_modifier = un_member_modifier }
		exists = scope:un_resolution
	}

	immediate = {
		# Vote counts as country vars for loc display (loc has no accessor
		# for a script container held in a saved scope)
		set_variable = {
			name = un_display_support
			value = scope:un_resolution.var:un_res_support
		}
		set_variable = {
			name = un_display_oppose
			value = scope:un_resolution.var:un_res_oppose
		}
		scope:un_resolution = {
			var:un_res_proposer ?= { save_scope_as = un_vote_proposer }
			var:un_res_target ?= { save_scope_as = un_vote_target }
		}
	}

	# Option A: Catch-all "acknowledge" — vote failed, you voted yes, or you
	# joined the UN after vote.1 fired and never had a chance to vote. In the
	# last case the country still gets bound by the passed resolution (default
	# acceptance) since the apply blocks below already gate on the passed tag.
	option = {
		name = un_vote.3.a
		default_option = yes
		trigger = {
			OR = {
				NOT = { scope:un_resolution ?= { has_tag = un_res_passed } }
				NOT = { scope:un_resolution ?= { un_resolution_has_vote = { LIST = un_res_no } } }
			}
		}

		# Apply the persistent treaty/declaration modifier directly in the
		# option block so the engine renders each add_modifier in the option
		# tooltip — players can see exactly what they're ratifying before
		# clicking acknowledge. (Previously these were buried inside
		# un_vote_apply_resolution_compliance, where the tooltip nesting
		# made the modifier name and fields easy to miss.)
		if = {
			limit = { scope:un_resolution ?= { has_tag = un_res_passed } }
			un_vote_apply_treaty_modifier = yes
			un_vote_apply_resolution_compliance = yes
		}
		ai_chance = { base = 100 }
	}

	# Option B: Vote passed AND you voted no - accept the resolution anyway
	option = {
		name = un_vote.3.b
		trigger = {
			scope:un_resolution ?= {
				has_tag = un_res_passed
				un_resolution_has_vote = { LIST = un_res_no }
			}
		}

		# Same effects as yes voters - you accept the majority decision
		un_vote_apply_treaty_modifier = yes
		un_vote_apply_resolution_compliance = yes

		# International standing: complying with a BINDING decision you voted
		# against is the one act in this event that is not merely voting — the
		# member is bound by something it argued against and submits anyway.
		# Voting with the majority pays nothing (option A above is untouched).
		# Restricted to binding resolutions because only those bind: yielding
		# on a recommendation costs nothing and so earns nothing.
		# Once per member per resolution — un_vote.3 fires once per member, and
		# an event runs exactly one option.
		#
		# peacekeeping_request is excluded because it is the ONE topic that is
		# both binding and a pledge topic: un_vote_apply_resolution_compliance
		# just paid this country the contribution award for taking on a share
		# of the deployment, and paying the compliance award on top would make
		# a no-voter who complies worth twice a yes-voter who does the same
		# thing. The pledge is the compliance, and it has already been paid.
		if = {
			limit = {
				scope:un_resolution ?= { un_resolution_is_binding = yes }
				NOT = { scope:un_resolution ?= { has_tag = un_topic_peacekeeping_request } }
			}
			custom_tooltip = {
				text = un_standing_complied_tt
				un_standing_gain = { AMOUNT = un_standing_compliance_gain REASON = 5 }
			}
		}
		ai_chance = { base = 50 }
	}

	# Option C: Vote passed AND you voted no - refuse to comply
	option = {
		name = un_vote.3.c
		trigger = {
			scope:un_resolution ?= {
				has_tag = un_res_passed
				un_resolution_has_vote = { LIST = un_res_no }
			}
		}

		# Diplomatic penalties for defying the international community
		if = {
			limit = { exists = scope:un_vote_proposer }
			change_relations = { country = scope:un_vote_proposer value = -20 }
			create_diplomatic_catalyst = { type = catalyst_event_negative target = scope:un_vote_proposer }
		}
		change_infamy = 3

		# International standing: openly refusing a BINDING decision is the
		# defiance the brief asks to punish. Refusing a recommendatory one is
		# legal dissent — the same reasoning that leaves the veto free — so it
		# costs nothing here.
		if = {
			limit = { scope:un_resolution ?= { un_resolution_is_binding = yes } }
			custom_tooltip = {
				text = un_standing_defied_tt
				un_standing_loss = { AMOUNT = un_standing_defiance_loss REASON = 26 }
			}
		}

		# Power bloc leverage: defying bloc decision
		if = {
			limit = {
				exists = scope:un_vote_proposer
				scope:un_vote_proposer = { is_power_bloc_leader = yes }
				is_in_same_power_bloc = scope:un_vote_proposer
			}
			scope:un_vote_proposer = {
				power_bloc ?= {
					add_leverage = { target = root value = -15 }
				}
			}
		}

		ai_chance = {
			base = 20
			# Countries that are rivals of proposer more likely to refuse
			modifier = {
				trigger = {
					exists = scope:un_vote_proposer
					has_diplomatic_pact = {
						who = scope:un_vote_proposer
						type = rivalry
					}
				}
				add = 40
			}
			# Great powers refuse more — sovereignty concerns
			modifier = {
				trigger = { country_rank >= rank_value:great_power }
				add = 20
			}
			# NPT: nuclear powers refuse rather than disarm
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_npt }
					has_variable = nuclear_weapon_stockpile
					var:nuclear_weapon_stockpile >= 1
				}
				add = 50
			}
			# Climate: heavy industry nations refuse binding targets
			modifier = {
				trigger = {
					scope:un_resolution ?= { has_tag = un_topic_climate }
					ig:ig_industrialists ?= {
						is_powerful = yes
					}
				}
				add = 25
			}
		}
	}
}
