namespace = minor_hidden_events

minor_hidden_events.1 = {
    type = country_event
    hidden = yes

    immediate = {
        every_scope_state = {
			limit = {
				NOT = {
					state_region = {
						any_scope_state = {
							NOT = { owner = ROOT.owner }
						}
					}
				}
			}
            if = {
                limit = {
                    NOT = { has_variable = arable_land_added }
                }
                set_variable = {
                    name = arable_land_added
                    value = 0
                }
            }
            add_arable_land_effect = {
                AMOUNT = state_arable_land_add_script_value
            }
		}
    }
}