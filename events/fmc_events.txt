namespace = fmc_update_events

# Game-start country setup: initialise FMC variables and place the first
# fmc_building_construction_site in the capital.
# Fired by the GLOBAL block in fmc_global.txt for every country.
fmc_update_events.1 = {
    type = country_event
    hidden = yes

    immediate = {
        fmc_country_setup = yes
    }
}
