# Language reform events — fire yearly while law_state_led_language_reform is active.
# Event 25 (founding) also fires immediately from on_law_activated in extra_on_actions.txt;
# it is included here as a fallback for existing saves that already have the law active.
on_yearly_events = {
	random_events = {
		chance_to_happen = 80
		10 = extra_law_events.25
		8 = extra_law_events.26
		8 = extra_law_events.27
	}
}
