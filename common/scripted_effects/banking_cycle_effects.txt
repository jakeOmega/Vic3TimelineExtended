# Banking cycle random event selection
# Called from je_banking_cycle monthly pulse.
# Handles cooldown countdown and weighted random event selection.

banking_cycle_random_event_effect = {
	# Cooldown countdown
	if = {
		limit = { has_variable = banking_event_cooldown }
		change_variable = { name = banking_event_cooldown add = -1 }
		if = {
			limit = { var:banking_event_cooldown <= 0 }
			remove_variable = banking_event_cooldown
		}
	}
	if = {
		limit = {
			NOT = { has_variable = banking_event_cooldown }
			var:crash <= 0  # don't fire events in the same month as a crash
		}
		random_list = {
			120 = { } # no event this month

			# ---- EARLY ERA (no extra tech gate beyond stock_exchange) ----

			# Event 1: Speculative Mania (expansion+, bubble building)
			0 = {
				modifier = {
					if = { limit = { var:bubble_pressure >= 15 banking_cycle_is_expansion = yes } add = 5 }
					if = { limit = { var:bubble_pressure >= 15 banking_cycle_is_boom = yes } add = 5 }
					if = { limit = { var:bubble_pressure >= 15 banking_cycle_is_frenzy = yes } add = 5 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 3 }
				}
				if = {
					limit = { has_law = law_type:law_command_economy }
					trigger_event = { id = banking_cycle_events.101 }
				}
				else_if = {
					limit = { has_law = law_type:law_cooperative_ownership }
					trigger_event = { id = banking_cycle_events.151 }
				}
				else = {
					trigger_event = { id = banking_cycle_events.1 }
				}
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 2: Foreign Capital Inflow (stable+, not at war)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							is_at_war = no
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 5
					}
				}
				trigger_event = { id = banking_cycle_events.2 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 3: Credit Crunch Fears (stagnation/downturn)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = { limit = { banking_cycle_is_stagnation = yes } add = 6 }
					if = { limit = { banking_cycle_is_downturn = yes } add = 9 }
				}
				trigger_event = { id = banking_cycle_events.3 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 4: Country Bank Run (RARE, HIGH IMPACT - downturn/panic)
			0 = {
				modifier = {
					if = { limit = { banking_cycle_is_downturn = yes } add = 1 }
					if = { limit = { banking_cycle_is_panic = yes } add = 2 }
					if = { limit = { var:bubble_pressure >= 50 } add = 1 }
				}
				if = {
					limit = { has_law = law_type:law_command_economy }
					trigger_event = { id = banking_cycle_events.104 }
				}
				else_if = {
					limit = { has_law = law_type:law_cooperative_ownership }
					trigger_event = { id = banking_cycle_events.154 }
				}
				else = {
					trigger_event = { id = banking_cycle_events.4 }
				}
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 5: Railway Bond Fever (expansion+, railroads tech)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = railways
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 5
					}
				}
				trigger_event = { id = banking_cycle_events.5 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 6: Harvest Failure & Credit (any phase)
			0 = {
				modifier = {
					add = 4
				}
				if = {
					limit = { has_law = law_type:law_command_economy }
					trigger_event = { id = banking_cycle_events.106 }
				}
				else_if = {
					limit = { has_law = law_type:law_cooperative_ownership }
					trigger_event = { id = banking_cycle_events.156 }
				}
				else = {
					trigger_event = { id = banking_cycle_events.6 }
				}
				set_variable = { name = banking_event_cooldown value = 12 }
			}

			# ---- MID ERA (central_banking / investment_banks / mutual_funds) ----

			# Event 7: Bank Consolidation Wave (boom/frenzy, central_banking)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = central_banking
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.7 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 8: Novel Financial Instrument (expansion+, investment_banks)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = investment_banks
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 5
					}
					if = { limit = { banking_cycle_is_frenzy = yes } add = 2 }
				}
				trigger_event = { id = banking_cycle_events.8 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 9: Asset Price Uncertainty (expansion/boom, bubble>=30, central_banking)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = central_banking
							var:bubble_pressure >= 30
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 5
					}
				}
				trigger_event = { id = banking_cycle_events.9 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 10: Sovereign Debt Scare (stagnation/downturn/panic, central_banking)
			0 = {
				modifier = {
					if = {
						limit = {
							has_technology_researched = central_banking
							OR = {
								banking_cycle_is_stagnation = yes
								banking_cycle_is_downturn = yes
								banking_cycle_is_panic = yes
							}
						}
						add = 4
					}
					if = { limit = { banking_cycle_is_panic = yes } add = 3 }
				}
				if = {
					limit = { has_law = law_type:law_command_economy }
					trigger_event = { id = banking_cycle_events.110 }
				}
				else_if = {
					limit = { has_law = law_type:law_cooperative_ownership }
					trigger_event = { id = banking_cycle_events.160 }
				}
				else = {
					trigger_event = { id = banking_cycle_events.10 }
				}
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 11: Insurance Sector Wobble (expansion+, mutual_funds)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = mutual_funds
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.11 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 12: Gold Standard Pressure (any phase, international_exchange_standards, gold standard law)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = international_exchange_standards
							has_law = law_type:law_gold_standard
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.12 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 13: Industrial Overinvestment (boom/frenzy, corporate_management)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = corporate_management
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
						}
						add = 4
					}
					if = { limit = { banking_cycle_is_frenzy = yes } add = 2 }
				}
				trigger_event = { id = banking_cycle_events.13 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}

			# ---- LATE ERA (keynesian_economics / modern_financial_instruments / consumer_credit) ----

			# Event 14: Housing Price Spiral (HIGH IMPACT, boom/frenzy, consumer_credit)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = consumer_credit
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
						}
						add = 4
					}
					if = { limit = { banking_cycle_is_frenzy = yes } add = 2 }
				}
				trigger_event = { id = banking_cycle_events.14 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 15: Currency Crisis (downturn/panic, modern_financial_instruments)
			0 = {
				modifier = {
					if = {
						limit = {
							has_technology_researched = modern_financial_instruments
							OR = {
								banking_cycle_is_downturn = yes
								banking_cycle_is_panic = yes
							}
						}
						add = 5
					}
					if = { limit = { banking_cycle_is_panic = yes } add = 3 }
				}
				if = {
					limit = { has_law = law_type:law_command_economy }
					trigger_event = { id = banking_cycle_events.115 }
				}
				else_if = {
					limit = { has_law = law_type:law_cooperative_ownership }
					trigger_event = { id = banking_cycle_events.165 }
				}
				else = {
					trigger_event = { id = banking_cycle_events.15 }
				}
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 16: CB Independence Debate (stable+, keynesian_economics, not unregulated banking)
			0 = {
				modifier = {
					if = {
						limit = {
							has_technology_researched = keynesian_economics
							NOT = { has_law = law_type:law_unregulated_banking }
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 4
					}
					if = {
						limit = { has_law = law_type:law_central_bank_independence }
						add = 2
					}
				}
				if = {
					limit = { has_law = law_type:law_command_economy }
					trigger_event = { id = banking_cycle_events.116 }
				}
				else_if = {
					limit = { has_law = law_type:law_cooperative_ownership }
					trigger_event = { id = banking_cycle_events.166 }
				}
				else = {
					trigger_event = { id = banking_cycle_events.16 }
				}
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 17: Consumer Credit Boom (expansion+, consumer_credit)
			0 = {
				modifier = {
					if = {
						limit = {
							has_technology_researched = consumer_credit
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
				}
				if = {
					limit = { has_law = law_type:law_command_economy }
					trigger_event = { id = banking_cycle_events.117 }
				}
				else_if = {
					limit = { has_law = law_type:law_cooperative_ownership }
					trigger_event = { id = banking_cycle_events.167 }
				}
				else = {
					trigger_event = { id = banking_cycle_events.17 }
				}
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 18: Derivatives Scandal (RARE, boom/frenzy, modern_financial_instruments)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = modern_financial_instruments
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
							var:bubble_pressure >= 40
						}
						add = 2
					}
				}
				trigger_event = { id = banking_cycle_events.18 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 19: Sovereign Wealth Fund Proposal (expansion+, keynesian_economics, NOT laissez_faire)
			0 = {
				modifier = {
					if = {
						limit = {
							has_technology_researched = keynesian_economics
							NOT = { has_law = law_type:law_laissez_faire }
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 3
					}
				}
				trigger_event = { id = banking_cycle_events.19 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 20: Shadow Banking Discovery (RARE, HIGH IMPACT, boom/frenzy, modern_financial_instruments, favors weak regulation)
			0 = {
				modifier = {
					if = {
						limit = {
							has_technology_researched = modern_financial_instruments
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
							var:bubble_pressure >= 50
						}
						add = 2
					}
					if = {
						limit = {
							OR = {
								has_law = law_type:law_unregulated_banking
								has_law = law_type:law_universal_banking_light_prudence
							}
						}
						add = 1
					}
				}
				if = {
					limit = { has_law = law_type:law_command_economy }
					trigger_event = { id = banking_cycle_events.120 }
				}
				else_if = {
					limit = { has_law = law_type:law_cooperative_ownership }
					trigger_event = { id = banking_cycle_events.170 }
				}
				else = {
					trigger_event = { id = banking_cycle_events.20 }
				}
				set_variable = { name = banking_event_cooldown value = 12 }
			}

			# ---- NEW EVENTS 21-45 ----

			# Event 21: Merchant Bank Prosperity (stable/expansion, no extra tech)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
							}
						}
						add = 5
					}
				}
				trigger_event = { id = banking_cycle_events.21 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 22: New Provincial Exchange (expansion+, no extra tech)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.22 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 23: Banking Scandal (any phase, no extra tech)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					add = 4
				}
				trigger_event = { id = banking_cycle_events.23 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 24: Warehouse Receipts Fraud (expansion+, bubble >= 20)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							var:bubble_pressure >= 20
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.24 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 25: Trade Bill Innovation (stable+, not at war)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							is_at_war = no
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.25 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}

			# ---- MID ERA NEW EVENTS ----

			# Event 26: Pension Fund Growth (expansion+, mutual_funds)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = mutual_funds
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.26 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 27: Telegraph Banking Revolution (expansion+, central_banking)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = central_banking
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.27 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 28: Interbank Lending Freeze (stagnation/downturn, central_banking)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = central_banking
							OR = {
								banking_cycle_is_stagnation = yes
								banking_cycle_is_downturn = yes
							}
						}
						add = 5
					}
					if = { limit = { banking_cycle_is_downturn = yes } add = 3 }
				}
				trigger_event = { id = banking_cycle_events.28 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 29: Agricultural Cooperative Banking (stable+, central_banking, favors mutual banking law)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = central_banking
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 4
					}
					if = {
						limit = { has_law = law_type:law_free_mutual_banking }
						add = 3
					}
				}
				trigger_event = { id = banking_cycle_events.29 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 30: Foreign Debt Arbitrage (expansion+, investment_banks)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = investment_banks
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
					if = { limit = { banking_cycle_is_frenzy = yes } add = 2 }
				}
				trigger_event = { id = banking_cycle_events.30 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 31: Bank Holiday Proposal (downturn/panic, central_banking)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = central_banking
							OR = {
								banking_cycle_is_downturn = yes
								banking_cycle_is_panic = yes
							}
						}
						add = 4
					}
					if = { limit = { banking_cycle_is_panic = yes } add = 3 }
				}
				trigger_event = { id = banking_cycle_events.31 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 32: Trust Company Failure (RARE, boom/frenzy, mutual_funds, bubble >= 30)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = mutual_funds
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
							var:bubble_pressure >= 30
						}
						add = 2
					}
				}
				trigger_event = { id = banking_cycle_events.32 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 33: Commercial Paper Market (stable/expansion, investment_banks)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = investment_banks
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.33 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 34: Deposit Insurance Debate (stagnation+, central_banking)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = central_banking
							OR = {
								banking_cycle_is_stagnation = yes
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
							}
						}
						add = 3
					}
				}
				trigger_event = { id = banking_cycle_events.34 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 35: Joint-Stock Company Boom (expansion+, corporate_management)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = corporate_management
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.35 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}

			# ---- LATE ERA NEW EVENTS ----

			# Event 36: Fintech Disruption (expansion+, modern_financial_instruments)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = modern_financial_instruments
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.36 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 37: Mortgage-Backed Securities (boom/frenzy, consumer_credit, favors weak regulation)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = consumer_credit
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
						}
						add = 3
					}
					if = { limit = { banking_cycle_is_frenzy = yes } add = 2 }
					if = {
						limit = {
							OR = {
								has_law = law_type:law_unregulated_banking
								has_law = law_type:law_universal_banking_light_prudence
							}
						}
						add = 2
					}
				}
				trigger_event = { id = banking_cycle_events.37 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 38: Microfinance Initiative (stable+, keynesian_economics)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = keynesian_economics
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.38 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 39: Quantitative Easing Debate (downturn/panic, keynesian_economics, requires fiat/digital currency + regulated banking)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = keynesian_economics
							OR = {
								has_law = law_type:law_fiat_currency
								has_law = law_type:law_digital_currency
							}
							NOT = { has_law = law_type:law_unregulated_banking }
							OR = {
								banking_cycle_is_downturn = yes
								banking_cycle_is_panic = yes
							}
						}
						add = 5
					}
					if = { limit = { banking_cycle_is_panic = yes } add = 3 }
				}
				trigger_event = { id = banking_cycle_events.39 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 40: Credit Rating Agency Failure (boom/frenzy, modern_financial_instruments)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = modern_financial_instruments
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
						}
						add = 3
					}
				}
				trigger_event = { id = banking_cycle_events.40 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 41: Student Loan Burden (expansion+, consumer_credit)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = consumer_credit
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
								banking_cycle_is_frenzy = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.41 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 42: Green Bond Initiative (stable+, keynesian_economics)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = keynesian_economics
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 3
					}
				}
				trigger_event = { id = banking_cycle_events.42 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 43: Venture Capital Boom (expansion/boom, modern_financial_instruments)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = modern_financial_instruments
							OR = {
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 4
					}
				}
				trigger_event = { id = banking_cycle_events.43 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 44: Predatory Lending Scandal (boom/frenzy, consumer_credit, blocked by prudential banking, favors weak regulation)
			0 = {
				trigger = { banking_is_market_economy = yes }
				modifier = {
					if = {
						limit = {
							has_technology_researched = consumer_credit
							NOT = { has_law = law_type:law_prudential_narrow_banking }
							OR = { banking_cycle_is_boom = yes banking_cycle_is_frenzy = yes }
						}
						add = 3
					}
					if = {
						limit = {
							OR = {
								has_law = law_type:law_unregulated_banking
								has_law = law_type:law_universal_banking_light_prudence
							}
						}
						add = 2
					}
				}
				trigger_event = { id = banking_cycle_events.44 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 45: International Bailout Request (downturn/panic, keynesian_economics)
			0 = {
				modifier = {
					if = {
						limit = {
							has_technology_researched = keynesian_economics
							OR = {
								banking_cycle_is_downturn = yes
								banking_cycle_is_panic = yes
							}
						}
						add = 3
					}
				}
				trigger_event = { id = banking_cycle_events.45 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}

			# ---- COMMAND ECONOMY EXCLUSIVE EVENTS ----

			# Event 50: Five-Year Plan Shortfall (stagnation/downturn)
			0 = {
				trigger = { has_law = law_type:law_command_economy }
				modifier = {
					if = { limit = { banking_cycle_is_stagnation = yes } add = 6 }
					if = { limit = { banking_cycle_is_downturn = yes } add = 9 }
				}
				trigger_event = { id = banking_cycle_events.50 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 51: Factory Director Corruption (expansion+)
			0 = {
				trigger = { has_law = law_type:law_command_economy }
				modifier = {
					if = { limit = { banking_cycle_is_expansion = yes } add = 5 }
					if = { limit = { banking_cycle_is_boom = yes } add = 5 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 5 }
				}
				trigger_event = { id = banking_cycle_events.51 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 52: Resource Hoarding Crisis (boom/frenzy)
			0 = {
				trigger = { has_law = law_type:law_command_economy }
				modifier = {
					if = { limit = { banking_cycle_is_boom = yes } add = 5 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 8 }
				}
				trigger_event = { id = banking_cycle_events.52 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 53: Black Market Emergence (downturn/panic)
			0 = {
				trigger = { has_law = law_type:law_command_economy }
				modifier = {
					if = { limit = { banking_cycle_is_downturn = yes } add = 6 }
					if = { limit = { banking_cycle_is_panic = yes } add = 9 }
				}
				trigger_event = { id = banking_cycle_events.53 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 54: Worker Motivation Campaign (stable+)
			0 = {
				trigger = { has_law = law_type:law_command_economy }
				modifier = {
					if = {
						limit = {
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 5
					}
				}
				trigger_event = { id = banking_cycle_events.54 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 55: Socialist Fraternal Aid (downturn/panic, keynesian)
			0 = {
				trigger = {
					has_law = law_type:law_command_economy
					has_technology_researched = keynesian_economics
				}
				modifier = {
					if = { limit = { banking_cycle_is_downturn = yes } add = 4 }
					if = { limit = { banking_cycle_is_panic = yes } add = 6 }
				}
				trigger_event = { id = banking_cycle_events.55 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 56: Industrial Sabotage Investigation (expansion+, central_banking)
			0 = {
				trigger = {
					has_law = law_type:law_command_economy
					has_technology_researched = central_banking
				}
				modifier = {
					if = { limit = { banking_cycle_is_expansion = yes } add = 4 }
					if = { limit = { banking_cycle_is_boom = yes } add = 4 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 4 }
				}
				trigger_event = { id = banking_cycle_events.56 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 57: Collectivization Efficiency Drive (boom/frenzy, corporate_management)
			0 = {
				trigger = {
					has_law = law_type:law_command_economy
					has_technology_researched = corporate_management
				}
				modifier = {
					if = { limit = { banking_cycle_is_boom = yes } add = 4 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 6 }
				}
				trigger_event = { id = banking_cycle_events.57 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}

			# ---- COOPERATIVE OWNERSHIP EXCLUSIVE EVENTS ----

			# Event 60: Federation Governance Dispute (stagnation/downturn)
			0 = {
				trigger = { has_law = law_type:law_cooperative_ownership }
				modifier = {
					if = { limit = { banking_cycle_is_stagnation = yes } add = 6 }
					if = { limit = { banking_cycle_is_downturn = yes } add = 9 }
				}
				trigger_event = { id = banking_cycle_events.60 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 61: Free Rider Problem (expansion+)
			0 = {
				trigger = { has_law = law_type:law_cooperative_ownership }
				modifier = {
					if = { limit = { banking_cycle_is_expansion = yes } add = 5 }
					if = { limit = { banking_cycle_is_boom = yes } add = 5 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 5 }
				}
				trigger_event = { id = banking_cycle_events.61 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 62: Democratic Investment Gridlock (boom/frenzy)
			0 = {
				trigger = { has_law = law_type:law_cooperative_ownership }
				modifier = {
					if = { limit = { banking_cycle_is_boom = yes } add = 5 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 8 }
				}
				trigger_event = { id = banking_cycle_events.62 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 63: Worker-Owner Burnout (downturn/panic)
			0 = {
				trigger = { has_law = law_type:law_cooperative_ownership }
				modifier = {
					if = { limit = { banking_cycle_is_downturn = yes } add = 6 }
					if = { limit = { banking_cycle_is_panic = yes } add = 9 }
				}
				trigger_event = { id = banking_cycle_events.63 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 64: Cooperative Export Success (stable+)
			0 = {
				trigger = { has_law = law_type:law_cooperative_ownership }
				modifier = {
					if = {
						limit = {
							OR = {
								banking_cycle_is_stable = yes
								banking_cycle_is_expansion = yes
								banking_cycle_is_boom = yes
							}
						}
						add = 5
					}
				}
				trigger_event = { id = banking_cycle_events.64 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 65: Mutual Insurance Fund Strain (downturn/panic, keynesian)
			0 = {
				trigger = {
					has_law = law_type:law_cooperative_ownership
					has_technology_researched = keynesian_economics
				}
				modifier = {
					if = { limit = { banking_cycle_is_downturn = yes } add = 4 }
					if = { limit = { banking_cycle_is_panic = yes } add = 6 }
				}
				trigger_event = { id = banking_cycle_events.65 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 66: Innovation Through Collaboration (expansion+, central_banking)
			0 = {
				trigger = {
					has_law = law_type:law_cooperative_ownership
					has_technology_researched = central_banking
				}
				modifier = {
					if = { limit = { banking_cycle_is_expansion = yes } add = 4 }
					if = { limit = { banking_cycle_is_boom = yes } add = 4 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 4 }
				}
				trigger_event = { id = banking_cycle_events.66 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
			# Event 67: Generational Leadership Transition (boom/frenzy, corporate_management)
			0 = {
				trigger = {
					has_law = law_type:law_cooperative_ownership
					has_technology_researched = corporate_management
				}
				modifier = {
					if = { limit = { banking_cycle_is_boom = yes } add = 4 }
					if = { limit = { banking_cycle_is_frenzy = yes } add = 6 }
				}
				trigger_event = { id = banking_cycle_events.67 }
				set_variable = { name = banking_event_cooldown value = 12 }
			}
		}
	}
}

# Remove all financial cycle phase modifiers.
# Called before applying the current phase modifier each month.
remove_all_banking_phase_modifiers = {
	# Market variants
	remove_modifier = financial_cycle_phase_panic
	remove_modifier = financial_cycle_phase_downturn
	remove_modifier = financial_cycle_phase_stagnation
	remove_modifier = financial_cycle_phase_expansion
	remove_modifier = financial_cycle_phase_boom
	remove_modifier = financial_cycle_phase_frenzy
	# Command Economy variants
	remove_modifier = financial_cycle_phase_panic_cmd
	remove_modifier = financial_cycle_phase_downturn_cmd
	remove_modifier = financial_cycle_phase_stagnation_cmd
	remove_modifier = financial_cycle_phase_expansion_cmd
	remove_modifier = financial_cycle_phase_boom_cmd
	remove_modifier = financial_cycle_phase_frenzy_cmd
	# Cooperative Ownership variants
	remove_modifier = financial_cycle_phase_panic_coop
	remove_modifier = financial_cycle_phase_downturn_coop
	remove_modifier = financial_cycle_phase_stagnation_coop
	remove_modifier = financial_cycle_phase_expansion_coop
	remove_modifier = financial_cycle_phase_boom_coop
	remove_modifier = financial_cycle_phase_frenzy_coop
	# Bubble inertia
	remove_modifier = bubble_inertia_moderate
	remove_modifier = bubble_inertia_high
	remove_modifier = bubble_inertia_extreme
}

# Apply crash effects to the origin country (event .6).
# Tiered by crash_severity: correction (<20), downturn (20-39), crash (40-59), panic (60-79), systemic (≥80).
# Uses custom_tooltip so effects are visible in event option tooltips.
apply_banking_crash_origin_effects = {
	if = {
		limit = { var:crash_severity >= 80 }
		custom_tooltip = {
			text = CRASH_CYCLE_VALUE_5_DESC
			set_variable = { name = finance_cycle_value value = 5 }
		}
		custom_tooltip = {
			text = CRASH_MOMENTUM_MINUS_5_DESC
			set_variable = { name = finance_cycle_momentum value = -5 }
		}
	}
	else_if = {
		limit = { var:crash_severity >= 60 }
		custom_tooltip = {
			text = CRASH_CYCLE_VALUE_10_DESC
			set_variable = { name = finance_cycle_value value = 10 }
		}
		custom_tooltip = {
			text = CRASH_MOMENTUM_MINUS_4_DESC
			set_variable = { name = finance_cycle_momentum value = -4 }
		}
	}
	else_if = {
		limit = { var:crash_severity >= 40 }
		custom_tooltip = {
			text = CRASH_CYCLE_VALUE_20_DESC
			set_variable = { name = finance_cycle_value value = 20 }
		}
		custom_tooltip = {
			text = CRASH_MOMENTUM_MINUS_3_DESC
			set_variable = { name = finance_cycle_momentum value = -3 }
		}
	}
	else_if = {
		limit = { var:crash_severity >= 20 }
		custom_tooltip = {
			text = CRASH_CYCLE_VALUE_30_DESC
			set_variable = { name = finance_cycle_value value = 30 }
		}
		custom_tooltip = {
			text = CRASH_MOMENTUM_MINUS_2_DESC
			set_variable = { name = finance_cycle_momentum value = -2 }
		}
	}
	else = {
		custom_tooltip = {
			text = CRASH_CYCLE_VALUE_40_DESC
			set_variable = { name = finance_cycle_value value = 40 }
		}
		custom_tooltip = {
			text = CRASH_MOMENTUM_MINUS_1_DESC
			set_variable = { name = finance_cycle_momentum value = -1 }
		}
	}
	custom_tooltip = {
		text = CRASH_BUBBLE_CLEAR_DESC
		set_variable = { name = bubble_pressure value = 0 }
	}
}

# Apply contagion effects to a country hit by financial contagion (event .7).
# Tiered by origin country's crash_severity (via scope:banking_crash_origin_country).
# Uses change_variable (subtraction) since the target has its own independent cycle.
apply_banking_contagion_effects = {
	if = {
		limit = { scope:banking_crash_origin_country = { var:crash_severity >= 80 } }
		custom_tooltip = {
			text = CYCLE_VALUE_MINUS_15_DESC
			change_variable = { name = finance_cycle_value add = -15 }
		}
		custom_tooltip = {
			text = MOMENTUM_MINUS_3_DESC
			change_variable = { name = finance_cycle_momentum add = -3 }
		}
		custom_tooltip = {
			text = BUBBLE_PRESSURE_MINUS_25_DESC
			change_variable = { name = bubble_pressure add = -25 }
		}
	}
	else_if = {
		limit = { scope:banking_crash_origin_country = { var:crash_severity >= 60 } }
		custom_tooltip = {
			text = CYCLE_VALUE_MINUS_10_DESC
			change_variable = { name = finance_cycle_value add = -10 }
		}
		custom_tooltip = {
			text = MOMENTUM_MINUS_2_DESC
			change_variable = { name = finance_cycle_momentum add = -2 }
		}
		custom_tooltip = {
			text = BUBBLE_PRESSURE_MINUS_20_DESC
			change_variable = { name = bubble_pressure add = -20 }
		}
	}
	else_if = {
		limit = { scope:banking_crash_origin_country = { var:crash_severity >= 40 } }
		custom_tooltip = {
			text = CYCLE_VALUE_MINUS_8_DESC
			change_variable = { name = finance_cycle_value add = -8 }
		}
		custom_tooltip = {
			text = MOMENTUM_MINUS_1_DESC
			change_variable = { name = finance_cycle_momentum add = -1 }
		}
		custom_tooltip = {
			text = BUBBLE_PRESSURE_MINUS_15_DESC
			change_variable = { name = bubble_pressure add = -15 }
		}
	}
	else = {
		custom_tooltip = {
			text = CYCLE_VALUE_MINUS_5_DESC
			change_variable = { name = finance_cycle_value add = -5 }
		}
		custom_tooltip = {
			text = MOMENTUM_MINUS_1_DESC
			change_variable = { name = finance_cycle_momentum add = -1 }
		}
		custom_tooltip = {
			text = BUBBLE_PRESSURE_MINUS_10_DESC
			change_variable = { name = bubble_pressure add = -10 }
		}
	}
	# Clamp values
	if = {
		limit = { var:finance_cycle_value < 10 }
		set_variable = { name = finance_cycle_value value = 10 }
	}
	if = {
		limit = { var:finance_cycle_momentum < -5 }
		set_variable = { name = finance_cycle_momentum value = -5 }
	}
	if = {
		limit = { var:bubble_pressure < 0 }
		set_variable = { name = bubble_pressure value = 0 }
	}
}

# ============================================================================
# MONTHLY PULSE SUB-EFFECTS
# These are called from je_banking_cycle on_monthly_pulse and from banking
# event `after` blocks.  Each handles one logical phase of the monthly cycle.
# ============================================================================

# 1. Fiscal policy modifier: re-apply every month to reflect current deficit/surplus.
banking_cycle_update_fiscal_policy = {
	remove_modifier = financial_cycle_government_fiscal_policy_effect
	add_modifier = {
		name = financial_cycle_government_fiscal_policy_effect
		multiplier = financial_cycle_government_fiscal_policy_effect_size
	}
}

# 2. Advance all cycle variables for the month.
#    - Momentum decays towards zero (×0.9)
#    - Modifier-driven changes to momentum, bubble, and value
#    - Investment pool contribution
#    - Random mean-reversion nudge (biased towards equilibrium)
#    - Apply momentum to value
#    - Clamp all variables to valid ranges
banking_cycle_advance_variables = {
	# Momentum decays towards zero
	change_variable = { name = finance_cycle_momentum multiply = 0.9 }

	# Modifier-driven monthly changes
	change_variable = {
		name = finance_cycle_momentum
		add = "modifier:country_finance_momentum_monthly_add"
	}
	change_variable = {
		name = bubble_pressure
		add = "modifier:country_bubble_pressure_monthly_add"
	}
	change_variable = {
		name = finance_cycle_value
		add = "modifier:country_finance_value_monthly_add"
	}

	# Investment pool contribution
	add_investment_pool = investment_pool_banking_cycle_add

	# Random mean-reversion nudge: biased towards equilibrium
	# Magnitude scales with country_banking_random_momentum_mult modifier
	random_list = {
		25 = {
			modifier = {
				value = var:finance_cycle_value
			}
			change_variable = { name = finance_cycle_momentum add = banking_random_nudge_down_value }
		}
		50 = { }
		25 = {
			modifier = {
				value = 100
				subtract = var:finance_cycle_value
			}
			change_variable = { name = finance_cycle_momentum add = banking_random_nudge_up_value }
		}
	}

	# Apply momentum to cycle value
	change_variable = {
		name = finance_cycle_value
		add = var:finance_cycle_momentum
	}

	# Clamp cycle value 0–100
	if = { limit = { var:finance_cycle_value < 0 }   set_variable = { name = finance_cycle_value value = 0 } }
	if = { limit = { var:finance_cycle_value > 100 } set_variable = { name = finance_cycle_value value = 100 } }

	# Clamp bubble pressure 0–100
	if = { limit = { var:bubble_pressure < 0 }   set_variable = { name = bubble_pressure value = 0 } }
	if = { limit = { var:bubble_pressure > 100 } set_variable = { name = bubble_pressure value = 100 } }
}

# 3. Check for a sudden crash (asymmetric probability).
#    If crash triggers, fire the origin event and spread contagion to
#    trade-linked countries weighted by GDP.
banking_cycle_check_and_execute_crash = {
	set_variable = { name = crash value = 0 }
	random_list = {
		0 = {
			modifier = {
				add = var:bubble_pressure
				if = {
					limit = { var:finance_cycle_value >= 88 } # frenzy
					subtract = 15
					multiply = 0.35
				}
				else_if = {
					limit = { var:finance_cycle_value >= 75 } # boom
					subtract = 25
					multiply = 0.15
				}
				else_if = {
					limit = { var:finance_cycle_value >= 60 } # expansion
					subtract = 50
					multiply = 0.05
				}
				else_if = {
					limit = { var:finance_cycle_value >= 40 } # stable
					subtract = 75
					multiply = 0.02
				}
				min = 0
				# Scale crash probability by the crash chance modifier
				multiply = banking_crash_chance_multiplier_value
			}
			set_variable = { name = crash value = 1 }
		}
		100 = { }
	}

	if = {
		limit = { var:crash > 0 }
		save_scope_as = banking_crash_origin_country

		# Severity scales with bubble pressure (random multiplier 0.5–2.0)
		set_variable = { name = crash_severity value = var:bubble_pressure }
		random_list = {
			10 = { change_variable = { name = crash_severity multiply = 0.5 } }
			20 = { change_variable = { name = crash_severity multiply = 0.75 } }
			40 = { }
			20 = { change_variable = { name = crash_severity multiply = 1.5 } }
			10 = { change_variable = { name = crash_severity multiply = 2 } }
		}

		trigger_event = { id = minor_events_timelineextended.6 popup = yes }

		# Spread contagion to trade-linked countries
		banking_cycle_spread_contagion = yes
	}
}

# 3b. Spread financial contagion to eligible countries.
#     Probability depends on trade linkage, severity, AND relative GDP.
#     Countries where the source is less than ~1% of target GDP are immune.
banking_cycle_spread_contagion = {
	every_country = {
		limit = {
			NOT = { THIS = scope:banking_crash_origin_country }
			has_journal_entry = je_banking_cycle
			# Trade-reachable
			OR = {
				this.market = scope:banking_crash_origin_country.market
				is_market_reachable_for_trade = scope:banking_crash_origin_country.market
			}
			# At least one trade channel
			OR = {
				this.market = scope:banking_crash_origin_country.market
				is_in_customs_union_with = scope:banking_crash_origin_country
				is_adjacent_to_country = scope:banking_crash_origin_country
				economic_dependence = {
					target = scope:banking_crash_origin_country
					value > 0.05
				}
				market = {
					market_trade_reliance = {
						target = scope:banking_crash_origin_country.market
						value > 0.01
					}
				}
				market = {
					market_production_share = {
						target = scope:banking_crash_origin_country
						value > 0.01
					}
				}
				market = {
					market_consumption_share = {
						target = scope:banking_crash_origin_country
						value > 0.01
					}
				}
			}
			# GDP filter: source must have at least 1% of target's GDP
			# Tiny economies cannot destabilise giants
			scope:banking_crash_origin_country.gdp >= banking_contagion_min_source_gdp
		}

		random = {
			chance = 25
			# GDP weight: larger source economy → higher contagion chance
			modifier = {
				add = banking_contagion_gdp_weight
			}
			# Same market = strongest contagion channel
			modifier = {
				trigger = { this.market = scope:banking_crash_origin_country.market }
				add = 10
			}
			# Hard crash spreads more easily
			modifier = {
				trigger = { scope:banking_crash_origin_country = { var:crash_severity >= 60 } }
				add = 10
			}
			modifier = {
				trigger = { scope:banking_crash_origin_country = { var:crash_severity >= 30 var:crash_severity < 60 } }
				add = 5
			}
			modifier = {
				trigger = { is_in_customs_union_with = scope:banking_crash_origin_country }
				add = 10
			}
			modifier = {
				trigger = { is_adjacent_to_country = scope:banking_crash_origin_country }
				add = 5
			}
			modifier = {
				trigger = { economic_dependence = { target = scope:banking_crash_origin_country value > 0.10 } }
				add = 5
			}
			modifier = {
				trigger = { economic_dependence = { target = scope:banking_crash_origin_country value > 0.25 } }
				add = 10
			}
			modifier = {
				trigger = { market = { market_trade_reliance = { target = scope:banking_crash_origin_country.market value > 0.05 } } }
				add = 5
			}
			modifier = {
				trigger = { market = { market_trade_reliance = { target = scope:banking_crash_origin_country.market value > 0.1 } } }
				add = 10
			}
			modifier = {
				trigger = { scope:banking_crash_origin_country.relations:THIS >= relations_threshold:cordial }
				add = 1.5
			}
			modifier = {
				trigger = { has_law_or_variant = law_type:law_free_trade }
				add = 1.5
			}

			scope:banking_crash_origin_country = {
				save_scope_as = banking_crash_origin_country
			}

			trigger_event = { id = minor_events_timelineextended.7 popup = yes days = 7 }
		}
	}
}

# 4. Apply phase modifiers based on current cycle values.
#    Removes old phase & bubble-inertia modifiers, then applies current ones.
banking_cycle_apply_phase_modifiers = {
	remove_all_banking_phase_modifiers = yes

	# Bubble inertia: high speculation sustains expansion (positive feedback)
	if = {
		limit = {
			NOR = {
				has_law = law_type:law_command_economy
				has_law = law_type:law_cooperative_ownership
			}
		}
		if = {
			limit = { var:bubble_pressure >= 65 }
			add_modifier = { name = bubble_inertia_extreme multiplier = bubble_inertia_multiplier_script_value }
		}
		else_if = {
			limit = { var:bubble_pressure >= 35 }
			add_modifier = { name = bubble_inertia_high multiplier = bubble_inertia_multiplier_script_value }
		}
		else_if = {
			limit = { var:bubble_pressure > 0.5 }
			add_modifier = { name = bubble_inertia_moderate multiplier = bubble_inertia_multiplier_script_value }
		}
	}

	# Phase modifier by economic law type
	if = {
		limit = { has_law = law_type:law_command_economy }
		if = { limit = { var:finance_cycle_value < 10 }  add_modifier = { name = financial_cycle_phase_panic_cmd } }
		else_if = { limit = { var:finance_cycle_value < 25 } add_modifier = { name = financial_cycle_phase_downturn_cmd } }
		else_if = { limit = { var:finance_cycle_value < 40 } add_modifier = { name = financial_cycle_phase_stagnation_cmd } }
		else_if = { limit = { var:finance_cycle_value < 60 } } # Stable = no modifier
		else_if = { limit = { var:finance_cycle_value < 75 } add_modifier = { name = financial_cycle_phase_expansion_cmd } }
		else_if = { limit = { var:finance_cycle_value < 88 } add_modifier = { name = financial_cycle_phase_boom_cmd } }
		else = { add_modifier = { name = financial_cycle_phase_frenzy_cmd } }
	}
	else_if = {
		limit = { has_law = law_type:law_cooperative_ownership }
		if = { limit = { var:finance_cycle_value < 10 }  add_modifier = { name = financial_cycle_phase_panic_coop } }
		else_if = { limit = { var:finance_cycle_value < 25 } add_modifier = { name = financial_cycle_phase_downturn_coop } }
		else_if = { limit = { var:finance_cycle_value < 40 } add_modifier = { name = financial_cycle_phase_stagnation_coop } }
		else_if = { limit = { var:finance_cycle_value < 60 } } # Stable = no modifier
		else_if = { limit = { var:finance_cycle_value < 75 } add_modifier = { name = financial_cycle_phase_expansion_coop } }
		else_if = { limit = { var:finance_cycle_value < 88 } add_modifier = { name = financial_cycle_phase_boom_coop } }
		else = { add_modifier = { name = financial_cycle_phase_frenzy_coop } }
	}
	else = {
		if = { limit = { var:finance_cycle_value < 10 }  add_modifier = { name = financial_cycle_phase_panic } }
		else_if = { limit = { var:finance_cycle_value < 25 } add_modifier = { name = financial_cycle_phase_downturn } }
		else_if = { limit = { var:finance_cycle_value < 40 } add_modifier = { name = financial_cycle_phase_stagnation } }
		else_if = { limit = { var:finance_cycle_value < 60 } } # Stable = no modifier
		else_if = { limit = { var:finance_cycle_value < 75 } add_modifier = { name = financial_cycle_phase_expansion } }
		else_if = { limit = { var:finance_cycle_value < 88 } add_modifier = { name = financial_cycle_phase_boom } }
		else = { add_modifier = { name = financial_cycle_phase_frenzy } }
	}
}

# 5. Update the three JE progress bars.
#    Called from the monthly pulse (where scope:journal_entry is available).
banking_cycle_update_progress_bars = {
	scope:journal_entry = {
		set_bar_progress = { name = banking_cycle_value_bar value = ROOT.var:finance_cycle_value }
		set_bar_progress = { name = banking_bubble_pressure_bar value = ROOT.var:bubble_pressure }
		if = {
			limit = { ROOT.var:finance_cycle_momentum < -5 }
			set_bar_progress = { name = banking_cycle_momentum_bar value = -5 }
		}
		else_if = {
			limit = { ROOT.var:finance_cycle_momentum > 5 }
			set_bar_progress = { name = banking_cycle_momentum_bar value = 5 }
		}
		else = {
			set_bar_progress = { name = banking_cycle_momentum_bar value = ROOT.var:finance_cycle_momentum }
		}
	}
}

# 6. Refresh phase modifiers and progress bars after a banking event option.
#    Called from banking event `after` blocks where scope:journal_entry
#    is NOT available, so we scope via the je: accessor instead.
banking_cycle_post_event_refresh = {
	hidden_effect = {
		banking_cycle_apply_phase_modifiers = yes
		je:je_banking_cycle = {
			set_bar_progress = { name = banking_cycle_value_bar value = ROOT.var:finance_cycle_value }
			set_bar_progress = { name = banking_bubble_pressure_bar value = ROOT.var:bubble_pressure }
			if = {
				limit = { ROOT.var:finance_cycle_momentum < -5 }
				set_bar_progress = { name = banking_cycle_momentum_bar value = -5 }
			}
			else_if = {
				limit = { ROOT.var:finance_cycle_momentum > 5 }
				set_bar_progress = { name = banking_cycle_momentum_bar value = 5 }
			}
			else = {
				set_bar_progress = { name = banking_cycle_momentum_bar value = ROOT.var:finance_cycle_momentum }
			}
		}
	}
}

# 7. Remove capital controls modifier if law/war conditions no longer apply.
banking_cycle_cleanup_capital_controls = {
	if = {
		limit = {
			has_modifier = banking_capital_controls_out
			NOT = {
				OR = {
					has_law = law_type:law_prudential_narrow_banking
					has_law = law_type:law_directed_credit_development_banks
					is_at_war = yes
				}
			}
		}
		remove_modifier = banking_capital_controls_out
	}
}
