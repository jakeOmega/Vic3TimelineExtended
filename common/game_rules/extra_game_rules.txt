custom_religions_allowed_rule = {
	default = custom_religions_not_allowed

	custom_religions_not_allowed = {
		flag = custom_religions_not_allowed
	}

	custom_religions_allowed = {
		flag = custom_religions_allowed
	}
}

# Three settings, not two. `banking_system_simplified` keeps the Banking Cycle
# journal entry — the cycle, its crashes, the prudential tools, the financial
# regulation laws — and switches off the whole monetary-policy layer built on
# top of it (docs/systems/monetary_policy_design.md phases 1-5): no policy-rate
# dial, no inflation, no exchange rate, no pegs / swap lines / guarantees /
# common currency. A country still pays a country-specific interest rate, it
# just cannot steer it: the world reference rate plus its risk premium.
#
# THE RATE PLUMBING RUNS UNDER ALL THREE SETTINGS. Vanilla's flat 20% base rate,
# the six rank multipliers, laissez-faire and the five finance techs are all
# cancelled by unconditional INJECTs (monetary_policy_design.md §16.1), which no
# game rule can switch off, so something in script has to supply the number or
# every country would borrow free. `te_mon_full_system`
# (common/scripted_triggers/te_monetary_triggers.txt) is the one trigger that
# tells the two halves apart.
banking_system_rule = {
	default = banking_system_enabled

	banking_system_enabled = {
		flag = banking_system_enabled
	}

	banking_system_simplified = {
		flag = banking_system_simplified
	}

	banking_system_disabled = {
		flag = banking_system_disabled
	}
}

global_warming_rule = {
	default = global_warming_enabled

	global_warming_enabled = {
		flag = global_warming_enabled
	}

	global_warming_disabled = {
		flag = global_warming_disabled
	}
}

world_war_rule = {
	default = world_war_disabled

	world_war_enabled = {
		flag = world_war_enabled
	}

	world_war_disabled = {
		flag = world_war_disabled
	}
}

cultural_hegemony_rule = {
	default = cultural_hegemony_enabled

	cultural_hegemony_enabled = {
		flag = cultural_hegemony_enabled
	}

	cultural_hegemony_disabled = {
		flag = cultural_hegemony_disabled
	}
}

covert_warfare_rule = {
	default = covert_warfare_enabled

	covert_warfare_enabled = {
		flag = covert_warfare_enabled
	}

	covert_warfare_disabled = {
		flag = covert_warfare_disabled
	}
}

heir_education_rule = {
	default = heir_education_disabled

	heir_education_enabled = {
		flag = heir_education_enabled
	}

	heir_education_disabled = {
		flag = heir_education_disabled
	}
}

united_nations_rule = {
	default = united_nations_enabled

	united_nations_enabled = {
		flag = united_nations_enabled
	}

	united_nations_disabled = {
		flag = united_nations_disabled
	}
}

nuclear_weapons_rule = {
	default = nuclear_weapons_enabled

	nuclear_weapons_enabled = {
		flag = nuclear_weapons_enabled
	}

	nuclear_weapons_disabled = {
		flag = nuclear_weapons_disabled
	}
}

decolonization_rule = {
	default = decolonization_enabled

	decolonization_enabled = {
		flag = decolonization_enabled
	}

	decolonization_disabled = {
		flag = decolonization_disabled
	}
}

space_race_rule = {
	default = space_race_enabled

	space_race_enabled = {
		flag = space_race_enabled
	}

	space_race_disabled = {
		flag = space_race_disabled
	}
}

social_movements_rule = {
	default = social_movements_enabled

	social_movements_enabled = {
		flag = social_movements_enabled
	}

	social_movements_disabled = {
		flag = social_movements_disabled
	}
}

# When enabled, aptitude traits (admin / diplo / military) are assigned to
# ALL adult characters, not just rulers / politicians / agitators / generals / admirals.
universal_aptitude_traits_rule = {
	default = universal_aptitude_traits_disabled

	universal_aptitude_traits_enabled = {
		flag = universal_aptitude_traits_enabled
	}

	universal_aptitude_traits_disabled = {
		flag = universal_aptitude_traits_disabled
	}
}