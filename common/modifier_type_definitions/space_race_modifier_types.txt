# space_race_modifier_types.txt
#
# Space race progress modifiers consumed by je_space_race and its scripted
# progress bar. Funding ceiling, monthly progress add/mult (ambitious approach),
# and risk multiplier (safe approach).

# Maximum space race funding level (base 3, increased by technologies)
country_space_race_max_funding_add = {
	color = good
	percent = no
	decimals = 0
	game_data = {
		ai_value = 50
	}
}

# Space race monthly progress bonus (added to base rate from space program PM)
country_space_race_progress_add = {
	color = good
	percent = no
	decimals = 1
	game_data = {
		ai_value = 5000
	}
}

# Space race progress multiplier (ambitious approach: +50% per JE)
country_space_race_progress_mult = {
	color = good
	percent = yes
	game_data = {
		ai_value = 5000
	}
}

# Space race failure risk multiplier (safe approach: -50% per JE)
country_space_race_risk_mult = {
	color = bad
	percent = yes
	game_data = {
		ai_value = -5000
	}
}
