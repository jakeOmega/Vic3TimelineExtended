# un_membership_modifier_types.txt
#
# UN membership flag read by the UN journal entry and its scripted_buttons.

country_un_membership_obligation_bool = {
	color = neutral
	boolean = yes
	game_data = {
		ai_value = 50
	}
	script_only = yes
}

# Institutional-alignment scaler for the Multilateral Institutions principle group
# (identity_diplomatic). Granted on member_modifier, scaling with tier (0.25 -> 0.5).
# Read by:
#   - un_authority_values.txt (un_member_stance): an aligned member's stance toward
#     the organisation rises by its alignment, which feeds the great-power
#     commitment pillar weighted by its power share, so heft drives it, not
#     headcount.
#   - je_united_nations.txt: scales that member's un_membership_benefits_modifier by
#     (1 + alignment), so the bloc both feeds and feeds off UN authority.
country_un_institutional_alignment = {
	color = good
	percent = no
	script_only = yes
}
