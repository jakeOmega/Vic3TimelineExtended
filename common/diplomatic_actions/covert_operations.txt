# ============================================================================
# COVERT WARFARE — Covert Operation Diplomatic Actions
# ============================================================================
# These diplomatic actions function like increase_relations: togglable pacts
# that persist until the player breaks them or conditions change.
# Effects are applied via the JE monthly pulse while the pact is active.
# ============================================================================

# ---- PEACETIME: Election Interference ----
# Undermine a target's political stability through covert influence campaigns
covert_election_interference_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_election_interference_action } }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_not_at_war_tt
					NOT = { has_war_with = scope:target_country }
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			covert_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
			OR = {
				has_diplomatic_pact = { who = scope:target_country type = rivalry }
				has_attitude = { who = scope:target_country attitude = antagonistic }
			}
		}

		propose_score = {
			value = 10
		}
	}
}

# ---- PEACETIME: Financial Subversion ----
# Destabilize a target's economy through market manipulation and currency attacks
covert_financial_subversion_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_financial_subversion_action } }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_not_at_war_tt
					NOT = { has_war_with = scope:target_country }
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			covert_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
			OR = {
				has_diplomatic_pact = { who = scope:target_country type = rivalry }
				has_attitude = { who = scope:target_country attitude = antagonistic }
			}
		}

		propose_score = {
			value = 10
		}
	}
}

# ---- WARTIME: Infrastructure Sabotage ----
# Disrupt enemy production and logistics through sabotage operations
covert_infrastructure_sabotage_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_infrastructure_sabotage_action } }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_at_war_tt
			is_at_war = yes
		}
		has_war_with = scope:target_country
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_at_war_tt
					has_war_with = scope:target_country
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.15
		}

		will_propose = {
			has_war_with = scope:target_country
			covert_operations_available_slots >= 1
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}

		propose_score = {
			value = 15
		}
	}
}

# ---- WARTIME: Communications Disruption ----
# Disrupt enemy military command and control systems
covert_comms_disruption_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_comms_disruption_action } }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_at_war_tt
			is_at_war = yes
		}
		has_war_with = scope:target_country
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_at_war_tt
					has_war_with = scope:target_country
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.15
		}

		will_propose = {
			has_war_with = scope:target_country
			covert_operations_available_slots >= 1
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}

		propose_score = {
			value = 15
		}
	}
}

# ---- PEACETIME: Industrial Espionage ----
# Steal production secrets and technology from a target nation
# Effect: boosts own production tech spread (only if target has tech advantage)
# and innovation (always)
covert_industrial_espionage_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_industrial_espionage_action } }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_not_at_war_tt
					NOT = { has_war_with = scope:target_country }
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			covert_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
			scope:target_country = {
				techs_researched > ROOT.techs_researched
			}
		}

		propose_score = {
			value = 10
		}
	}
}

# ---- PEACETIME: Military Espionage ----
# Gather intelligence on a target's military capabilities and steal secrets
# Effect: boosts own military tech spread (only if target has tech advantage)
# and unit effectiveness (always)
covert_military_espionage_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_military_espionage_action } }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_not_at_war_tt
					NOT = { has_war_with = scope:target_country }
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			covert_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
			scope:target_country = {
				techs_researched > ROOT.techs_researched
			}
		}

		propose_score = {
			value = 10
		}
	}
}

# ---- PEACETIME: Influence Campaign ----
# Bribery, blackmail, and covert media campaigns to weaken a nation's
# resistance to power bloc pressure. Requires being a Power Bloc leader.
covert_influence_campaign_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_influence_campaign_action } }
		is_power_bloc_leader = yes
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		custom_tooltip = {
			text = iw_target_not_in_bloc_tt
			NOT = { is_in_same_power_bloc = scope:target_country }
		}
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_not_at_war_tt
					NOT = { has_war_with = scope:target_country }
				}
			}
		}

		requirement_to_maintain = {
			trigger = {
				is_power_bloc_leader = yes
			}
		}

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_target_not_in_bloc_tt
					NOT = { is_in_same_power_bloc = scope:target_country }
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			is_power_bloc_leader = yes
			NOT = { is_in_same_power_bloc = scope:target_country }
			covert_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}

		propose_score = {
			value = 10
		}
	}
}

# ---- PEACETIME: Ideological Subversion ----
# Covertly support political movements aligned with YOUR government type
# in a foreign nation. Does NOT require rivalry.
covert_ideological_subversion_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_ideological_subversion_action } }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		custom_tooltip = {
			text = iw_target_has_movements_tt
			scope:target_country = {
				any_political_movement = { count >= 1 }
			}
		}
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_not_at_war_tt
					NOT = { has_war_with = scope:target_country }
				}
			}
		}

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_target_has_movements_tt
					scope:target_country = {
						any_political_movement = { count >= 1 }
					}
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			covert_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
			scope:target_country = {
				any_political_movement = { count >= 1 }
			}
		}

		propose_score = {
			value = 8
		}
	}
}

# ---- PEACETIME: Destabilization ----
# Covertly fund separatist movements and general unrest in a rival nation.
# Requires rivalry — the most aggressive peacetime operation.
covert_destabilization_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_game_rule = covert_warfare_enabled
		has_technology_researched = television
		NOT = { any_scope_diplomatic_pact = { is_diplomatic_action_type = covert_destabilization_action } }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			covert_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		has_diplomatic_pact = { who = scope:target_country type = rivalry }
		custom_tooltip = {
			text = iw_target_has_movements_tt
			scope:target_country = {
				any_political_movement = { count >= 1 }
			}
		}
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	pact = {
		cost = 0
		is_two_sided_pact = no
		show_in_outliner = yes

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_not_at_war_tt
					NOT = { has_war_with = scope:target_country }
				}
			}
		}

		requirement_to_maintain = {
			trigger = {
				has_diplomatic_pact = { who = scope:target_country type = rivalry }
			}
		}

		requirement_to_maintain = {
			trigger = {
				custom_tooltip = {
					text = iw_target_has_movements_tt
					scope:target_country = {
						any_political_movement = { count >= 1 }
					}
				}
			}
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			has_diplomatic_pact = { who = scope:target_country type = rivalry }
			covert_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
			scope:target_country = {
				any_political_movement = { count >= 1 }
			}
		}

		propose_score = {
			value = 10
		}
	}
}
