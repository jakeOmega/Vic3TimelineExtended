# ============================================================================
# INFORMATION WARFARE — Cyber Operation Diplomatic Actions
# ============================================================================
# These diplomatic actions allow a country to launch covert cyber operations
# against a target country. The operations persist as tracked variables and
# their effects are applied via the JE monthly pulse.
# ============================================================================

# ---- PEACETIME: Election Interference ----
# Undermine a rival's political stability through covert influence campaigns
cyber_election_interference_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_technology_researched = television
		NOT = { has_variable = iw_op_election_interference }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			cyber_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		has_diplomatic_pact = { who = scope:target_country type = rivalry }
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	accept_effect = {
		set_variable = iw_op_election_interference
		set_variable = { name = iw_target_election_interference value = scope:target_country }
		if = {
			limit = { NOT = { has_variable = iw_active_operations } }
			set_variable = { name = iw_active_operations value = 0 }
		}
		change_variable = { name = iw_active_operations add = 1 }
		post_notification = iw_operation_launched
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			has_diplomatic_pact = { who = scope:target_country type = rivalry }
			cyber_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}

		propose_score = {
			value = 10
		}
	}
}

# ---- PEACETIME: Financial Subversion ----
# Destabilize a rival's economy through market manipulation and currency attacks
cyber_financial_subversion_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_technology_researched = television
		NOT = { has_variable = iw_op_financial_subversion }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			cyber_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_not_at_war_tt
			is_at_war = no
		}
		has_diplomatic_pact = { who = scope:target_country type = rivalry }
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	accept_effect = {
		set_variable = iw_op_financial_subversion
		set_variable = { name = iw_target_financial_subversion value = scope:target_country }
		if = {
			limit = { NOT = { has_variable = iw_active_operations } }
			set_variable = { name = iw_active_operations value = 0 }
		}
		change_variable = { name = iw_active_operations add = 1 }
		post_notification = iw_operation_launched
	}

	ai = {
		evaluation_chance = {
			value = 0.1
		}

		will_propose = {
			has_diplomatic_pact = { who = scope:target_country type = rivalry }
			cyber_operations_available_slots >= 1
			is_at_war = no
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}

		propose_score = {
			value = 10
		}
	}
}

# ---- WARTIME: Infrastructure Sabotage ----
# Disrupt enemy production and logistics through infrastructure attacks
cyber_infrastructure_sabotage_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_technology_researched = television
		NOT = { has_variable = iw_op_infrastructure_sabotage }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			cyber_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_at_war_tt
			is_at_war = yes
		}
		has_war_with = scope:target_country
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	accept_effect = {
		set_variable = iw_op_infrastructure_sabotage
		set_variable = { name = iw_target_infrastructure_sabotage value = scope:target_country }
		if = {
			limit = { NOT = { has_variable = iw_active_operations } }
			set_variable = { name = iw_active_operations value = 0 }
		}
		change_variable = { name = iw_active_operations add = 1 }
		post_notification = iw_operation_launched
	}

	ai = {
		evaluation_chance = {
			value = 0.15
		}

		will_propose = {
			has_war_with = scope:target_country
			cyber_operations_available_slots >= 1
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}

		propose_score = {
			value = 15
		}
	}
}

# ---- WARTIME: Communications Disruption ----
# Jam and disrupt enemy military command and control systems
cyber_comms_disruption_action = {
	groups = {
		general
	}
	requires_approval = no
	show_confirmation_box = yes
	is_hostile = yes
	should_notify_third_parties = no

	potential = {
		has_technology_researched = television
		NOT = { has_variable = iw_op_comms_disruption }
	}

	possible = {
		custom_tooltip = {
			text = iw_has_available_slot_tt
			cyber_operations_available_slots >= 1
		}
		custom_tooltip = {
			text = iw_at_war_tt
			is_at_war = yes
		}
		has_war_with = scope:target_country
		custom_tooltip = {
			text = iw_funding_not_zero_tt
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}
	}

	accept_effect = {
		set_variable = iw_op_comms_disruption
		set_variable = { name = iw_target_comms_disruption value = scope:target_country }
		if = {
			limit = { NOT = { has_variable = iw_active_operations } }
			set_variable = { name = iw_active_operations value = 0 }
		}
		change_variable = { name = iw_active_operations add = 1 }
		post_notification = iw_operation_launched
	}

	ai = {
		evaluation_chance = {
			value = 0.15
		}

		will_propose = {
			has_war_with = scope:target_country
			cyber_operations_available_slots >= 1
			has_variable = iw_funding_level
			var:iw_funding_level >= 1
		}

		propose_score = {
			value = 15
		}
	}
}
