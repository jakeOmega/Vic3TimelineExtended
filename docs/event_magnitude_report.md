# Event magnitude audit report

Auto-generated by `event_magnitude_audit.py` on every full
`POST /reload` of the mod state server. Do not hand-edit.

Suppress a flag by adding a same-line comment in the format:
`# REVIEWED YYYY-MM-DD: rationale`

## Unreviewed Flags

### bureaucracy (1)

- `events/un_events.txt:786` — event `un_events.7` — `country_bureaucracy_add = country_bureaucracy_add=-50 (in un_humanitarian_aid_modifier)` — fix: use bureaucracy_loss_<tier> / bureaucracy_gain_<tier> static modifier (mult-based, no multiplier needed) — `un_humanitarian_aid_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.

### prestige (308)

- `events/augmentation_events.txt:133` — event `augmentation_events.2` — `country_prestige_add = country_prestige_add=10 (in augmentation_regulation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `augmentation_regulation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/augmentation_events.txt:199` — event `augmentation_events.3` — `country_prestige_add = country_prestige_add=10 (in augmentation_workplace_protection_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `augmentation_workplace_protection_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/augmentation_events.txt:405` — event `augmentation_events.5` — `country_prestige_add = country_prestige_add=-10 (in augmentation_kill_switch_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `augmentation_kill_switch_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/augmentation_events.txt:468` — event `augmentation_events.100` — `country_prestige_add = country_prestige_add=-25 (in augmentation_divide_permanent_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `augmentation_divide_permanent_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/augmentation_events.txt:481` — event `augmentation_events.100` — `country_prestige_add = country_prestige_add=-15 (in human_purity_enforced_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `human_purity_enforced_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/augmentation_events.txt:514` — event `augmentation_events.200` — `country_prestige_add = country_prestige_add=50 (in augmentation_integrated_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `augmentation_integrated_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:66` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=-50 (in decolonization_negotiations_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `decolonization_negotiations_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:77` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:99` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=-75 (in colonial_crackdown_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_crackdown_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:111` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:147` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=100 (in peaceful_decolonization_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `peaceful_decolonization_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:173` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:190` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=-50 (in neocolonial_dependency_imposed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `neocolonial_dependency_imposed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:196` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=25 (in neocolonial_economic_extraction_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `neocolonial_economic_extraction_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:222` — event `decolonization_events.1` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:290` — event `decolonization_events.2` — `country_prestige_add = country_prestige_add=-50 (in decolonization_negotiations_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `decolonization_negotiations_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:312` — event `decolonization_events.2` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:320` — event `decolonization_events.2` — `country_prestige_add = country_prestige_add=-75 (in colonial_crackdown_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_crackdown_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:340` — event `decolonization_events.2` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:352` — event `decolonization_events.2` — `country_prestige_add = country_prestige_add=25 (in neocolonial_economic_extraction_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `neocolonial_economic_extraction_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:373` — event `decolonization_events.2` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:434` — event `decolonization_events.3` — `country_prestige_add = country_prestige_add=-100 (in colonial_war_exhaustion_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_war_exhaustion_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:452` — event `decolonization_events.3` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:462` — event `decolonization_events.3` — `country_prestige_add = country_prestige_add=-75 (in colonial_crackdown_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_crackdown_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:474` — event `decolonization_events.3` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:491` — event `decolonization_events.3` — `country_prestige_add = country_prestige_add=-50 (in neocolonial_dependency_imposed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `neocolonial_dependency_imposed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:497` — event `decolonization_events.3` — `country_prestige_add = country_prestige_add=25 (in neocolonial_economic_extraction_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `neocolonial_economic_extraction_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:514` — event `decolonization_events.3` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:572` — event `decolonization_events.4` — `country_prestige_add = country_prestige_add=25 (in cultural_reclamation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_reclamation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:647` — event `decolonization_events.5` — `country_prestige_add = country_prestige_add=75 (in post_colonial_partnership_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `post_colonial_partnership_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:724` — event `decolonization_events.6` — `country_prestige_add = country_prestige_add=-100 (in colonial_reparations_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_reparations_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:825` — event `decolonization_events.7` — `country_prestige_add = country_prestige_add=-25 (in foreign_investment_dependency_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `foreign_investment_dependency_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:846` — event `decolonization_events.7` — `country_prestige_add = country_prestige_add=25 (in post_colonial_self_reliance_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `post_colonial_self_reliance_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:907` — event `decolonization_events.8` — `country_prestige_add = country_prestige_add=-50 (in decolonization_negotiations_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `decolonization_negotiations_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:927` — event `decolonization_events.8` — `country_prestige_add = country_prestige_add=-75 (in colonial_crackdown_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_crackdown_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:932` — event `decolonization_events.8` — `country_prestige_add = country_prestige_add=-25 (in anti_colonial_momentum_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `anti_colonial_momentum_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:940` — event `decolonization_events.8` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1008` — event `decolonization_events.9` — `country_prestige_add = country_prestige_add=50 (in colonial_integration_progress_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_integration_progress_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1015` — event `decolonization_events.9` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1029` — event `decolonization_events.9` — `country_prestige_add = country_prestige_add=-50 (in colonial_unrest_suppressed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_unrest_suppressed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1039` — event `decolonization_events.9` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1065` — event `decolonization_events.9` — `country_prestige_add = country_prestige_add=100 (in peaceful_decolonization_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `peaceful_decolonization_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1071` — event `decolonization_events.9` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1089` — event `decolonization_events.9` — `country_prestige_add = country_prestige_add=25 (in neocolonial_economic_extraction_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `neocolonial_economic_extraction_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1095` — event `decolonization_events.9` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1168` — event `decolonization_events.10` — `country_prestige_add = country_prestige_add=50 (in colonial_integration_progress_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_integration_progress_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1172` — event `decolonization_events.10` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1184` — event `decolonization_events.10` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1249` — event `decolonization_events.11` — `country_prestige_add = country_prestige_add=-50 (in decolonization_negotiations_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `decolonization_negotiations_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1267` — event `decolonization_events.11` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1281` — event `decolonization_events.11` — `country_prestige_add = country_prestige_add=-75 (in gp_condemns_colonialism_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_condemns_colonialism_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1296` — event `decolonization_events.11` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1349` — event `decolonization_events.12` — `country_prestige_add = country_prestige_add=50 (in gp_supports_colonialism_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_supports_colonialism_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1361` — event `decolonization_events.12` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1378` — event `decolonization_events.12` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1450` — event `decolonization_events.13` — `country_prestige_add = country_prestige_add=-100 (in colonial_war_exhaustion_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_war_exhaustion_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1457` — event `decolonization_events.13` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1467` — event `decolonization_events.13` — `country_prestige_add = country_prestige_add=-75 (in colonial_crackdown_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_crackdown_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1478` — event `decolonization_events.13` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1496` — event `decolonization_events.13` — `country_prestige_add = country_prestige_add=25 (in neocolonial_economic_extraction_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `neocolonial_economic_extraction_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1502` — event `decolonization_events.13` — `country_prestige_add = country_prestige_add=5 (in colonial_stability_positive_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_positive_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1584` — event `decolonization_events.14` — `country_prestige_add = country_prestige_add=25 (in gp_anti_colonial_policy_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_anti_colonial_policy_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1649` — event `decolonization_events.14` — `country_prestige_add = country_prestige_add=10 (in gp_pro_colonial_policy_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_pro_colonial_policy_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1752` — event `decolonization_events.15` — `country_prestige_add = country_prestige_add=50 (in independence_nationalization_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `independence_nationalization_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1804` — event `decolonization_events.15` — `country_prestige_add = country_prestige_add=25 (in selective_nationalization_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `selective_nationalization_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:1851` — event `decolonization_events.15` — `country_prestige_add = country_prestige_add=-25 (in foreign_property_protected_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `foreign_property_protected_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2003` — event `decolonization_events.16` — `country_prestige_add = country_prestige_add=25 (in partition_territorial_claims_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `partition_territorial_claims_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2046` — event `decolonization_events.16` — `country_prestige_add = country_prestige_add=40 (in partition_accepted_borders_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `partition_accepted_borders_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2061` — event `decolonization_events.16` — `country_prestige_add = country_prestige_add=40 (in partition_accepted_borders_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `partition_accepted_borders_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2155` — event `decolonization_events.17` — `country_prestige_add = country_prestige_add=-25 (in border_dispute_active_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `border_dispute_active_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2179` — event `decolonization_events.17` — `country_prestige_add = country_prestige_add=25 (in border_dispute_resolved_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `border_dispute_resolved_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2199` — event `decolonization_events.17` — `country_prestige_add = country_prestige_add=25 (in border_dispute_resolved_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `border_dispute_resolved_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2284` — event `decolonization_events.18` — `country_prestige_add = country_prestige_add=-50 (in military_strongman_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `military_strongman_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2311` — event `decolonization_events.18` — `country_prestige_add = country_prestige_add=50 (in democratic_transition_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `democratic_transition_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2416` — event `decolonization_events.19` — `country_prestige_add = country_prestige_add=-150 (in gp_military_intervention_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_military_intervention_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2423` — event `decolonization_events.19` — `country_prestige_add = country_prestige_add=-75 (in gp_intervention_target_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_intervention_target_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2465` — event `decolonization_events.19` — `country_prestige_add = country_prestige_add=-50 (in gp_economic_sanctions_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_economic_sanctions_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2497` — event `decolonization_events.19` — `country_prestige_add = country_prestige_add=100 (in peaceful_decolonization_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `peaceful_decolonization_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2584` — event `decolonization_events.20` — `country_prestige_add = country_prestige_add=-100 (in gp_intervention_condemned_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_intervention_condemned_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2598` — event `decolonization_events.20` — `country_prestige_add = country_prestige_add=100 (in gp_moral_authority_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `gp_moral_authority_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2724` — event `decolonization_events.21` — `country_prestige_add = country_prestige_add=50 (in non_aligned_movement_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `non_aligned_movement_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2740` — event `decolonization_events.21` — `country_prestige_add = country_prestige_add=25 (in superpower_aligned_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `superpower_aligned_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2769` — event `decolonization_events.21` — `country_prestige_add = country_prestige_add=25 (in superpower_aligned_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `superpower_aligned_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2817` — event `decolonization_events.200` — `country_prestige_add = country_prestige_add=200 (in colonial_empire_solidified_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_empire_solidified_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2895` — event `decolonization_events.201` — `country_prestige_add = country_prestige_add=-200 (in colonial_empire_collapsed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_empire_collapsed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2932` — event `decolonization_events.202` — `country_prestige_add = country_prestige_add=200 (in colonial_empire_solidified_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_empire_solidified_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:2990` — event `decolonization_events.203` — `country_prestige_add = country_prestige_add=200 (in colonial_empire_solidified_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_empire_solidified_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:3083` — event `decolonization_events.205` — `country_prestige_add = country_prestige_add=200 (in colonial_empire_solidified_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_empire_solidified_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:3155` — event `decolonization_events.50` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:3170` — event `decolonization_events.50` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:3206` — event `decolonization_events.51` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:3220` — event `decolonization_events.51` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:3260` — event `decolonization_events.52` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/decolonization_events.txt:3270` — event `decolonization_events.52` — `country_prestige_add = country_prestige_add=-5 (in colonial_stability_negative_event)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `colonial_stability_negative_event` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:43` — event `environmental_events.1` — `country_prestige_add = country_prestige_add=10 (in pollution_regulations_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `pollution_regulations_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:71` — event `environmental_events.1` — `country_prestige_add = country_prestige_add=-10 (in industry_protected_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `industry_protected_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:149` — event `environmental_events.2` — `country_prestige_add = country_prestige_add=10 (in environmental_concessions_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `environmental_concessions_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:190` — event `environmental_events.2` — `country_prestige_add = country_prestige_add=5 (in greenwashing_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `greenwashing_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:263` — event `environmental_events.3` — `country_prestige_add = country_prestige_add=10 (in eco_reform_justification_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `eco_reform_justification_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:301` — event `environmental_events.3` — `country_prestige_add = country_prestige_add=-10 (in environmentalist_crackdown_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `environmentalist_crackdown_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:355` — event `environmental_events.4` — `country_prestige_add = country_prestige_add=15 (in oil_spill_cleanup_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `oil_spill_cleanup_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:382` — event `environmental_events.4` — `country_prestige_add = country_prestige_add=10 (in industry_accountability_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `industry_accountability_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:462` — event `environmental_events.5` — `country_prestige_add = country_prestige_add=25 (in environmental_pledge_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `environmental_pledge_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:486` — event `environmental_events.5` — `country_prestige_add = country_prestige_add=-15 (in environmental_sovereignty_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `environmental_sovereignty_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:504` — event `environmental_events.5` — `country_prestige_add = country_prestige_add=5 (in environmental_greenwash_intl_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `environmental_greenwash_intl_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:546` — event `environmental_events.100` — `country_prestige_add = country_prestige_add=-50 (in environmental_neglect_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `environmental_neglect_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmental_events.txt:591` — event `environmental_events.200` — `country_prestige_add = country_prestige_add=75 (in environmental_legislation_triumph_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `environmental_legislation_triumph_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:410` — event `environmentalism_events.6` — `country_prestige_add = country_prestige_add=50 (in climate_refugee_welcome_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `climate_refugee_welcome_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:428` — event `environmentalism_events.6` — `country_prestige_add = country_prestige_add=-25 (in climate_refugee_rejection_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `climate_refugee_rejection_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:840` — event `environmentalism_events.11` — `country_prestige_add = country_prestige_add=100 (in green_tech_boom_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `green_tech_boom_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:862` — event `environmentalism_events.11` — `country_prestige_add = country_prestige_add=50 (in green_tech_market_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `green_tech_market_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:1006` — event `environmentalism_events.13` — `country_prestige_add = country_prestige_add=200 (in climate_summit_leader_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `climate_summit_leader_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:1013` — event `environmentalism_events.13` — `country_prestige_add = country_prestige_add=200 (in climate_summit_leader_no_gw_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `climate_summit_leader_no_gw_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:1030` — event `environmentalism_events.13` — `country_prestige_add = country_prestige_add=50 (in climate_summit_pledge_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `climate_summit_pledge_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:1037` — event `environmentalism_events.13` — `country_prestige_add = country_prestige_add=50 (in climate_summit_pledge_no_gw_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `climate_summit_pledge_no_gw_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:1320` — event `environmentalism_events.16` — `country_prestige_add = country_prestige_add=-50 (in arctic_adaptation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `arctic_adaptation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:1595` — event `environmentalism_events.21` — `country_prestige_add = country_prestige_add=100 (in climate_victory_dividend_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `climate_victory_dividend_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/environmentalism_events.txt:1602` — event `environmentalism_events.21` — `country_prestige_add = country_prestige_add=100 (in climate_victory_dividend_no_gw_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `climate_victory_dividend_no_gw_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/extra_law_events.txt:3956` — event `extra_law_events.36` — `country_prestige_add = country_prestige_add=-50 (in radical_law_retreat)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `radical_law_retreat` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:44` — event `feminist_events.1` — `country_prestige_add = country_prestige_add=15 (in equal_pay_mandate_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `equal_pay_mandate_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:69` — event `feminist_events.1` — `country_prestige_add = country_prestige_add=-10 (in equal_pay_suppressed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `equal_pay_suppressed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:149` — event `feminist_events.2` — `country_prestige_add = country_prestige_add=10 (in glass_ceiling_broken_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `glass_ceiling_broken_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:245` — event `feminist_events.3` — `country_prestige_add = country_prestige_add=15 (in reproductive_rights_progressive_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `reproductive_rights_progressive_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:289` — event `feminist_events.3` — `country_prestige_add = country_prestige_add=-10 (in contraception_restricted_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `contraception_restricted_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:349` — event `feminist_events.4` — `country_prestige_add = country_prestige_add=15 (in women_combat_roles_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `women_combat_roles_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:431` — event `feminist_events.5` — `country_prestige_add = country_prestige_add=10 (in feminist_literature_funded_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `feminist_literature_funded_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:475` — event `feminist_events.5` — `country_prestige_add = country_prestige_add=-15 (in feminist_literature_banned_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `feminist_literature_banned_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:521` — event `feminist_events.100` — `country_prestige_add = country_prestige_add=-25 (in feminism_movement_crushed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `feminism_movement_crushed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/feminist_events.txt:590` — event `feminist_events.200` — `country_prestige_add = country_prestige_add=100 (in feminism_triumph_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `feminism_triumph_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:64` — event `international_relations_events.1` — `country_prestige_add = country_prestige_add=25 (in arms_race_escalation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `arms_race_escalation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:103` — event `international_relations_events.1` — `country_prestige_add = country_prestige_add=20 (in diplomatic_overture_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `diplomatic_overture_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:122` — event `international_relations_events.1` — `country_prestige_add = country_prestige_add=-50 (in diplomatic_overture_rebuffed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `diplomatic_overture_rebuffed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:229` — event `international_relations_events.2` — `country_prestige_add = country_prestige_add=-25 (in proxy_conflict_funding_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `proxy_conflict_funding_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:258` — event `international_relations_events.2` — `country_prestige_add = country_prestige_add=-10 (in proxy_conflict_avoided_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `proxy_conflict_avoided_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:450` — event `international_relations_events.4` — `country_prestige_add = country_prestige_add=25 (in propaganda_campaign_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `propaganda_campaign_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:476` — event `international_relations_events.4` — `country_prestige_add = country_prestige_add=-30 (in propaganda_ignored_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `propaganda_ignored_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:538` — event `international_relations_events.5` — `country_prestige_add = country_prestige_add=25 (in trade_embargo_imposed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `trade_embargo_imposed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:565` — event `international_relations_events.5` — `country_prestige_add = country_prestige_add=-15 (in trade_diplomacy_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `trade_diplomacy_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:632` — event `international_relations_events.6` — `country_prestige_add = country_prestige_add=25 (in propaganda_campaign_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `propaganda_campaign_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:651` — event `international_relations_events.6` — `country_prestige_add = country_prestige_add=-25 (in espionage_prisoner_exchange_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `espionage_prisoner_exchange_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:749` — event `international_relations_events.7` — `country_prestige_add = country_prestige_add=-50 (in space_race_redirected_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `space_race_redirected_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:836` — event `international_relations_events.8` — `country_prestige_add = country_prestige_add=-30 (in nuclear_deescalation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `nuclear_deescalation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:902` — event `international_relations_events.101` — `country_prestige_add = country_prestige_add=-25 (in proxy_conflict_destabilized_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `proxy_conflict_destabilized_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:931` — event `international_relations_events.102` — `country_prestige_add = country_prestige_add=10 (in trade_embargo_suffered_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `trade_embargo_suffered_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:960` — event `international_relations_events.103` — `country_prestige_add = country_prestige_add=-30 (in espionage_agent_exposed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `espionage_agent_exposed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:1066` — event `international_relations_events.104` — `country_prestige_add = country_prestige_add=15 (in nuclear_standoff_relief_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `nuclear_standoff_relief_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:1134` — event `international_relations_events.105` — `country_prestige_add = country_prestige_add=-10 (in arms_race_rival_pressure_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `arms_race_rival_pressure_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:1163` — event `international_relations_events.106` — `country_prestige_add = country_prestige_add=-20 (in counter_propaganda_backlash_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `counter_propaganda_backlash_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/international_relations_events.txt:1192` — event `international_relations_events.107` — `country_prestige_add = country_prestige_add=-30 (in espionage_agent_exposed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `espionage_agent_exposed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:44` — event `lgbtq_events.1` — `country_prestige_add = country_prestige_add=10 (in pride_march_momentum_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `pride_march_momentum_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:77` — event `lgbtq_events.1` — `country_prestige_add = country_prestige_add=-10 (in pride_march_suppressed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `pride_march_suppressed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:241` — event `lgbtq_events.3` — `country_prestige_add = country_prestige_add=15 (in lgbtq_military_inclusion_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `lgbtq_military_inclusion_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:284` — event `lgbtq_events.3` — `country_prestige_add = country_prestige_add=-10 (in lgbtq_military_ban_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `lgbtq_military_ban_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:339` — event `lgbtq_events.4` — `country_prestige_add = country_prestige_add=10 (in lgbtq_cultural_shift_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `lgbtq_cultural_shift_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:391` — event `lgbtq_events.4` — `country_prestige_add = country_prestige_add=-15 (in lgbtq_censorship_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `lgbtq_censorship_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:445` — event `lgbtq_events.5` — `country_prestige_add = country_prestige_add=10 (in hate_crime_investigation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `hate_crime_investigation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:470` — event `lgbtq_events.5` — `country_prestige_add = country_prestige_add=5 (in lgbtq_reform_leverage_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `lgbtq_reform_leverage_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:540` — event `lgbtq_events.100` — `country_prestige_add = country_prestige_add=-25 (in lgbtq_movement_crushed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `lgbtq_movement_crushed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/lgbtq_events.txt:597` — event `lgbtq_events.200` — `country_prestige_add = country_prestige_add=100 (in lgbtq_rights_triumph_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `lgbtq_rights_triumph_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/mental_health_events.txt:56` — event `mental_health_events.1` — `country_prestige_add = country_prestige_add=5 (in resilience_programs_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `resilience_programs_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/mental_health_events.txt:115` — event `mental_health_events.2` — `country_prestige_add = country_prestige_add=10 (in youth_mental_health_funding_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `youth_mental_health_funding_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/mental_health_events.txt:132` — event `mental_health_events.2` — `country_prestige_add = country_prestige_add=5 (in social_media_regulation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `social_media_regulation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/mental_health_events.txt:288` — event `mental_health_events.4` — `country_prestige_add = country_prestige_add=10 (in veteran_mental_health_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `veteran_mental_health_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/mental_health_events.txt:372` — event `mental_health_events.5` — `country_prestige_add = country_prestige_add=10 (in institutional_reform_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `institutional_reform_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/mental_health_events.txt:393` — event `mental_health_events.5` — `country_prestige_add = country_prestige_add=5 (in institutional_oversight_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `institutional_oversight_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/mental_health_events.txt:455` — event `mental_health_events.100` — `country_prestige_add = country_prestige_add=-25 (in mental_health_crisis_ignored_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `mental_health_crisis_ignored_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/mental_health_events.txt:504` — event `mental_health_events.200` — `country_prestige_add = country_prestige_add=50 (in mental_health_infrastructure_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `mental_health_infrastructure_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/minor_events.txt:232` — event `minor_events_timelineextended.3` — `country_prestige_add = country_prestige_add=-25 (in foreign_investment_dependency_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `foreign_investment_dependency_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/modern_election_events.txt:146` — event `modern_election_events.2` — `country_prestige_add = country_prestige_add=25 (in me_deepfake_exposed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `me_deepfake_exposed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/modern_election_events.txt:223` — event `modern_election_events.3` — `country_prestige_add = country_prestige_add=50 (in me_climate_pledge_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `me_climate_pledge_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/modern_election_events.txt:506` — event `modern_election_events.6` — `country_prestige_add = country_prestige_add=-25 (in me_foreign_interference_investigation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `me_foreign_interference_investigation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/modern_election_events.txt:812` — event `modern_election_events.9` — `country_prestige_add = country_prestige_add=10 (in me_media_savvy_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `me_media_savvy_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/modern_election_events.txt:1375` — event `modern_election_events.16` — `country_prestige_add = country_prestige_add=25 (in me_equal_rights_platform_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `me_equal_rights_platform_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/modern_election_events.txt:1466` — event `modern_election_events.17` — `country_prestige_add = country_prestige_add=50 (in me_space_program_pledge_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `me_space_program_pledge_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/modern_election_events.txt:1834` — event `modern_election_events.21` — `country_prestige_add = country_prestige_add=25 (in me_pollution_cleanup_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `me_pollution_cleanup_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/modern_election_events.txt:2786` — event `modern_election_events.32` — `country_prestige_add = country_prestige_add=25 (in me_colonial_representation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `me_colonial_representation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:96` — event `movement_events_te.1` — `country_prestige_add = country_prestige_add=-25 (in civil_rights_suppression_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `civil_rights_suppression_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:277` — event `movement_events_te.3` — `country_prestige_add = country_prestige_add=25 (in cultural_renaissance_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_renaissance_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:311` — event `movement_events_te.3` — `country_prestige_add = country_prestige_add=50 (in cultural_cooptation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_cooptation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:518` — event `movement_events_te.13` — `country_prestige_add = country_prestige_add=-25 (in refugee_network_crackdown_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `refugee_network_crackdown_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:723` — event `movement_events_te.15` — `country_prestige_add = country_prestige_add=25 (in civil_rights_martyrdom_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `civil_rights_martyrdom_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:745` — event `movement_events_te.15` — `country_prestige_add = country_prestige_add=-25 (in civil_rights_suppression_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `civil_rights_suppression_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:767` — event `movement_events_te.15` — `country_prestige_add = country_prestige_add=10 (in civil_rights_investigation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `civil_rights_investigation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:832` — event `movement_events_te.16` — `country_prestige_add = country_prestige_add=-25 (in civil_disobedience_crackdown_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `civil_disobedience_crackdown_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:908` — event `movement_events_te.100` — `country_prestige_add = country_prestige_add=-50 (in movement_crushed_order_restored_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `movement_crushed_order_restored_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:926` — event `movement_events_te.100` — `country_prestige_add = country_prestige_add=-25 (in movement_crushed_assimilation_drive_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `movement_crushed_assimilation_drive_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:965` — event `movement_events_te.100` — `country_prestige_add = country_prestige_add=-25 (in movement_crushed_political_suppression_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `movement_crushed_political_suppression_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:983` — event `movement_events_te.100` — `country_prestige_add = country_prestige_add=-75 (in movement_crushed_expulsion_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `movement_crushed_expulsion_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:1164` — event `movement_events_te.6` — `country_prestige_add = country_prestige_add=50 (in veterans_peace_testimony_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `veterans_peace_testimony_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:1267` — event `movement_events_te.7` — `country_prestige_add = country_prestige_add=-25 (in peace_rally_suppressed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `peace_rally_suppressed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:1353` — event `movement_events_te.8` — `country_prestige_add = country_prestige_add=25 (in war_profiteering_investigation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `war_profiteering_investigation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:1764` — event `movement_events_te.12` — `country_prestige_add = country_prestige_add=100 (in digital_rights_recognition_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `digital_rights_recognition_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/movement_events_te.txt:1804` — event `movement_events_te.200` — `country_prestige_add = country_prestige_add=100 (in civil_rights_triumph_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `civil_rights_triumph_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/nuclear_weapon_events.txt:487` — event `nuclear_weapon_events.10` — `country_prestige_add = country_prestige_add=500 (in nuclear_power)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `nuclear_power` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/nuclear_weapon_events.txt:500` — event `nuclear_weapon_events.10` — `country_prestige_add = country_prestige_add=500 (in nuclear_power)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `nuclear_power` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/nuclear_weapon_events.txt:817` — event `nuclear_weapon_events.17` — `country_prestige_add = 100` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier>
- `events/nuclear_weapon_events.txt:840` — event `nuclear_weapon_events.17` — `country_prestige_add = 50` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier>
- `events/nuclear_weapon_events.txt:856` — event `nuclear_weapon_events.17` — `country_prestige_add = 200` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier>
- `events/nuclear_weapon_events.txt:909` — event `nuclear_weapon_events.18` — `country_prestige_add = 10` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier>
- `events/nuclear_weapon_events.txt:979` — event `nuclear_weapon_events.18` — `country_prestige_add = 5` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier>
- `events/nuclear_weapon_events.txt:1096` — event `nuclear_weapon_events.19` — `country_prestige_add = 15` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier>
- `events/nuclear_weapon_events.txt:1167` — event `nuclear_weapon_events.20` — `country_prestige_add = 10` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier>
- `events/nuclear_weapon_events.txt:1263` — event `nuclear_weapon_events.21` — `country_prestige_add = -20` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier>
- `events/post_scarcity_events.txt:113` — event `post_scarcity_events.2` — `country_prestige_add = country_prestige_add=15 (in cultural_renaissance_funding_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_renaissance_funding_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/post_scarcity_events.txt:130` — event `post_scarcity_events.2` — `country_prestige_add = country_prestige_add=-10 (in mandatory_work_programs_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `mandatory_work_programs_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/post_scarcity_events.txt:190` — event `post_scarcity_events.3` — `country_prestige_add = country_prestige_add=10 (in ai_bureaucracy_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ai_bureaucracy_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/post_scarcity_events.txt:207` — event `post_scarcity_events.3` — `country_prestige_add = country_prestige_add=5 (in ai_assisted_governance_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ai_assisted_governance_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/post_scarcity_events.txt:344` — event `post_scarcity_events.5` — `country_prestige_add = country_prestige_add=15 (in art_renaissance_funded_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `art_renaissance_funded_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/post_scarcity_events.txt:365` — event `post_scarcity_events.5` — `country_prestige_add = country_prestige_add=10 (in organic_creativity_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `organic_creativity_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/post_scarcity_events.txt:423` — event `post_scarcity_events.200` — `country_prestige_add = country_prestige_add=200 (in post_scarcity_achieved_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `post_scarcity_achieved_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/religious_revival_events.txt:367` — event `religious_revival_events.3` — `country_prestige_add = country_prestige_add=25 (in rre_progressive_faith_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `rre_progressive_faith_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/religious_revival_events.txt:479` — event `religious_revival_events.4` — `country_prestige_add = country_prestige_add=-50 (in rre_religious_nationalism_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `rre_religious_nationalism_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/religious_revival_events.txt:498` — event `religious_revival_events.4` — `country_prestige_add = country_prestige_add=25 (in rre_civic_faith_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `rre_civic_faith_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/secular_events.txt:39` — event `secular_events.1` — `country_prestige_add = country_prestige_add=10 (in religious_institution_investigation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `religious_institution_investigation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/secular_events.txt:72` — event `secular_events.1` — `country_prestige_add = country_prestige_add=5 (in secular_momentum_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `secular_momentum_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/secular_events.txt:210` — event `secular_events.3` — `country_prestige_add = country_prestige_add=10 (in spiritual_diversity_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `spiritual_diversity_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/secular_events.txt:296` — event `secular_events.4` — `country_prestige_add = country_prestige_add=10 (in disestablishment_push_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `disestablishment_push_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/secular_events.txt:398` — event `secular_events.5` — `country_prestige_add = country_prestige_add=10 (in interfaith_dialogue_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `interfaith_dialogue_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/secular_events.txt:435` — event `secular_events.5` — `country_prestige_add = country_prestige_add=5 (in secular_agenda_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `secular_agenda_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/secular_events.txt:494` — event `secular_events.100` — `country_prestige_add = country_prestige_add=-15 (in religious_revivalism_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `religious_revivalism_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/secular_events.txt:547` — event `secular_events.200` — `country_prestige_add = country_prestige_add=50 (in secular_transition_complete_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `secular_transition_complete_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:78` — event `social_tensions_events.1` — `country_prestige_add = country_prestige_add=50 (in terror_resilience_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `terror_resilience_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:107` — event `social_tensions_events.1` — `country_prestige_add = country_prestige_add=50 (in terror_preempted_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `terror_preempted_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:158` — event `social_tensions_events.2` — `country_prestige_add = country_prestige_add=25 (in foreign_terror_retaliation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `foreign_terror_retaliation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:196` — event `social_tensions_events.2` — `country_prestige_add = country_prestige_add=50 (in terror_resilience_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `terror_resilience_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:274` — event `social_tensions_events.3` — `country_prestige_add = country_prestige_add=-55 (in corporate_lobbying_exposed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `corporate_lobbying_exposed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:342` — event `social_tensions_events.4` — `country_prestige_add = country_prestige_add=25 (in monopoly_broken_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `monopoly_broken_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:634` — event `social_tensions_events.7` — `country_prestige_add = country_prestige_add=-50 (in war_crime_coverup_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `war_crime_coverup_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:642` — event `social_tensions_events.7` — `country_prestige_add = country_prestige_add=-150 (in war_crime_scandal_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `war_crime_scandal_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:654` — event `social_tensions_events.7` — `country_prestige_add = country_prestige_add=50 (in war_crime_tribunal_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `war_crime_tribunal_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:675` — event `social_tensions_events.7` — `country_prestige_add = country_prestige_add=75 (in war_crime_reform_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `war_crime_reform_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:724` — event `social_tensions_events.8` — `country_prestige_add = country_prestige_add=75 (in enemy_war_crime_tribunal_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `enemy_war_crime_tribunal_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:1047` — event `social_tensions_events.12` — `country_prestige_add = country_prestige_add=25 (in nativist_countered_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `nativist_countered_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:1357` — event `social_tensions_events.16` — `country_prestige_add = country_prestige_add=25 (in cultural_openness_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_openness_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/social_tensions_events.txt:1389` — event `social_tensions_events.16` — `country_prestige_add = country_prestige_add=25 (in cultural_synthesis_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_synthesis_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:43` — event `surveillance_events.1` — `country_prestige_add = country_prestige_add=15 (in whistleblower_reform_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `whistleblower_reform_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:60` — event `surveillance_events.1` — `country_prestige_add = country_prestige_add=-15 (in whistleblower_prosecuted_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `whistleblower_prosecuted_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:121` — event `surveillance_events.2` — `country_prestige_add = country_prestige_add=10 (in data_protection_strengthened_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `data_protection_strengthened_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:138` — event `surveillance_events.2` — `country_prestige_add = country_prestige_add=-10 (in surveillance_expanded_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `surveillance_expanded_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:200` — event `surveillance_events.3` — `country_prestige_add = country_prestige_add=5 (in hacker_collaboration_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `hacker_collaboration_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:244` — event `surveillance_events.3` — `country_prestige_add = country_prestige_add=-20 (in surveillance_massive_expansion_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `surveillance_massive_expansion_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:328` — event `surveillance_events.4` — `country_prestige_add = country_prestige_add=15 (in ai_policing_banned_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ai_policing_banned_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:379` — event `surveillance_events.5` — `country_prestige_add = country_prestige_add=10 (in cyber_defense_strengthened_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cyber_defense_strengthened_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:396` — event `surveillance_events.5` — `country_prestige_add = country_prestige_add=5 (in counter_espionage_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `counter_espionage_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:413` — event `surveillance_events.5` — `country_prestige_add = country_prestige_add=-15 (in foreign_threat_surveillance_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `foreign_threat_surveillance_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:455` — event `surveillance_events.100` — `country_prestige_add = country_prestige_add=-50 (in security_state_established_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `security_state_established_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/surveillance_events.txt:500` — event `surveillance_events.200` — `country_prestige_add = country_prestige_add=75 (in digital_rights_triumph_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `digital_rights_triumph_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:80` — event `treaty_article_events.1` — `country_prestige_add = country_prestige_add=10 (in intelligence_tip_off_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `intelligence_tip_off_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:95` — event `treaty_article_events.1` — `country_prestige_add = country_prestige_add=10 (in intelligence_tip_off_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `intelligence_tip_off_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:117` — event `treaty_article_events.1` — `country_prestige_add = country_prestige_add=50 (in intelligence_victory_showcase_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `intelligence_victory_showcase_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:122` — event `treaty_article_events.1` — `country_prestige_add = country_prestige_add=10 (in intelligence_tip_off_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `intelligence_tip_off_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:214` — event `treaty_article_events.2` — `country_prestige_add = country_prestige_add=-10 (in intelligence_failure_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `intelligence_failure_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:229` — event `treaty_article_events.2` — `country_prestige_add = country_prestige_add=-10 (in intelligence_failure_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `intelligence_failure_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:243` — event `treaty_article_events.2` — `country_prestige_add = country_prestige_add=-10 (in intelligence_failure_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `intelligence_failure_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:307` — event `treaty_article_events.3` — `country_prestige_add = country_prestige_add=15 (in cultural_renaissance_prestige_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_renaissance_prestige_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:312` — event `treaty_article_events.3` — `country_prestige_add = country_prestige_add=15 (in cultural_flowering_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_flowering_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:323` — event `treaty_article_events.3` — `country_prestige_add = country_prestige_add=15 (in cultural_flowering_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_flowering_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:405` — event `treaty_article_events.4` — `country_prestige_add = country_prestige_add=-10 (in nationalist_appeasement_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `nationalist_appeasement_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:451` — event `treaty_article_events.5` — `country_prestige_add = country_prestige_add=-5 (in military_disarmament_anger_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `military_disarmament_anger_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:489` — event `treaty_article_events.5` — `country_prestige_add = country_prestige_add=-5 (in military_disarmament_anger_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `military_disarmament_anger_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:579` — event `treaty_article_events.6` — `country_prestige_add = country_prestige_add=25 (in military_doctrine_prestige_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `military_doctrine_prestige_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:760` — event `treaty_article_events.9` — `country_prestige_add = country_prestige_add=-10 (in security_dependency_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `security_dependency_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:820` — event `treaty_article_events.10` — `country_prestige_add = country_prestige_add=-5 (in population_transfer_aftermath_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `population_transfer_aftermath_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:850` — event `treaty_article_events.10` — `country_prestige_add = country_prestige_add=-25 (in nation_building_controversy_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `nation_building_controversy_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:935` — event `treaty_article_events.11` — `country_prestige_add = country_prestige_add=15 (in cultural_exchange_soft_power_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_exchange_soft_power_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/treaty_article_events.txt:1031` — event `treaty_article_events.12` — `country_prestige_add = country_prestige_add=-10 (in cultural_exchange_backlash_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `cultural_exchange_backlash_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:44` — event `wonder_events.1` — `country_prestige_add = country_prestige_add=50 (in we_se_milestone_celebrate)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_se_milestone_celebrate` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:93` — event `wonder_events.2` — `country_prestige_add = country_prestige_add=-25 (in we_se_structural_halt)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_se_structural_halt` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:103` — event `wonder_events.2` — `country_prestige_add = country_prestige_add=25 (in we_se_structural_reinforce)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_se_structural_reinforce` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:143` — event `wonder_events.3` — `country_prestige_add = country_prestige_add=50 (in we_se_gateway_open)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_se_gateway_open` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:200` — event `wonder_events.4` — `country_prestige_add = country_prestige_add=25 (in we_se_trade_auction)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_se_trade_auction` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:360` — event `wonder_events.7` — `country_prestige_add = country_prestige_add=75 (in we_se_sovereignty)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_se_sovereignty` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:372` — event `wonder_events.7` — `country_prestige_add = country_prestige_add=25 (in we_se_shared_access)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_se_shared_access` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:415` — event `wonder_events.10` — `country_prestige_add = country_prestige_add=50 (in we_sc_first_light_pride)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_sc_first_light_pride` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:464` — event `wonder_events.11` — `country_prestige_add = country_prestige_add=-25 (in we_sc_beam_compensate)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_sc_beam_compensate` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:475` — event `wonder_events.11` — `country_prestige_add = country_prestige_add=-25 (in we_sc_beam_minimize)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_sc_beam_minimize` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:526` — event `wonder_events.12` — `country_prestige_add = country_prestige_add=25 (in we_sc_surplus_export)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_sc_surplus_export` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:627` — event `wonder_events.14` — `country_prestige_add = country_prestige_add=25 (in we_sc_storm_rideout)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_sc_storm_rideout` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:674` — event `wonder_events.15` — `country_prestige_add = country_prestige_add=75 (in we_sc_monopoly_assert)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_sc_monopoly_assert` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:729` — event `wonder_events.20` — `country_prestige_add = country_prestige_add=50 (in we_ob_highground_accelerate)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_highground_accelerate` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:833` — event `wonder_events.22` — `country_prestige_add = country_prestige_add=75 (in we_ob_deterrence_strength)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_deterrence_strength` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:844` — event `wonder_events.22` — `country_prestige_add = country_prestige_add=25 (in we_ob_deterrence_negotiate)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_deterrence_negotiate` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:884` — event `wonder_events.23` — `country_prestige_add = country_prestige_add=-50 (in we_ob_weapons_test_relief)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_weapons_test_relief` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:901` — event `wonder_events.23` — `country_prestige_add = country_prestige_add=-25 (in we_ob_weapons_test_classify)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_weapons_test_classify` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:958` — event `wonder_events.24` — `country_prestige_add = country_prestige_add=50 (in we_ob_arms_race_treaty)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_arms_race_treaty` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:998` — event `wonder_events.25` — `country_prestige_add = country_prestige_add=50 (in we_ob_strike_project)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_strike_project` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:1010` — event `wonder_events.25` — `country_prestige_add = country_prestige_add=25 (in we_ob_strike_defensive)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_strike_defensive` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:1051` — event `wonder_events.26` — `country_prestige_add = country_prestige_add=150 (in we_ob_shield)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_ob_shield` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:1256` — event `wonder_events.33` — `country_prestige_add = country_prestige_add=75 (in we_am_diplomacy_arsenal)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_am_diplomacy_arsenal` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:1268` — event `wonder_events.33` — `country_prestige_add = country_prestige_add=25 (in we_am_diplomacy_olive)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_am_diplomacy_olive` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:1308` — event `wonder_events.34` — `country_prestige_add = country_prestige_add=-25 (in we_am_incident_transparent)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_am_incident_transparent` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/wonder_events.txt:1324` — event `wonder_events.34` — `country_prestige_add = country_prestige_add=-50 (in we_am_incident_suppress)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `we_am_incident_suppress` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:842` — event `world_war_events.20` — `country_prestige_add = country_prestige_add=200 (in ww_arsenal_of_democracy_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_arsenal_of_democracy_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:869` — event `world_war_events.20` — `country_prestige_add = country_prestige_add=100 (in ww_lend_lease_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_lend_lease_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1036` — event `world_war_events.100` — `country_prestige_add = country_prestige_add=500 (in ww_victors_peace_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_victors_peace_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1041` — event `world_war_events.100` — `country_prestige_add = country_prestige_add=50 (in ww_occupation_duties_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_occupation_duties_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1072` — event `world_war_events.100` — `country_prestige_add = country_prestige_add=500 (in ww_victors_peace_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_victors_peace_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1109` — event `world_war_events.100` — `country_prestige_add = country_prestige_add=500 (in ww_victors_peace_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_victors_peace_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1114` — event `world_war_events.100` — `country_prestige_add = country_prestige_add=100 (in ww_liberation_hero_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_liberation_hero_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1155` — event `world_war_events.101` — `country_prestige_add = country_prestige_add=-500 (in ww_defeated_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_defeated_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1189` — event `world_war_events.101` — `country_prestige_add = country_prestige_add=-500 (in ww_defeated_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_defeated_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1239` — event `world_war_events.102` — `country_prestige_add = country_prestige_add=-500 (in ww_defeated_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_defeated_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1269` — event `world_war_events.102` — `country_prestige_add = country_prestige_add=-500 (in ww_defeated_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_defeated_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1333` — event `world_war_events.103` — `country_prestige_add = country_prestige_add=100 (in ww_liberation_hero_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_liberation_hero_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1375` — event `world_war_events.103` — `country_prestige_add = country_prestige_add=-300 (in ww_war_crimes_exposed_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_war_crimes_exposed_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1424` — event `world_war_events.104` — `country_prestige_add = country_prestige_add=500 (in ww_victors_peace_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_victors_peace_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1446` — event `world_war_events.104` — `country_prestige_add = country_prestige_add=100 (in ww_liberation_hero_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `ww_liberation_hero_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.
- `events/world_war_events.txt:1531` — event `world_war_events.105` — `country_prestige_add = country_prestige_add=25 (in arms_race_escalation_modifier)` — fix: use prestige_loss_<tier> / prestige_gain_<tier> static modifier (mult-based, no multiplier needed), OR multiplier = sv_prestige_event_<tier> — `arms_race_escalation_modifier` carries a hardcoded fast-scaling field; either replace the reference with a mult-based generic (prestige_loss_<tier> / bureaucracy_loss_<tier>), or convert the static modifier itself to use _mult.

## Reviewed Exemptions

_None._

## Coverage

- files_audited: 36
- total flags: 309
- unreviewed: 309
- exempted: 0
