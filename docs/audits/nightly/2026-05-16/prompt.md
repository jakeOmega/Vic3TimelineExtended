# Nightly mod audit — 2026-05-16

Generated by `scripts/nightly_audit_select.py`. Read `docs/audits/nightly_audit_README.md` first if you haven't.

## What you're doing

Audit the slice of mod content below for things the procedural audits *don't* catch:
internal logic, mod-convention adherence, balance/coherence vs peers, localization tone.
File findings as GitHub issues; fix what's trivially fixable as PRs.

## Pre-audit reading (in order)

- `CLAUDE.md`
- `docs/guides/scripting_best_practices.md`
- `docs/vanilla/CLAUDE.md`
- `docs/audits/open_issues.md`
- `docs/vanilla/vanilla_known_bugs.md`
- `docs/vanilla/vanilla_war_reference.md`
- `docs/audits/script_parameterization_audit.md`
- `docs/audits/nightly_checklists/production_methods_and_buildings.md`
- `docs/audits/nightly_checklists/scripted_effects_and_triggers.md`

Also skim `gh issue list --search nightly-audit --state open --limit 30` and
`gh issue list --state open --limit 30` to avoid duplicate filings.

## Targets

| # | File | Lines | Area | Last audited | Count | Recent findings |
|---|---|---|---|---|---|---|
| 1 | `common/mobilization_options/extra_mobilization_options.txt` | 1–600 (600) | production_methods_and_buildings | never | 0 | 0 |
| 2 | `common/mobilization_option_groups/extra_mobilization_groups.txt` | all (4) | production_methods_and_buildings | never | 0 | 0 |
| 3 | `common/_meta/duplicate_image_allowlist.yml` | 1–600 (600) | scripted_effects_and_triggers | never | 0 | 0 |
| 4 | `common/battle_conditions/extra_battle_conditions.txt` | all (296) | production_methods_and_buildings | never | 0 | 0 |
| 5 | `common/_meta/modifier_patterns.yml` | all (240) | scripted_effects_and_triggers | never | 0 | 0 |
| 6 | `common/ship_modifications/extra_utility_modifications.txt` | all (202) | production_methods_and_buildings | never | 0 | 0 |
| 7 | `common/combat_unit_groups/extra_combat_unit_groups.txt` | all (13) | production_methods_and_buildings | never | 0 | 0 |
| 8 | `common/mobilization_options/_mobilization_options.info` | all (87) | production_methods_and_buildings | never | 0 | 0 |
| 9 | `common/scripted_triggers/irredentism_triggers.txt` | all (205) | scripted_effects_and_triggers | never | 0 | 0 |
| 10 | `common/on_actions/social_tensions_on_actions.txt` | all (29) | scripted_effects_and_triggers | never | 0 | 0 |
| 11 | `common/on_actions/modern_election_on_actions.txt` | all (50) | scripted_effects_and_triggers | never | 0 | 0 |
| 12 | `common/script_values/te_construction_market_sector_placement_values.txt` | all (100) | scripted_effects_and_triggers | never | 0 | 0 |
| 13 | `common/script_values/te_map_mode_script_values.txt` | all (39) | scripted_effects_and_triggers | never | 0 | 0 |
| 14 | `common/scripted_triggers/resource_discovery_triggers.txt` | all (6) | scripted_effects_and_triggers | never | 0 | 0 |
| 15 | `common/on_actions/legacy_modifier_cleanup.txt` | all (23) | scripted_effects_and_triggers | never | 0 | 0 |

**Budget**: 2494 of 2500 lines, 15 of 15 files.

## Registry drift

`docs/auto_generated_files.md` and `EXCLUDED_GLOBS` (in `scripts/nightly_audit_select.py`) have drifted.
Investigate as part of this audit; if action is needed, update `EXCLUDED_GLOBS` or `INTENTIONALLY_NOT_EXCLUDED` in the script accordingly.

**In registry but not excluded** (newly generator-managed files? — add to `EXCLUDED_GLOBS` or `INTENTIONALLY_NOT_EXCLUDED`):

- `common/company_types/*.txt`

## Focus ranking

Prioritize, in this order:

1. **Internal logic** — triggers fire when intended, effects do what's described, no orphan references, scope chains valid, modifier durations in correct units (days vs months — note `*_modifier_time` constants are days), state-scoped scaling runs from `on_yearly_pulse_state`.
2. **Mod convention adherence** — patterns documented in `CLAUDE.md` § Building blocks and `docs/guides/scripting_best_practices.md`. **If you see repeated convention violations across files, flag it as a question for the user** rather than fixing each instance — the convention may need to broaden.
3. **Balance / design coherence** — within-system consistency, ludo-narrative consistency (e.g. becoming an exploited dependency shouldn't raise SoL), modifier magnitudes out of whack vs peers.
4. **Localization narrative / style** — tone consistency (`event_creation_guide.md` § General Tone), mechanical-narrative alignment.

Explicitly **deprioritized** (don't actively search — these are covered by procedural audits running on `/reload`):

- Syntax / parser correctness (the engine and `paradox_file_parser.py` flag these)
- Loc completeness (`loc_coverage_audit`)
- Modifier visibility (`modifier_visibility_audit`)
- Magnitude-based event fast-scaling (`event_magnitude_audit`)
- `kill_character` guards (`kill_character_audit`)
- Concept link presence (`concept_reference_audit`)
- Localization accessor chain parse-validity (`localization_accessor_audit`)
- Performance hazards (flag obvious ones only)

## Bar for filing

Every finding must include:

- A specific `file:line` reference.
- A concrete proposed fix **or** a specific question to the user.
- Reasoning tied to a doc, a convention, or a vanilla mechanic (cite the source).

"This section feels underspecified" without a concrete proposal does not get filed.

## Auto-fix rule

> If fixing it is no more effort than filing an issue, fix it directly via PR. Otherwise, file an issue.

Judgment-based, not category-based. Calibrating examples:

- Typo in a loc string → fix.
- Missing `# REVIEWED YYYY-MM-DD` on a known-intentional suppression → fix.
- Two effects firing in the wrong order per a clear doc rule → fix.
- A modifier that's wrong by 30× because months were used in place of days → fix.
- A whole system of events whose IG approvals don't match the politics reference → **issue** (the system may need re-thinking, not 12 patches).
- Anything where the fix depends on understanding designer intent → **issue**.

**Cap**: 5 files / 50 lines changed per night across all auto-fix PRs. If more would fit, fix some and file an issue listing the rest.

**Branch + PR convention**:

- Branch: `nightly-audit/YYYY-MM-DD-<short-slug>` from `main`.
- PR title: `nightly-audit(YYYY-MM-DD): <short description>`.
- PR body: cite the auto-fix rule, link to this night's `docs/audits/nightly/<date>/` directory, list the rule each fix passes.

## Dedup workflow

Before filing any issue:

1. `gh issue list --search "<file_path>" --state open --limit 30`
2. Skim `docs/audits/open_issues.md` for matching entries.
3. If a match exists with roughly the same finding: **comment on the existing issue, don't file a new one**.

## Wrap-up

After all targets are audited:

1. Update `docs/audits/.nightly_coverage.json` for each target: bump `audit_count`, set `last_audited` to `2026-05-16`, set `recent_findings` to the count this run produced for that file. For sliced files, append the slice's `[start, end]` to `partial_coverage`. Decay any *other* file's `recent_findings` by half if its `last_audited` is older than 90 days.
2. **State bump rides in a PR. Never direct-push to `main`.**
   - If there are auto-fix PRs this night: include the `.nightly_coverage.json` diff in the **first** auto-fix PR (or a dedicated coordinating PR — one PR per night carries the state diff).
   - If there are no auto-fix PRs: open a state-bump-only PR titled `nightly-audit(2026-05-16): state bump` from branch `nightly-audit/2026-05-16-state-bump`. Enable auto-merge if available; otherwise leave open for manual merge.

## Failure handling

If you cannot complete the audit for any reason — context exhaustion, tool failure, unexpected error — **before exiting**:

1. Open a GitHub issue with labels `priority:critical` and `nightly-audit:failure`. Title: `nightly-audit failure (<date>): <short reason>`. Body: what failed, at which step (selector run / pre-reading / per-target audit / wrap-up), and any partial findings.
2. **Do not update the state file.** Leaving it untouched ensures the next run re-selects the same targets.
3. Do not open a state-bump-only PR.

## What NOT to do

- Don't reimplement what `modifier_visibility_audit`, `event_magnitude_audit`, `kill_character_audit`, `loc_coverage_audit`, `concept_reference_audit`, `localization_accessor_audit`, `mod_structure_audit` already do.
- Don't auto-fix anything that touches design intent.
- Don't audit auto-generated files (the selector already excluded them).
- Don't audit the Python tooling (out of scope, has its own unittest coverage). Exception: file an issue if a generator visibly breaks while you're working.
- Don't push directly to `main`.

