# Event Image Report

Visible mod events that declare no `event_image` and no alternate art (`gui_window` / `left_icon` / `right_icon`). The default event window has no fallback art, so these render the engine's magenta missing-texture placeholder — silently, with no log line of any kind. Vanilla has exactly one such event across the whole base game (`test.120`).

- Events defined: **784**
- Visible (not `hidden = yes`): **770**
- Visible via alternate art (`gui_window` / icons): **0**
- Imageless (unreviewed): **0**
- Imageless (REVIEWED-suppressed): **0**

No unreviewed imageless events. ✅
