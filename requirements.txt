# Python dependencies for the Vic3TimelineExtended mod tooling.
# Install with: pip install -r requirements.txt
#
# Core (mod state server, parsers, generators):
requests
pyyaml
# regex: used by ig_feminism.py (auto-run on server reload). Install or the
# server will log "[post-load] ig_feminism FAILED" and continue without it.
regex
# numpy: used by pop_needs_curves.py for power-law extrapolation in the
# auto-run buy_packages regenerator. matplotlib is only needed for --plot.
numpy

# Image generation (gen_image.py); optional for headless / non-GPU setups.
# Comment out on machines without CUDA or a HuggingFace account.
# torch
# torchvision
# torchaudio
# diffusers
# transformers
# accelerate
# sentencepiece
# pillow
# huggingface_hub

# Dev / CI:
# ruff: lint gate for the Python tooling (config in ruff.toml, pyflakes "F"
# rules only). Run `ruff check .` before pushing — the "Lint (ruff, pyflakes
# rules)" step in .github/workflows/ci.yml runs that exact command. The range is
# loose here so a dev machine can track releases; CI installs one pinned version
# (see that step's comment) so a ruff release can't redden an unrelated commit.
ruff>=0.5,<1
