---
description: Triage Victoria 3 game logs (debug.log first, then error.log) in this mod repo. Read logs via the mod_state_server /logs API, categorize issues, distinguish mod from vanilla problems, fix mod-related ones, and append non-obvious lessons learned. Use whenever investigating crashes, in-game bugs, missing content, modifier no-ops, journal entries that don't surface, or after a game session ends.
---

# Log triage skill

## When to use

- User reports an in-game bug, crash, or modded content not appearing.
- A modifier, journal entry, decree, or event doesn't behave as expected.
- After re-launching the game with mod changes — diff the new debug.log against the prior session.
- User asks "what's broken in the logs?" or "did my edit break anything?".

## Pre-flight

```bash
curl -s http://localhost:8950/status
```

If that fails, the mod state server isn't running. Start it yourself with `.venv/bin/python mod_state_server.py` via Bash `run_in_background: true` (use the venv Python — system `python3` is missing the `regex` package). Warmup is ~60–110s; poll `curl -s http://localhost:8950/status` until it returns before continuing. Don't fall back to grepping log files by hand; the categorization, mod-only filtering, and dedupe live in the server.

After mod-file edits: `curl -X POST http://localhost:8950/reload`. After only re-launching the game with no mod-file changes: `curl -X POST 'http://localhost:8950/reload?engine_only=true'` (skips the slow ModState rebuild).

**`/logs/*` reads runtime logs, not parse-time output.** A fix that resolves a `script_parse_error` won't disappear from `/logs/debug` until the **game is re-launched** and writes a new `debug.log`. `/validate/engine-coverage` reports go clean immediately on `POST /reload`, but log entries are stale until the user runs the game. Don't chase phantom failures by re-fixing what's already fixed — confirm via `/validate/engine-coverage` (immediate) and `/logs/debug/diff?against=1` (after next launch).

## Canonical first command

Always start with `debug.log`, never `error.log` — Vic3's `error.log` only carries a small subset of engine diagnostics (mostly script-value type errors); the actionable signal lives in `debug.log`.

```bash
curl -s "http://localhost:8950/logs/debug?summary=false&dedupe=true&mod_only=unknown&vanilla_bugs=hide&mod_noise=hide" | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'Total: {len(d[\"entries\"])}'); [print(f'{e[\"category\"]} | {e[\"source\"]} | {(e[\"files\"] or [\"?\"])[0]} | {e[\"message\"][:200]}') for e in d['entries']]"
```

Output: a `Total: N` line, then one line per deduped entry — `category | cpp_source:line | first-referenced-file | message-truncated-to-200-chars`.

Three things this command does that matter:

- **`vanilla_bugs=hide` and `mod_noise=hide`** apply the parsed registries (`docs/vanilla/vanilla_known_bugs.md`, `docs/audits/mod_known_noise.md`), so previously-categorized vanilla bugs and tracked low-priority mod noise drop out of the first view. The per-entry mod-vs-vanilla decision-tree should focus on genuinely-new entries — if you're re-discovering something the registry already covers, you're doing the registry's job by hand. Audit what's currently swept away with `?vanilla_bugs=show&mod_noise=show` (default behavior) or scope to one bucket with `?vanilla_bugs=only` / `?mod_noise=only`.
- **`mod_only=unknown`** is the recommended triage mode. The default `mod_only=true` for debug/error filters out *every* entry whose message body contains no script path — including engine errors fired from vanilla cpp source (e.g. `pdx_data_factory.cpp` "Could not find data system function ...") that are actually caused by mod content. `unknown` keeps mod-path entries AND uncategorized empty-files entries, dropping only entries positively tagged as registered vanilla. Switch to `mod_only=true` if you want the strict "has a script path" view, or `mod_only=false` for everything-including-known-vanilla.
- **The `source` field** (cpp file:line, e.g. `jomini_scriptvalue.cpp:1659`) is what you anchor on when registering engine noise into `vanilla_known_bugs.md` § "Engine noise (source-anchored)" — printing it default avoids a second curl just to look up the source token.

For a histogram of categories instead of entries, use `?summary=true`.

## Log layout

- On disk: `game_logs_path` from `path_constants` — autodetected by `scripts/setup.py`, typically `/mnt/c/Users/<winuser>/OneDrive/Documents/Paradox Interactive/Victoria 3/logs/`. Print it with `python3 -c 'import path_constants; print(path_constants.game_logs_path)'`.
- Families served by `/logs/<family>`: `error`, `debug`, `game`, `gui`, `graphics`, `event_scopes`, `dedicated_server`.
- Generations: `?gen=0` is current, `?gen=1` is the newest backup (= the prior launch).
- `/logs/debug/diff?against=1` — what's new since the previous game session. Run this after a fix to confirm the entries are gone.
- `/logs/sessions` — clusters file mtimes into runs; useful to scope triage to "this session only".

Useful `/logs/<family>` query params: `q=` (substring), `file=` (glob on referenced file path), `category=` (exact filter), `since=HH:MM:SS`, `dedupe_key=message+file|message|category|exact`, `mod_only=true|false|unknown`, `vanilla_bugs=hide|only|show`, `mod_noise=hide|only|show`, `include_external=true|false`.

## Triage workflow

1. Run the canonical command above.
2. **Bulk-fix routine noise first.** Some categories are non-actionable per-entry but trivially fixable in bulk. Doing them up front shortens the actionable list and saves the next session from re-triaging the same noise. See "Bulk-fixable noise" below.
3. For each remaining entry, decide **mod vs vanilla**:
   - Look at the path in `files`. If it doesn't exist under this repo, it's a vanilla file → record it in `docs/vanilla/vanilla_known_bugs.md` if it's not already there, then skip.
   - If it does exist, it's a mod issue → fix it.
4. Group mod entries by category and tackle each batch — cheaper to fix related entries together than ping-pong between unrelated bugs.
5. After fixes: `POST /reload`, re-launch game, then `/logs/debug/diff?against=1` to confirm the fixed entries are gone.

## Bulk-fixable noise

These produce a lot of triage entries that look like errors but are mechanical to fix in bulk. Fix them at the start of the session, before per-entry triage, so they don't crowd the actionable list.

### Missing UTF-8 BOM (`should be in utf8-bom encoding`)

The Clausewitz engine warns once per Paradox `.txt` that lacks the leading `\xef\xbb\xbf` byte sequence. Files still load (the warning literally says "will try to use it anyways"), but every missing-BOM file is one extra triage line forever.

**This is now automatic.** `bom_normalizer.py` runs last in `POST_LOAD_REGENERATORS` and prepends the BOM to any in-scope script file missing it on every full `POST /reload` (`.txt` under `common/`, `events/`, `gfx/`; `.gui` under `gui/`) — so the warning class stays at zero with no manual step. The file-writing generators (`pm_costs`, `resources`, `gen_fleet_entities`, `ig_feminism`, …) also write `utf-8-sig` so their outputs are correct at the source. The normalizer is gated like the other file-rewriting generators: `audits_only` / `mod_only` fast reloads skip it. The manual one-pass fallback below is only needed if you can't run a full reload:

```bash
python3 <<'EOF'
import os
BOM = b'\xef\xbb\xbf'
fixed = []
for r in ['common', 'events']:
    for dp, _, files in os.walk(r):
        for f in files:
            if not f.endswith('.txt'):
                continue
            p = os.path.join(dp, f)
            with open(p, 'rb') as fh:
                data = fh.read()
            if not data.startswith(BOM):
                with open(p, 'wb') as fh:
                    fh.write(BOM + data)
                fixed.append(p)
print(f'Prepended BOM to {len(fixed)} files')
EOF
```

**Before doing the bulk write, check whether any of the files are auto-generated.** Per `CLAUDE.md` § "Don't hand-edit auto-generated files," the auto-gen list lives in `POST_LOAD_GENERATORS` in `mod_state_server.py` and the ownership map in `docs/auto_generated_files.md`. If a generator writes its outputs without BOM (e.g. `ig_feminism.py` previously used `encoding="utf-8"`), the next `POST /reload` regenerates them and the warning comes back. Fix the generator's write call to `encoding="utf-8-sig"` *first*, then run the bulk fix. **Restart the server (`pkill -f mod_state_server.py` then re-start)** after editing a generator — `/reload` doesn't pick up Python source changes.

YAML/JSON/Python files use a different convention; only target the roots `bom_normalizer.py` covers — `.txt` under `common/`, `events/`, `gfx/`, and `.gui` under `gui/`. Don't write BOMs to `.metadata/` or anything outside those roots without verifying the engine wants one. (This mod has no `descriptor.mod`; Vic3 reads `.metadata/metadata.json`.)

### `dds_dimensions` warnings

DDS textures whose width/height aren't multiples of 4 produce one warning per file. Fixing requires re-exporting the DDS through an image pipeline (BC1/BC3 block-compressed needs multiple-of-4 dimensions). Not bulk-fixable from text editing — leave them in the noise pile unless the user asks to re-export.

### Vanilla `00_code_on_actions.txt:7362: <Phrase> Created`

Intentional vanilla `debug_log = "..."` calls that fire every election/movement cycle. Always skip; they aren't fixable.

## Category playbook

`SOURCE_CATEGORY_PREFIX` / `SOURCE_CATEGORY_FILE` in `game_log_reader.py` (and `_classify`, which reads them) are the source of truth for category names. Common ones and first-look fix paths:

| Category | What it usually means | Where to look |
|---|---|---|
| `script_parse_error` | Typo, vanilla rename, unregistered modifier, or unknown trigger | `docs/guides/vanilla_patch_runbook.md` § Known vanilla renames; `/modifier-search?q=` to validate names |
| `inject_to_missing` | Vanilla renamed/removed an `INJECT:` target | Same runbook |
| `duplicated_key` | Top-level entity collision (mod redeclares a vanilla key) | Convert mod's declaration to `INJECT:`. `docs/guides/scripting_best_practices.md` § Top-Level Database Collisions |
| `inconsistent_trigger_scope` / `inconsistent_effect_scope` | Wrong scope for the trigger or effect | `/engine-docs/triggers`, `/engine-docs/effects`; `docs/guides/scripting_best_practices.md` § scope chain |
| `missing_file` | Asset (often `gfx/event_pictures/*.dds`) not found | Substitute thematically-similar existing asset, or remove the reference |
| `missing_texture_for_entity` | Entity references a DDS that doesn't exist | Same |
| `dds_dimensions` | DDS not a multiple of 4 | Visual-only warning; usually skip unless re-exporting |
| `other` with `should be in utf8-bom encoding` | Mod `.txt` file lacks the leading `\xef\xbb\xbf` BOM | Bulk-fix at session start — see "Bulk-fixable noise" below |
| `vfs_mount` | Filesystem/mount issue | Usually environmental, not a mod bug |
| `gui_parse_error` / `gui_widget_error` | GUI script error | `docs/guides/gui_modding_guide.md` |
| `gamedatabase_other`, `localization`, `ai`, `other` | Fallback buckets | Case-by-case |

For **"modifier silently no-ops"** reports that don't show in logs at all: the engine ignores invalid modifier names without logging. Validate via `/modifier-search?q=` and check `common/modifier_type_definitions/mod_entity_modifier_types.txt` for missing per-entity registration of dynamic patterns (`building_*`, `goods_*`, `state_building_*`, `ship_battle_against_ship_type_*`).

## Where lessons live

When you discover something worth recording, route by topic — write it in exactly one place:

- **Engine syntax, scope rule, modifier validation, scripting gotcha** → `docs/guides/scripting_best_practices.md`
- **Log-triage workflow, category interpretation, tooling quirk** → this skill's "Lessons learned" section below
- **One-off vanilla bug** → `docs/vanilla/vanilla_known_bugs.md`
- **Refactor pattern / helper inventory** → `docs/audits/script_parameterization_audit.md`
- **Per-helper context** → that helper's own header comment

This mirrors `CLAUDE.md` § "Recording lessons learned" — don't duplicate across homes.

## End-of-turn requirement

Before ending your turn on a log-triage task, ask: did I learn something a future Claude instance would hit again? If yes, route per the decision tree above. If it belongs here, append a dated bullet to "Lessons learned" below (`- YYYY-MM-DD: …`). One paragraph max — if it's longer, it probably belongs in a doc, not the skill.

The bar is: would the next Claude rediscover this from scratch? If yes, write it down. If it's already obvious from the code or covered in the playbook above, don't.

## Lessons learned

Workflow- and tooling-level lessons accrue here. Engine/scripting lessons go to `docs/guides/scripting_best_practices.md` instead.

- 2026-04-29: `debug.log` is the primary signal; `error.log` only surfaces a small subset (mostly script-value type errors). Always start with the canonical debug.log curl above. Source: `CLAUDE.md` § "Triage workflow for log issues".
- 2026-04-29: `category=other` entries with `source=jomini_effect_impl.cpp:454` and a message body that's a plain English phrase (e.g. "Election Campaign Started", "Conservative Party Created", "Revolutionary Coalition Disbanded") are **intentional vanilla `debug_log = "..."` calls** in `common/on_actions/00_code_on_actions.txt`, not bugs. They dominate `mod_only=true` triage results during election cycles because the categorizer can't distinguish them from real errors. Recognize and skip; don't dig into vanilla source for them.
- 2026-05-02: A referenced file path that exists in **neither** this mod (`ls <path>` from repo root) **nor** vanilla (`"$VAN/game/<path>"`, where `VAN=$(python3 -c 'import path_constants; print(path_constants.base_game_path)')`) is almost always a **third-party Steam Workshop mod**. Locate it under the sibling workshop tree — `find "$(dirname "$(dirname "$VAN")")/workshop/content/529340" -name <basename>` and identify via `<workshop>/<id>/.metadata/metadata.json`'s `name` field. The `mod_only=true` filter can't distinguish workshop mods from this mod, so they leak through. Not our problem — skip without recording in `vanilla_known_bugs.md`. Recent example: `GDPGR_scripted_effects.txt` and `events/gdp_events.txt` Div/0 entries trace to "GDP Growth Rate Improved" v1.2.2 (workshop id 3255320685).
- 2026-05-05: **Use `?vanilla_bugs=hide` (or `=only` to inspect what's tagged) on every `/logs/*` call.** `docs/vanilla/vanilla_known_bugs.md` is now a parsed registry — entries with `### \`path/to/file.txt[:line]\`` headings, an optional fenced code block whose lines become signature substrings, and a body whose backticked path references extend the heading's basenames. Multiple distinct error variants per bug should all go inside the same code block (`tag_vanilla_bugs` matches if ANY signature substring appears in the message, normalized for `:NNN` line refs). Avoid `<X>` placeholders in signatures — they're matched literally and won't hit real messages with quoted values. The `## Expected mod-override noise` section uses the same parser to register the `replace/*.yml` warnings as known by-design noise.
- 2026-05-05: **For comprehensive vanilla-bug discovery, aggregate across all `error*.log` and `debug*.log` generations, not just current.log.** Bugs that only fire under specific conditions (certain country exists / specific JE active / particular UI panel open) often appear in older generations but not current. Pattern: `for path in sorted(glob.glob(os.path.join(game_logs_path, 'error*.log'))): if 'Manfred' not in path and 'pickle' not in path: all_entries.extend(parse_log(path))`. Then `tag_vanilla_bugs(...)` and group by basename — the high-volume basenames are the registration targets. Took error-log tagging from 22% → 86% in one session.
- 2026-05-05: **`add_loyalists` and `add_radicals` share the same `*_radicals` script values.** Vanilla `event_values.txt` only defines `small_radicals = 0.02 / medium_radicals = 0.05 / large_radicals = 0.1`. There are NO `*_loyalists` script values — vanilla event code uses the radicals values for both effects (`acceptance_events.txt` calls `add_loyalists = { value = large_radicals }`). Mod usages of `value = small_loyalists` will silently fail (`Failed to find a valid event target link`) — replace with the matching `*_radicals` value, not a literal. Note: this triple-fires per occurrence — `Failed to find a valid event target link 'small_loyalists'` + `Badly read script value small_loyalists` + `add_loyalists effect [ The 'value' field must be set ]` — recognize as one bug, not three.
- 2026-05-06: **A single `script_parse_error` typically chains downstream `inconsistent_effect_scope` / "Unknown trigger type" / "Unknown effect" entries in the same file.** The parser loses block boundaries after the first failure and mis-attributes valid sibling triggers/effects as unknown. Fix the upstream parse error first, then re-check — the cascade usually disappears on its own. Don't independently chase the downstream entries; they're symptoms, not root causes. Recent example: `country_prestige >= 100` (script value used as trigger) at extra_decisions.txt:160 cascaded into "Unknown effect add_authority" at :164 and "Unknown effect set_country_flag" at :169 — all three were one bug. Same shape with `radical_fraction >= 0.10` (block trigger called as scalar) cascading into "Unknown trigger type: add" at the next line.
- 2026-05-06: **Crashes that ONLY happen on new-game start (load-from-save is fine) implicate `common/history/`.** New-game start runs every `common/history/*` file; load-from-save bypasses them entirely. When a crash is gated specifically on new-game and not on load, the suspect is whatever changed in `common/history/` — not the broader entity definitions, scripted effects, events, or modifier types (those load on both paths). Bisect by removing recently-touched history files first. **Parse-clean ≠ runtime-safe**: the engine can crash inside the C++ effect-execution path (e.g. `create_political_lobby`) without flushing a script_parse_error or any actionable log line — `debug.log` may stop mid-load with only benign notices like `should be in utf8-bom encoding` against the file. Don't trust silence in logs as innocence; the load-vs-new-game differential is faster than further log analysis. Recent example: a single domestic-lobby (`category = foreign`) seed in the mod's then-existing `common/history/lobbies/01_extended_lobbies.txt` crashed new games; logs showed only the BOM warning, no script error. That whole political-lobbies extension was subsequently **reverted** — neither the history file nor `common/political_lobbies/` exists today (design preserved in `docs/archive/political_lobbies_design.md`), so the path is history, not something to go look at.
- 2026-05-09: **The canonical `mod_only=true` filter hides errors whose `files` field never resolves to a mod path, even when a mod loc string is the cause.** Unregistered concept refs (`[concept_X]` with no `common/game_concepts/` entry) fire `Could not find data system function 'concept_X'` from `pdx_data_factory.cpp` — vanilla source, no mod file in the message body — at hundreds of times per render, causing UI lag. The canonical curl returned `Total: 0` while the underlying file had 600+ such lines. **For a thorough sweep, use `?mod_only=unknown` instead of the default `?mod_only=true`** — it keeps mod-path entries AND uncategorized empty-files entries, dropping only entries positively tagged as registered vanilla bugs. The unregistered-concept failure mode itself is now caught at parse time by `concept_reference_audit` (`docs/engine/concept_reference_report.md`), which scans every loc YAML for `[concept_X]` / `[Concept('concept_X', ...)]` references and flags any not declared in `common/game_concepts/`. Engine-fact write-up of the concept-registration requirement lives in `docs/guides/scripting_best_practices.md` § Game Concept Hyperlinks.
- 2026-05-10: **`vanilla_known_bugs.md` entries can parse, persist, and still match nothing if the heading isn't shaped `### \`anchor\` — title`.** A free-text heading like `### Vanilla journal_entries — ...` survives parse but never tags real log entries; only a backtick-quoted anchor at the front (file path, file path + line refs, or cpp source token) gets indexed. Full reload's `warnings` array does NOT flag this — it only catches the related "no file path or source anchor — would match every log entry" case (which fires when the signature block has no script path *and* there's no `- source:` line). The "parses but matches nothing" failure is silent; verify by running the canonical curl after each batch and confirming the entries you expected to swallow are actually gone.
- 2026-05-10: **`POST /reload?engine_only=true` re-parses the bug registry but skips audits.** Useful for fast iteration when adding `vanilla_known_bugs.md` entries (~1s instead of ~77s). But you have to run a full `POST /reload` once at the end to surface the parser's rejection warnings — `engine_only` returns `warnings: []` even when entries were rejected.
- 2026-05-11: **Two registry-heading parse gotchas, both surface as `'X' has no file path or source anchor — rejected` warnings:**
  - **Line-ref format:** the `_PATH_REF_RE` regex in `game_log_reader.py` accepts `\`path.txt:N\`` and `\`path.txt:N, N, N\`` (single colon, then comma-separated numbers), but NOT `\`path.txt:N, :N, :N\`` (repeated colons). The repeated-colon form fails the whole anchor capture, leaving `file_basenames` empty even though the path is right there. Format multi-line headings as `### \`events/foo.txt:112, 205, 206, 210\`` not `### \`events/foo.txt:112, :205, :206, :210\``.
  - **Title-as-signature:** the parser ALSO treats the text after ` — ` in the heading (`_TITLE_AFTER_DASH_RE`) as a signature line. If that title text *looks* like an engine error message (e.g. `Undefined event target 'X'`), the parser captures it, fails its anchor check, and rejects the whole entry. Keep titles paraphrased ("unbound option-branch scope") rather than verbatim engine-error keywords.
- 2026-05-14: **`debug.log` rotates aggressively — capture the canonical triage output to a file before drilling in.** Vic3 caps each debug.log at 524 KB, then rolls 0..5; a 5-launch session (or any session with high-volume mod-side spam) can scroll your initial gen=0 findings entirely out of all six retained files. Pattern: run the canonical curl, drill into one entry, return for the next entry — by then the user may have re-launched the game and gen=0 is a fresh 5 KB file. Earlier findings (line numbers, repeat counts) are still valid against the mod files themselves (those haven't changed), but the API view of them is gone. Solution: `curl -s "http://localhost:8950/logs/debug?..." > /tmp/debug_triage.json` early; re-query with `python3 -c "import json; ..." < /tmp/debug_triage.json`. The /tmp file survives the rotation. Same pattern for `error.log`. The `summary=true` view is small enough to keep in conversation but the full deduped entry list is not — save it.
- 2026-05-16: **`pdx_3dtypes.cpp:217` "Total time X_mesh in N ms" entries can dominate a triage round (671 of 697 in one session).** Pure 3D asset-load instrumentation, never actionable. Now registered in `vanilla_known_bugs.md` § "Engine noise (source-anchored)" with signature `Total time "` so `vanilla_bugs=hide` swallows them. If a triage curl returns an inflated count dominated by mesh-load entries, the registry entry may have been removed or the source line shifted in an engine patch — check `vanilla_known_bugs.md`.
- 2026-05-16: **Engine assertions (`pdx_assert.cpp:637`) mix actionable mod bugs with vanilla noise — message body distinguishes.** This source emits both: vanilla internals (`IsWithinBounds( ProvinceIndex )`, `_ProvinceSet.GetNumRanges() <= 1`, `Possible loss of data when converting from int to float` — register and ignore) AND mod-caused asserts (e.g. `Exceeding maximum amount of Prestige Goods per Goods for engines (3)` — fix the mod). Don't auto-suppress all `pdx_assert.cpp:637` entries; register each distinct vanilla message individually so new mod-caused asserts still surface. Each `vanilla_known_bugs.md` entry needs a distinct heading + signature substring so the parser can tag them separately.
- 2026-05-16: **The "single-script_parse_error cascades downstream" rule does NOT apply to engine-foreign keywords.** When the engine reports "Unknown trigger type: X" / "Unknown effect: X" / "Unexpected token: X" on a keyword that looks valid but isn't actually in Vic3 (Stellaris/EU4 imports like `is_triggered_only`, `has_country_flag`, `set_country_flag`, `has_leader = yes`), each occurrence is independent — the parser doesn't lose state because the surrounding block parses fine, just the unknown token gets dropped. Don't defer these as "investigate later, might be cascades" — investigate immediately. Detection: `grep -rl "<keyword>" "$(python3 -c 'import path_constants; print(path_constants.base_game_path)')/game/" | head -3` — zero vanilla hits means engine-foreign. See `docs/guides/scripting_best_practices.md` § "Stellaris/EU4 Idioms That Silently Fail in Vic3" for the equivalence table.
- 2026-05-24: **`Duplicated event ID 'X' found. New Location: A, Previous Location: B` — the engine KEEPS `Previous` (B) and rejects `New` (A).** So a mod event override of a vanilla event *works* iff the mod file is reported as `Previous`. Example this session: `te_formation_overrides.txt` (mod) was `Previous`, vanilla `misc_unifications.txt` was `New` → mod's `formation.17` won, the warning is benign by-design noise. A drift-warning comment that says "if you see Duplicated key X, the engine dropped THIS copy" is too coarse — it ignores the New/Previous field, which tells you *which* copy was dropped. Only conclude the override failed if the **mod** file is `New`. (Definitive confirmation still needs in-game observation of the overridden behavior.)
- 2026-05-24: **`Event X is orphaned` (`jomini_eventmanager.cpp:376`) = the event is fully defined but reachable from no dispatch path.** For this mod's law-enactment events, dispatch is `random_events = { <weight> = <id> }` lists in `common/on_actions/law_events_on_actions.txt` (advance / debate / stall pools) — NOT explicit `trigger_event = { id = X }`. An orphaned enactment event is almost always one the author wrote (full loc + options) but forgot to add to the matching pool; fix by adding `10 = <id>` to the pool whose theme matches the event's `# ... – <X> flavor` header comment. Distinguish from parse-cascade orphans: check the event's line number vs. any parse-error line in the same file — orphans *before* the parse error are genuinely unreferenced, not cascade fallout.
- 2026-05-24: **A rotated/late `debug.log` hides parse-time errors; only a fresh game-start log has them.** A first triage pass on a mid-session log showed just 2 runtime entries; after a relaunch the fresh startup log surfaced ~10 more (parse errors, BOM, duplicate-key, orphans) that load only at game start. After any fix-verify relaunch, re-run the full canonical curl — the "clean" view from a rotated log is not proof the mod is clean.
- 2026-05-24: **Ad-hoc `/logs/error?...` queries need `mod_noise=hide` too, not just `vanilla_bugs=hide`.** The canonical *debug* command sets both, but when you drop to error.log with a hand-built query (e.g. `mod_only=unknown&vanilla_bugs=hide`) and omit `mod_noise=hide`, already-registered mod noise re-surfaces and looks like a fresh finding. This session the harvest-condition sound-entity warnings (`Couldn't find any animation state for harvest condition type` — already in `mod_known_noise.md`, tracked `open_issues.md#l7-mod-harvest-condition-sound-entity-states-missing`) appeared "new" purely because the error.log query lacked `mod_noise=hide`. Mirror the canonical filter set on every family.
- 2026-05-24: **`GetCustom` in a JE `name` loc has no vanilla precedent and emits a per-render `jomini_custom_text.h:91 "Object of type 'country' is not valid for 'X'"` burst** — even when the title renders correctly. A JE name like `[GetPlayer.GetCustom('te_banking_cycle_title')]` (custom loc `type = country`) renders fine but the JE name appears to evaluate in a `journal_entry` data context that validates the country-typed `GetCustom` against the call-site rather than `GetPlayer`'s return. Measured ~83 error.log writes in a ~1s burst per panel render (0 in debug.log); cosmetic, only cost is synchronous-write hitch. Registered as tracked mod noise (`mod_known_noise.md`, `open_issues.md#l11-banking-cycle-je-title-custom-loc-scope-validation-burst`). Candidate fix (resolve the country from the JE scope chain instead of `GetPlayer`) deferred. Discriminator: `grep -rln "GetCustom" "<vanilla>/localization/english/"*journal*` returns nothing — vanilla never does this.
- 2026-07-03: **A vanilla patch bump invalidates source-anchored registry entries (cpp line drift) and spawns new-validation error classes — do both checks up front.** (1) The 1.13.9 engine moved `character_templates_utils.cpp:1812` → `:1814`, silently un-tagging the registered missing-last-name noise; when a "new" entry looks familiar, grep `vanilla_known_bugs.md` for its signature text before re-diagnosing, and keep both old+new `- source:` anchors on re-anchor. (2) Date every unfamiliar error class against pre-patch generations (`for g in error.*.log; do grep -c "<sig>" $g; done` — all-zero pre-patch = new engine validation surfacing old state, not a new mod regression). 1.13.9 examples: law-retention warnings (474, all pre-existing state), create_building tech refusal (69, real behavior change), set_institution_investment_level validation (18, issue #226).
- 2026-09-13: **Older log generations can be OneDrive cloud-only placeholders** — `grep` on `debug.2..5.log` returned `Input/output error` because the files were never downloaded to this machine (the logs folder is OneDrive-synced across machines). The API silently returns nothing for them, so "0 hits in older generations" is not evidence an error class is new. Date error classes against the vanilla clone's version commits (`git -C <vanilla_source_repo_path> grep ... <OLD_REF>` vs `<NEW_REF>`) and the Modding-Digests docs dumps instead, or ask the user to mark the logs folder "Always keep on this device".
- 2026-09-13: **error.log `files` lists the whole script call stack, so a third-party mod's error can be attributed to this mod.** A Workshop mod's on_action (`historical_record_on_actions.txt`, "历史记录", id 3772525185) fired from a law change our `extra_on_actions.txt` / `extra_law_consistency_generated.txt` made, and the server grouped ~9,700 errors under our files. Read the `Script location:` line (first frame) before treating an entry as mod-owned; if that file exists in neither repo nor vanilla, it's the third-party mod. A 5-generation error.log rotation inside one second is usually one such spam burst.
- 2026-09-14: **A diplomatic action's `accept_effect` erroring hundreds of times a second is a tooltip render, not repeated acceptances.** Tells: error.log rotates several times within seconds. Every generation cites one action's `accept_effect` line (`covert_operations.txt:766`, then `:1050`, then `:351`) and it changes as the user hovers from action to action. Lines that only write (`add_to_variable_list`) stay silent while scope changes (`scope:x = { }`) error. A real acceptance fires once per pact. The fix is an `exists` guard on the scope, not a logic change (`scripting_best_practices.md` § Script Containers, rule 6). Also, an error line inside a parameterized scripted effect can be a few lines off: `:111` meant file line `:114` in `covert_op_start`, and the cause is unknown. Before suspecting a stale deploy, `cmp` the repo file against `mod_deploy_target`, then look a few lines around the reported number.
- 2026-09-20: **When the game is running while you triage, snapshot the logs to the scratchpad and analyse them with a direct `game_log_reader` import — the server can't boot fast enough.** Cold start is ~110s to load plus the full generator/audit chain before the port binds (~3.5 min total), and a live session with a render-loop spam burst rotates all six `debug.*.log` / `error.*.log` generations inside that window. `cp` every generation first, then: `parse_log(p)` per file, `filter_external_mods(entries)`, `load_vanilla_bug_registry('docs/vanilla/vanilla_known_bugs.md')` + `load_mod_noise_registry('docs/audits/mod_known_noise.md')`, then `tag_vanilla_bugs(entries, by_basename, by_source)` once per registry (the second pass won't overwrite the first's matches). That reproduces the canonical curl's filtering across *all* generations at once — this session it went 34,399 parsed → 15,228 untagged → 19 after registry work, and the 19 were exactly the actionable mod bugs. `/logs/*` only reads `gen=0` by default, so even a healthy server would have missed the rotated-out ones.
- 2026-09-20: **Check cpp source-anchor drift before triaging — a vanilla bump silently un-tags whole registry entries.** The 2026-07-03 lesson said to watch for it; this makes it a mechanical first step. Compare the registry's `- source:` tokens against the sources actually present in the logs, grouped by cpp basename: a basename registered at one line and observed only at a *different* line is drift. This session `pdx_assert.cpp:637` → `:641` (11 entries, silently dead) and `pdx_gui_localize.cpp:140` → `:302`. Keep the old anchor and add the new one — `- source:` lines are a list, and signature substrings still filter within a source match, so an extra anchor cannot over-swallow. Script:

```python
reg = set(by_src) | set(mod_by_src)
seen = collections.Counter(e.source for e in entries if e.source)
for b, regs in itertools.groupby(sorted(reg), lambda s: s.split(':')[0]):
    obs = {s for s in seen if s.split(':')[0] == b}
    regs = set(regs)
    if obs and not (regs & obs):
        print('DRIFT', b, sorted(regs), '->', sorted(obs))
```
- 2026-09-20: **A burst that interleaves several *vanilla* files in one frame is one UI render, and the mod file mixed into it is the actionable one.** `28_invite_to_power_bloc.txt:87`, `14_treaty_port.txt:425` and `00_infamy_values.txt` cycled together ~15,000 times in six generations; buried two-per-frame in the same interleave were 10 lines from `common/scripted_effects/extra_effects.txt`. Sort by count and you chase vanilla; group by *frame* (identical timestamp) and the mod entry stands out. Both root causes were the same class — a preview pass evaluating an effect/value tree whose scopes are only bound at execution time.
- 2026-09-20: **A missing-asset entry must be source-anchored — the registry's basename index only accepts `.txt` / `.gui` / `.yml` / `.yaml`.** `_PATH_REF_RE` in `game_log_reader.py` is `` `([A-Za-z0-9_./-]+\.(?:txt|gui|yml|yaml))(?::[0-9,\s\-]+)?` ``, so a `missing_file` / `missing_texture_for_entity` entry whose only file reference is a `.dds` can never be tagged by path, no matter how the heading is written — and the parser does not warn, because the heading still carries a valid `.gui` anchor for the file that *references* the asset. Give those a `- source:` line (e.g. `virtualfilesystem.cpp:569`) and pin the signature to the specific asset. Same trap for the second and third file in a multi-file entry: only backticked `.txt`/`.gui`/`.yml` paths **in the body prose** extend the basename index — a path that appears only inside the fenced signature block does not, so `03_conquer_state.txt` stayed untagged until it was named in a sentence. After adding any entry, re-run the tagging over a snapshot and confirm the count actually dropped; `POST /validate/registries` returns 0 warnings either way.

- 2026-09-20: **A UI section that renders as *nothing* is a log-spam suspect, not a script-logic bug — check `debug.log` before reading the script.** A history-chart marker tooltip showed no marker section at all; the loc, the scripted GUI and the recorded variables all looked correct on paper. The log named the cause outright, 2,239 times in two and a half minutes (and again in error.log): `pdx_data_callstack.cpp:325 New data types does not contain a 'JournalEntry' data context`, then `Promote 'SetRoot' returned nullptr`, then `pdx_data_localize.cpp:151 Data error in loc string '<key>'`. That triple is the signature of a loc expression asking for a data context the render does not have; the third line names the offending key directly, which beats any amount of reading. Two gotchas when confirming a fix: `filter_external_mods` + both registries leave this class untagged (it is real and ours), and if the user is committing alongside you, `git show HEAD:<file>` is **not** a pre-fix baseline — reintroduce the bug in a scratch copy of the tree instead, or you will 'prove' a regression guard works while it silently matches nothing.

- 2026-09-21: **A clean `docs/engine/*_report.md` is not evidence of a clean audit — check the report's timestamp against the content it covers.** 612 debug lines + 32 error lines traced to `[concept_leverage]` (vanilla's concept is `concept_power_bloc_leverage`) in five loc values, while the committed `concept_reference_report.md` said `total flags: 0`. The report was regenerated at 07:52 and the offending loc landed at 17:32 the same day — the feature PR shipped without a full `POST /reload`, so nobody re-ran the audit. `git log -1 --format=%ci -- <report>` vs `-- <source file>` settles stale-vs-blind-spot in one command; to actually prove the audit isn't blind, most of these modules take an explicit path (`concept_reference_audit.audit(ms, mod_path=<scratch tree>)`) and `ms` can be a `types.SimpleNamespace(mod_parsers={'Game Concepts': <stub with .data>})`, so you can run the pre-fix file through the real audit in seconds without touching the working tree.
- 2026-09-21: **A live loc key can be exiled to `te_unused_l_english.yml` and still render — and log errors from there.** All `*_l_english.yml` under `localization/english/` load regardless of filename, so organize_loc's "unused" bucket is a filing decision, not a disabling one. The five `[concept_leverage]` strings above were `<treaty_article>_effects_desc` keys: an engine autokey family (`<article>`, `_desc`, `_effects_desc`, `_article_short_desc`, `_effect_desc_first/_third/_global`) that is never referenced from script, so `organize_loc.py` had no way to see it — 49 live keys across 34 articles were filed as unused. Fixed by adding `find_treaty_article_keys`. The general shape (per `te_unused_l_english.yml` being an organize_loc *output*): when a log error cites a key living in te_unused, the filing is the second bug — fix the detector, don't hand-move the key.
- 2026-09-23: **For a crash, start from `<user dir>/crashes/victoria3_<stamp>/`, not the rotating logs.** Each folder holds `exception.txt`, `meta.yml` (exe version, launch args, **the enabled mod list**) and a `logs/` copy of debug/error/game.log frozen at the moment of the crash, so rotation can't erase the lead-up. Two cheap signals do most of the work. (1) The same fault address in every `exception.txt` means one deterministic code site, not random memory corruption. (2) A crash still happening with a smaller mod list in `meta.yml` rules out whichever mods were dropped. To see what a Steam patch changed, run `find "<base_game_path>/game" -newermt "<day before>" -type f`. Steam rewrites only the files that changed, so that is the patch's file list; the exe's mtime tells you when the update landed. Then grep the crash-folder logs for `... is missing X!`-style warnings naming mod entities. A new mandatory field emits one such line per offending entity and keeps loading. Here it was `ship_type.cpp:385: Ship Type is missing ai_ship_category!` × 19 mod ship types, after 1.14.4 added `common/ai_ship_categories/` (see `scripting_best_practices.md` § Every Ship Type Needs `ai_ship_category`).
- 2026-09-25: **A raw loc key in the UI never shows up in any log — go straight to the key family, not the logs.** The engine logs nothing for a missing key (not even with `mod_only=unknown`), so a screenshot of `<entity>_<suffix>` is the whole signal. Check whether `loc_coverage_audit._REQUIREMENTS[<entity type>]` models that suffix. If it is still `_simple_name`, the engine's autokey family for that type is probably unmodelled. Read the family off vanilla loc (which suffixes exist, keyed against the entity's flags), then add a derivation function like `_diplomatic_action_keys`. Check `organize_loc.py`'s matching `find_*_keys` suffix list too: an unmodelled family usually also leaves live keys filed in `te_unused_l_english.yml`. Example: `nd_nuclear_ultimatum_action_action_notification_third_party_desc` (PR #433).
- 2026-09-26: **The drift script's `DRIFT` lines need the message checked before adding an anchor.** It groups by cpp basename, so one file that emits *different* messages from different lines looks like drift: `application.cpp:1861` (registered, "Quit from inside game") vs `:925` ("Starting checksum calculation"), and `character_templates_utils.cpp:1812/1814` (missing last name) vs `:40` (a mod ideology given to a character, a real mod bug). Re-anchor only when the observed line carries the registered entry's signature text.
- 2026-09-26: **For `Unknown modifier type` after a vanilla patch, trust `/validate/vanilla-surface-diff?old_ref=HEAD`, not `/engine-docs/origin`.** The engine-docs dump predates the installed patch until someone runs `script_docs` in game. So `/engine-docs/origin` answers `found: true, origin: vanilla` for `country_prestige_from_navy_power_projection_mult`, a name 1.14.4 renamed, and `found: false` for its replacement `…navy_combat_power_mult`. Going by the dump, #436 switched a modifier from the new name back to the old one two days after #386 had renamed it, and #424 used the old name too. #436's commit message even calls the five correct uses a bug left "for a separate fix". The surface diff's `removed_and_used_by_mod` listed it the whole time. `test_validate_migration`'s HTTP smoke test fails on the same entry, so treat that failure as a lead, not flake. Tooling gap tracked in #391.
